---
reviewed: false
merge_ready: false
---

# 카드 — PR #507: 뷰포트 매트릭스 회귀 하네스 (런치 게이트 a10, 교차 리뷰 + fix1 완료)

작성: 냐옹이 Gen 6, 2026-08-13. 원문 SSOT: PR #507 본문(fix 라운드 1 절 포함) + 교차 리뷰 문서(swatch-ops contracts/2026-08-13-pr507-codex-review.md).

## 1. 뭐가 생겼나

§9 결정 2의 집행: 뷰포트 320·375 + breakpoint 경계 499/500에서 3화면(홈/탐색/프로필)의 레이아웃 분기를 판정하는 playwright 매트릭스(12케이스) + **매트릭스 전용 CI job**. 오프라인 하네스(백엔드 불요 — 세션·온보딩 상태를 localStorage에 심음, 출시 DB에 테스트 데이터 안 남김).

## 2. 서사 (이 PR의 진짜 값어치)

1. 워커가 하네스를 만들고 **음성 대조**로 증명 (CSS 경계 1px 이동 → 3/3 실패).
2. **codex 교차 리뷰가 거짓 초록 구멍 2건(높음)을 적중**: ① 화면이 빈 깡통/에러여도 통과 ② project 폭 오설정을 자기일관으로 통과. 마스터 소스 대조로 전건 확정 — 감시선 코드에 2차 시선을 넣는 규칙이 또 값을 했다 (PR #500·#501 계보).
3. fix1: 화면별 data-testid 앵커(src 4줄, 선례 있음) + project→기대폭 계약표. **음성 대조 3종 재실측** — 특히 ③ 탐색 화면 강제 에러 실험은 리뷰가 지적한 바로 그 시나리오이고, 수리 전엔 통과했을 케이스가 이제 잡힌다.

## 3. 부산물 (별도 처리)

- **발견물**: Playwright CI가 7월 6일 문서 PR #328에서 사고성 삭제된 상태였다 — 복구 여부는 네 결정 대기(sw-8gm).
- 기존 스모크 1건은 로컬 env 부재로 선재 실패(이 PR과 무관, 본문 명시). CI 복귀도 sw-8gm에서.

## 4. 검증 (마스터 독립 재측정)

매트릭스 12/12 재실행 통과, 앵커 4줄 diff 실측(동작 영향 0), CI 체크 'Viewport breakpoint matrix' SUCCESS 실물, vitest 566 정합(+2는 머지해 온 #505 테스트), 스레드 0, 충돌 0.

## 5. 네 클릭

퀴즈 통과 후 머지. 머지 후 자원 회수(a10 터미널)와 워크트리 UI 삭제 요청은 내가 배치로 묶는다.
