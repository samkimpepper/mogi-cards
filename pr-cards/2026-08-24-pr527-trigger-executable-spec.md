---
reviewed: true
merge_ready: false
---

# 리드 카드 — PR #527: 회수·승격 트리거 executable spec (sw-20n)

작성: 냐옹이(Gen 13), 2026-08-24. **최종 READY — HEAD `9455c21`, 리뷰 루프 종결(필수 0).** 라운드 이력: 교차 리뷰 필수 2·후가능 3(`../../swatch-ops/contracts/2026-08-24-pr527-cross-review-verdict.md`) → fix1(5건) → delta 신규 필수 1(fix1이 심은 실패 흡수) → fix2 → delta2 클린(`-delta2-verdict.md`). 픽스처 7 PASS · 전량 27/27+하네스 6/6 · 음성 대조 3종 · 마스터 독립 재실측 매 라운드.

## 정체 확인 (머지 인계 최소선)

1. **뭔지**: 네 "Supabase 질렸다" 절규에서 나온 A+B 설계의 **B 1호** — 코드 한 줄 안 바꾸고(동작 변경 0·마이그 0) #526 트리거들과 대표 재선정 기계의 현재 동작을 **실행 가능한 테스트로 박제**. "로직이 DB에 숨어있다"는 네 고통 1번을 직접 깎는 물건이다.
2. **백미**: sw-kya의 동시성 결함(동시 unmap → 매핑 없는 사진이 대표로 잔존)을 **결정적으로 재현하는 하네스** — 이제 재설계는 "이 스펙 전량 초록 + 이 결함 재현이 실패로 바뀜"이라는 명확한 합격선을 갖는다. 결함을 고치기 전에 결함을 재현 가능하게 만든 것.
3. **특성화 원칙**: 테스트는 "바람직한 동작"이 아니라 **현재 동작**을 적는다 — 리뷰 판정도 "기대값 전건 일치, 몰래 고쳐 적은 곳 없음". 결함 재현 부분엔 "이것은 결함의 문서화다" 경고가 파일에 박혀있다.

**policy-snapshot과의 관계 (네 완독 질문, 2026-08-24)**: 완전 별개다 — 스냅샷은 권한(RLS·GRANT)의 상태 사진, 이 PR은 동작의 실측. 같은 CI 잡이 둘 다 돌려서 헷갈리기 쉽다. 이 PR은 마이그 0이라 권한 변화 0 → 스냅샷 diff 0이 정상이다.

## 네가 알아야 할 미결 2건

- **E3 의도 판정 (sw-5m6, P3)**: 어드민이 shade_id만 비우면 slug 캐시로 매핑이 사실상 유지되고 회수가 무동작 — 테스트는 이 현재 동작을 그대로 적었고, 의도인지 결함인지는 네 판정 대기(5분 대화감).
- **하네스 견고성 후속 (sw-a8q, P3)**: 로컬 한정·저확률 4건(프로세스 트리 회수·trap 재진입·barrier 소유자·빈 임시 디렉토리) — 리뷰가 "머지 후 가능"으로 분류, 루프 밖으로 뺐다.

## 결정 골격 (원문 포인터)

- 설계: `../../swatch-ops/docs/decisions/2026-08-24-kotlin-sidecar-and-test-first-design.md` (A+B, 네 승인) · 계약: `contracts/2026-08-24-trigger-executable-spec.md` + `-pr527-fix1.md` + `-fix2.md`
- 리뷰 3부: `-pr527-cross-review-verdict.md` → `-delta-verdict.md` → `-delta2-verdict.md` (원문 .out 3본 보존)
- 이날의 사고 1건: 발주 base를 main으로 잘못 그음(대상은 dev) — 워커 선측정이 검출, 네 절충 결정으로 조항 16+카운터(sw-857) 제도화.

## 머지 후

원격 창 **없음** — 테스트 파일뿐이라 마이그 적용 절차 자체가 없다. CI migration-verify가 PR에서 자동으로 돌고, 머지 후 남는 일은 sw-kya 재설계 계약 발주 시 이 스펙을 합격선으로 물리는 것뿐.
