---
type: personal
status: active
owner: samkimpepper
created: 2026-07-02
topic: 모기 리드 리뷰 — LAUNCH-03 RPC anon 감사 + 가드 (PR 승인용 SUMMARY Read Card)
guide: guide-mogi-read-cards.md
---

# 모기 리드 리뷰 — LAUNCH-03 RPC 감사 (2026-07-02)

> 원문이 SSOT. 이 카드는 읽는 길잡이 + 승인 체크포인트.
> 원문: `supabase/migrations/20260702010000_...sql` (revoke) · `20260702020000_...sql` (가드) · `.planning/todos/pending/rpc-missing-auth-guards.md` (잔여)

### [READ] 완료된 것 (3줄)

- 비로그인(세션 없는 호출자)이 쓰기·어드민·레거시 RPC 28종을 호출할 수 있던 표면을 전부 회수했다. 게스트·유저 플로우는 영향 없음 (게스트도 세션이 있어서).
- 대표 사진: 그 shade 에 실제 등록된 발색 사진 URL 만 대표가 될 수 있게 검증 추가. 자동 채움 규칙(D-099)과 모기 수동 정리는 그대로.
- 텍스처 태그: 게시된 shade 는 어드민만 수정 가능 (미게시 초안은 기존대로).
- *모기질문*: 대표사진은 그럼 원래는 실제 등록된 발색 사진. 그 쉐이드에 등록된 발색사진이 아니라 다른 발색사진도 대표가 될수 있었어? (비교발색 제외하고.)
- *답 (Claude)*: 함수 차원에서는 응 — 다른 shade 사진이든 외부 인터넷 이미지든 아무 URL 이나 가능했어. 다만 정상 앱 플로우는 클라이언트가 방금 등록한 그 사진만 넘겨서 실제로 일어난 적은 없을 것. 즉 "UI 로는 안 되지만 API 직접 호출로는 되던" 걸 이번에 서버에서 막은 것.

### [READ] 계획과 달라진 점

- 원래 예상 = "권한 표면 위생 정리(P2)". 실측하니 **가드 없는 SECURITY DEFINER 4종 = 실구멍(P1)** 이었다. 2건(대표 사진·텍스처)은 이번에 닫았고, 2건은 모기 결정대로 후속(본인 확인 = Phase 3, 미사용 라인 = 현상 유지).
- 덤 수정: 텍스처 저장 시 primary 미지정이면 저장이 터지던 기존 버그(원본 20260619010000)를 같이 고침.
- *모기질문*: 아뭐야. 버그 이번세션에 고친거야? 왜 말을 안해ㅡㅡ 어떤 동작으로 수정한건지도 말해줘.
- *답 (Claude)*: 보고에 한 줄 있었는데 묻힌 건 인정 ㅋ. 동작으로: **전** = 텍스처 칩만 고르고 "대표 텍스처" 를 안 정한 채 저장 → DB 에러로 저장 전체 실패. **후** = 대표 미지정이면 전부 비대표(false)로 정상 저장. 원인 = SQL 에서 NULL 과 비교하면 결과도 NULL 인데, 그걸 true/false 만 받는 `shade_traits.is_primary` 에 그대로 넣고 있었음.

### [READ] 아직 완료가 아닌 것

- **QA 원격은 아직 예전 상태.** PR 머지 → 모기 `supabase db push` 전까지 원격엔 아무것도 안 바뀜.
- LAUNCH-03 나머지 축: 테이블 RLS 전수 + D-073/D-074 cross-check 는 별건으로 남음.
- *모기질문*: D-073, 074는왜 크로스체크해야되?
- *답 (Claude)*: 둘 다 표준 RLS 패턴에 **일부러 낸 예외**라서. D-073 = `shade_color_families`/`shade_textures` 로그인 유저 전부 쓰기 허용 (첫 등록자 정책, 4a 완화). D-074 = `swatches`/`swatch_items` owner-only 위에 어드민 override 추가. "anon read + auth write + admin override" 잣대로만 감사하면 이 둘이 위반처럼 보이거나, 반대로 의도보다 넓게 열린 걸 놓침. 그래서 "예외가 결정 범위 그대로인가" 를 따로 대조하는 것. **덧**: 오늘 텍스처 게이트("게시되면 어드민만")는 D-073 의 "전부 허용" 보다 좁힘 — 모기 오늘 결정이 최신이라 우선하지만, RLS 감사 때 D-073 보충 메모 후보.

### [EVIDENCE] 검증한 것

| 증거 | 등급 | 무엇을 증명하나 |
|---|---|---|
| 로컬 감사 쿼리 재실행 | 동작 검증 | anon 잔여 = 읽기 9종 + 트리거 함수뿐 |
| 로컬 픽스처 4케이스 | 동작 검증 | 임의 URL 거부·정상 URL 통과·게시 shade 텍스처 거부·미게시 통과 |

### [DECIDE] 모기 손으로

- [ ] PR (fix/launch03-rpc-anon-execute-revoke → dev) 머지 승인
- [ ] 머지 후 `supabase db push` + 감사 쿼리 원격 재실행 (→ 어디 보면: 20260702010000 마이그 맨 아래 주석에 쿼리와 기대 결과 있음)
- [ ] `guest-anon-auth-can-write-comparison-notes` todo 닫기 동의 여부 — RPC 가드로 이미 해결 확인됨

### 모기 손으로 (주관식 · 짧게)

> 빈 줄에 한두 줄. 모르겠으면 "모르겠음 + 왜" 라고 적어도 됨.

1. **revoke 했는데 왜 안 끝났나** — 이번에 anon EXECUTE 를 전부 회수했는데도 "게스트가 남의 미게시 초안을 수정할 수 있는" 문제가 남은 이유를 한 줄로:
   - 힌트: 게스트가 로그인 안 한 것 같아도 DB 입장에서는 무슨 role 로 보이는지 생각.
   - → 어디 보면: 이 카드 [READ] 완료된 것 첫 줄 괄호 + `rpc-missing-auth-guards.md` § 남은 것 1
   - 답: 그거 게스트도 세션이라서. 게스트도 DB에 데이터가 남음(role이 anon일뿐..) 근데 어떤 시나리오에서 게스트 세션이 남의 미게시초안 수정할수있는지는 모르겠어 미안ㅋ
   - 채점 (Claude): **5/10.** "게스트도 세션이라서" 까지 맞고, 괄호에서 정답 뒤집음 — **게스트 role 은 anon 이 아니라 `authenticated`** (is_anonymous=true 딱지만 붙음). anon = 세션 자체가 없는 호출자. 시나리오: 공격자가 앱 화면 없이 개발자도구로 자기 게스트 토큰을 들고 `update_inline_product_shade_v2` 에 남의 shade_id(1, 2, 3… 순번이라 추측 쉬움)를 넣어 호출. **UI 가 안 보여주는 것 ≠ 서버가 막는 것.**

2. **대표 사진 검증의 거절 기준** — 이제 대표 사진 함수가 어떤 URL 을 거절하나? 그리고 일반 등록 플로우가 안 깨지는 이유 한 줄:
   - 힌트: "이 사진이 어디에 이미 저장돼 있어야 통과인가" 로 생각. 등록 순서(발색 등록 → 대표 채움)도 힌트.
   - → 어디 보면: `20260702020000_launch03_rpc_guards.sql` 1번 함수의 "가드:" 주석 (45행 근처)
   - 답: swatch_items에 연결된 shade_id, shade_slug로 조건 거는군. 그럼 지금까진 안했다는거야..? 허허.. 그래도 대표사진함수에 다른 쉐이드 넣을 진입점은 없었기는 했어..
   - 채점 (Claude): **6/10.** join 키만 읽고 본체 누락 — 핵심은 **"URL 이 그 발색의 `swatches.image_urls` 배열 안에 실제로 있어야 통과"**. "진입점 없었다" 는 1번과 같은 오해 반복: RPC 는 anon key 만 있으면 UI 없이 호출되는 공개 API 라 진입점은 항상 있었음. 둘째 물음(플로우 왜 안 깨지나) 무응답 — 등록이 먼저 성공해서 사진이 이미 DB 에 있고, 그 다음에 대표 채움이 돌아서 통과.

3. **"게시됐다" 의 데이터 기준** — 텍스처 게이트가 말하는 "게시된 shade" 는 DB 에서 정확히 어떤 상태인가? (어느 테이블에 뭐가 있으면 게시?)
   - 힌트: shade 혼자로는 게시가 아님. 발색(swatch)이 그 shade 를 가리키는 연결이 생기는 순간부터.
   - → 어디 보면: `20260702020000` 마이그 2번 함수 게이트 EXISTS 절 + `rpc-missing-auth-guards.md` § 처리 완료 둘째 줄
   - 답: 그러니까. swatch_items의 shade_id, shade_slug에 있는 shade가 게시된거아님?ㅋ 근데 이건 왜물어봐? 이거 예전에 게시되면 수정 못하게 한다고 정책세웠었던 것 같기도 하고 깅거이 안나네; 그래서 물어본건가.
   - 채점 (Claude): **9/10.** 정답. 기억도 맞음 — `update_inline_product_shade_v2` 의 `shade_already_published` 게이트(20260620010000)가 그 정책이고, 텍스처 게이트를 **그 기존 기준과 같은 잣대로 맞췄다** 는 걸 확인시키려고 낸 문항. -1 은 답을 물음표로 끝내서 ㅋ

4. **지금 이 순간의 QA 원격** — 아직 db push 전인데, 지금 비로그인 상태로 QA 원격의 `create_dupe_pair` 를 직접 호출하면 어떻게 되나? (호출 가능 여부 / 데이터가 실제로 생기는지 나눠서)
   - 힌트: 오늘 고친 건 "방어선을 두 겹으로" 인데, 원격엔 아직 몇 겹이 살아 있는지. 첫 겹과 둘째 겹이 각각 뭔지 생각.
   - → 어디 보면: 이 카드 [READ] 아직 완료가 아닌 것 + `.planning/todos/pending/supabase-rpc-anon-execute-audit.md` § 증상
   - 답: 엇.. 근데 이거에대한 답변은 아니긴한데. create_dupe_pair가 혹시.. 레거시 dupe_pairs 테이블에 create하는건 아니지 설마?
   - 채점 (Claude): **0/10.** 답 안 하고 되물음으로 회피 ㅋㅋ 정답: **호출 자체는 됨** (원격엔 anon EXECUTE 아직 살아있음 — 오늘 만든 첫 겹이 push 전이라 없음) / **데이터는 안 생김** (함수 안의 `auth.uid() IS NULL → login required` 거절 — 둘째 겹은 원래부터 있었음). 지금 원격도 데이터는 안전, 표면만 열린 상태.
   - 되물음 답: `create_dupe_pair` 는 **`comparison_pairs`(현행 테이블)에 INSERT** (20260624020000:78). `dupe_pairs` 는 D-056/D-059 재구조화 때 이름이 바뀐 옛 명칭이고 FK 제약 이름에만 흔적 남음. 레거시 테이블 아님, 안심.

#### 총평 (Claude)

- **총점 20/40** ㅋ
- 다시 볼 것은 딱 하나: **게스트 = `authenticated` (세션 있음), anon = 세션 없음.** 이거 하나 잡히면 1·2·4번 오해가 전부 풀림.
- 오늘의 교훈 문장: **"UI 가 안 보여주는 것과 서버가 막는 것은 다른 문제다."** RPC 는 화면 없이도 호출되는 공개 API 다.

### [FOLLOW-UP] 머지를 막지 않는 후속

- 미게시 초안 본인 확인 (Phase 3, `rpc-missing-auth-guards.md`)
- 레거시 RPC 3종 DROP (`legacy-rpc-drop-followup.md`)
- 게스트 Atlas 정리 버튼 노출 (기존 todo, 순수 UX)

### [DETAIL] 이번 승인에서 안 읽어도 되는 것

- revoke DO 블록 SQL 전문 · 함수 시그니처 · pg_trgm 함수 목록
- 텍스처 함수 내부 DELETE/INSERT 순서

### [STOP] 이러면 머지 보류

- QA 원격에서 감사 쿼리 돌렸을 때 쓰기 RPC 가 anon 목록에 남아 있으면 (마이그 미적용 신호)
- 머지 후 등록 플로우에서 대표 사진이 안 채워지는 사례가 나오면 (URL 검증이 실데이터와 어긋난다는 신호)
