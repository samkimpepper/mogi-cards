# 모기 리드 카드 — D-108 resolved_finishes: 구현 + 리뷰 부대가 잡은 "테스트 전부 초록인데 실환경에서 죽는 코드"

> 종류: PLAN→EXEC→REVIEW 통합 Read Card. 원문 SSOT = `docs/wiki/decisions/D-108.md` (2026-07-16 개정) + `.planning/quick/260716-c3a-*/` (PLAN·SUMMARY·REVIEW) + `TRAVELOG-16`.
> 작성: 2026-07-16 Claude 세션. 직전 카드 = `1-card-2026-07-14-phase47-gstack-review-fixes.md`.
> 브랜치 `feat/d108-resolved-finishes` (push 완료, PR 은 아직). **이 카드의 이해 체크 D1~D3 = PR 승인 게이트 퀴즈 대체.** 모기가 "코드리뷰 어떻게 했는지 자세하게" 요청해서 2절이 평소보다 길다.

## 1. 무슨 일이 있었나 `[READ]`

- `[READ]` **착수 전 질문 하나가 ADR 모순을 잡았다.** 모기가 "2번(resolved_finish 구현) 할 건데 열린 거 더 있나?" 물어서 D-108 원문을 다시 대조했더니 — D-108 은 "finish 는 최대 1개라 최종 마감은 단일값"이라고 적혀 있는데, 실제 DB 는 line 에 finish 를 **2개까지** 허용하고 (마죠마죠 싱섀 = 글리터+쉬머 실등록), 다중 finish 칩 화면도 이미 배열 대응이었다. 모기 결정으로 D-108 을 **"override 우선 집합"** 으로 개정: shade 에 finish 있으면 그 하나만(shade 는 최대 1개), 없으면 line 전체(최대 2개) 상속, 둘 다 없으면 빈 집합.
- `[READ]` **"line 은 2개인데 shade 는 왜 1개?"** — line finish 는 "이 **제품**에 어떤 마감이 들어 있나"(성격 표시)고, shade finish 는 "이 **색**의 최종 마감이 결국 뭐냐"(단일 답). 층이 다르다. 모기가 나중에 또 물을 것 같다고 해서 D-108 본문에 이 비모순 근거를 절로 넣어뒀다.
- `[READ]` **계획 단계에서 두 번째 함정을 잡았다.** 원래 todo 는 "수정 화면 입력칸에 최종 마감(resolved)을 채운다"였는데, planner 가 코드를 실측해 보니 수정 화면의 저장은 finish 를 **line 에** 쓴다. 즉 resolved(shade 예외 반영값)를 입력칸에 채우면 — VDL 빈티지 로즈를 열었다 그냥 저장만 해도 shade 예외(shimmer)가 line 기본값(matte) 자리를 덮어쓰는 **오염 경로**가 생긴다. 모기 결정: 입력칸은 line 값만 + shade 예외는 **읽기 전용 배지**로 표시 + 예외를 바꾸는 UI 는 후속 todo.
- `[READ]` **구현 완료 후 테스트는 전부 초록이었다.** vitest 167/167, 타입검사·빌드 클린, 로컬 DB 적용 + 시나리오 4종 통과. 여기까지 보면 완벽해 보였다.
- `[READ]` **그런데 코드리뷰가 "실환경에서 카탈로그가 통째로 죽는 코드"를 찾았다.** 새 조회 코드가 `shade_traits` 테이블에 **존재하지 않는 컬럼**(`sort_order`)을 요청하고 있었다. 실제 서버는 이 요청을 통째로 거부하고(에러 42703), 앱은 에러 나면 옛 백업 데이터(Notion 폴백)로 조용히 후퇴하게 돼 있어서 — 배포됐으면 **모든 유저가 몇 달 전 카탈로그를 보게 되는** 급이었다. 수정 시트도 같은 원인으로 항상 빈 값으로 열렸을 것.
- `[READ]` **왜 테스트가 못 잡았나: mock 이라서.** 테스트는 서버에 진짜 요청을 보내지 않고 "서버가 이렇게 답할 거야"라고 **지어낸 응답**을 쓴다. 그 가짜 응답에 존재하지 않는 컬럼을 넣어놨으니, 테스트는 실환경에서 불가능한 세상을 검증하며 초록불을 켠 것. 수정 후에는 로컬 서버에 **진짜 요청**을 쏴서 옛 쿼리 = 400 에러 재현, 새 쿼리 = 200 정상까지 증명했다.
- `[READ]` **리뷰 findings 9건 전부 반영 + 검증 강화.** 시나리오 6종(신규 2: 에디터 역순 등록 정렬, shade form 거부) 통과. 원격 DB 는 안 건드림.

## 2. 코드리뷰를 어떻게 돌렸나 (모기 요청 — 자세히)

이번 리뷰는 사람이 diff 를 한 번 읽는 방식이 아니라 **에이전트 16개짜리 2단 구조**다:

**1단 — finder 4명 (찾는 사람).** 서로 모르는 상태로 병렬 투입. 정확성 각도 3명(각자 다른 관점에서 "이 코드가 어떻게 틀릴 수 있나"만 탐색) + 정리 각도 1명(중복·비효율·이름 혼동 담당). 이들이 후보 15건을 냈다. 중요한 건 **후보는 아직 "주장"일 뿐**이라는 것 — finder 는 그럴듯한 오탐도 낸다.

**2단 — verifier 10명 (반박하는 사람).** 후보를 (파일, 줄) 단위로 묶어서, 각 지점마다 **독립** verifier 를 붙인다. verifier 의 임무는 확인이 아니라 **반박** — "이 지적이 틀렸다는 증거를 찾아라". 반박에 실패해야 CONFIRMED 로 살아남는다. 실제로 1건이 여기서 탈락했다 (DB 검증 함수의 성능 지적 — 반박 검증에서 실측 근거 부족으로 REFUTED).

**결과: 15건 → 확정 9건** (정확성 5 + 정리 4). 최악의 버그(없는 컬럼)는 **finder 두 명이 각자 독립적으로 같은 지점에 도착**했다 — 서로 모르는 둘이 같은 곳을 짚으면 신뢰도가 급등한다 (지난번 Phase 47 리뷰에서 악역 둘이 같은 버그에 수렴한 것과 같은 패턴).

**3단 — 나도 못 믿어서 한 번 더.** "카탈로그 전체 후퇴"급 주장은 잘못 전달하면 비용이 커서, 모기한테 보고하기 전에 내가 직접 테이블 생성 SQL 원문을 열어 컬럼 3개(`shade_id, trait_id, is_primary`)뿐인 것을 재확인했다. 그 다음에야 객관식으로 수정 범위를 물었고, 모기가 "전부"를 골라서 **같은 executor 에게 이어서** 고치게 했다 (자기가 만든 코드라 맥락이 있음).

**이 구조의 요점**: 찾는 사람과 검증하는 사람을 분리하고, 검증을 "맞다고 해줘"가 아니라 "틀렸다고 증명해봐"로 시키는 것. 그래야 그럴듯한 오탐이 걸러지고, 살아남은 것은 믿을 수 있다.

## 3. 코드로 보기 (공부용)

**죽은 쿼리 vs 산 쿼리.** 서버 조회는 "관계 테이블을 같이 달라"고 문자열로 요청하는데(embed), 거기 적은 컬럼이 실제로 없으면 요청 전체가 거부된다:

```ts
// 죽은 버전 — shade_traits 에 sort_order 라는 컬럼 자체가 없음
.select('*, shade_traits(sort_order, traits(type, slug, ...))')
// → 서버: 42703 "column shade_traits_1.sort_order does not exist" → 400
// → 앱: 에러니까 옛 백업 카탈로그로 조용히 후퇴 (유저는 원인을 모름)

// 산 버전 — 있는 것만 요청 + finish 만 걸러서 받음
.select('*, shade_traits(traits!inner(type, slug, label_kr, label_en))')
.eq('shade_traits.traits.type', 'finish')
```

**resolved_finishes 의 개념** (DB 함수 + 앱 helper 가 같은 규칙):

```text
shade 에 finish 있음  → 그 하나만        (VDL 빈티지 로즈: line=matte 여도 답은 shimmer)
shade 에 finish 없음  → line 전체 상속    (마죠마죠 싱섀: glitter+shimmer 둘 다)
둘 다 없음           → 빈 집합
```

**정렬도 통일했다.** 리뷰 finding 중 하나 — DB 함수는 "전역 어휘 순서"로, 앱은 "에디터가 등록한 순서"로 정렬하고 있어서 대표 finish(첫 값)가 서로 다를 수 있었다. DB 를 에디터 등록 순으로 바꿔 화면 칩 순서와 항상 일치.

## 4. 새로 나온 말

| 말 | 쉬운 뜻 | 이번 예시 |
|---|---|---|
| resolved_finishes | shade 예외가 있으면 그것, 없으면 line 값을 물려받아 "이 색의 최종 마감 후보 집합"을 계산하는 함수 | VDL=shimmer 하나, 마죠마죠=2개 |
| mock (테스트) | 진짜 서버 대신 "이렇게 답할 거야"라고 지어낸 가짜 응답으로 테스트하는 것 | 가짜 응답에 없는 컬럼을 넣어놔서 버그를 못 잡음 |
| embed | 서버 조회 때 "관계된 테이블도 같이 달라"고 요청 문자열에 적는 것 | `shade_traits(traits(...))` |
| finder / verifier | 리뷰 1단(버그 후보를 찾는 에이전트) / 2단(그 후보를 반박해보는 독립 에이전트) | 후보 15 → 반박 1 탈락 → 확정 9 |
| 42703 | "그런 컬럼 없는데?" 라는 DB 에러 번호 | sort_order 요청이 이걸로 죽음 |

## 5. 모기가 해야 할 일 `[DECIDE]`

- [ ] **(지금)** 아래 이해 체크 D1~D3 답 → PR 발주 ("PR 올려줘" 하면 dev 대상 생성. 이 카드가 PR 게이트 퀴즈 대체라 따로 퀴즈 안 냄)
- [ ] **(머지 후)** `supabase db push` — 이번 마이그 1개 (resolver + 검증 강화). 실패 시 자동 수정 말고 나한테.
- [ ] **(push 후 확인 1분)** 앱 열어서 카탈로그가 정상 로딩되는지만 확인 — 이번 버그의 증상이 "조용한 후퇴"라서, 화면이 뜨는 것만으로는 부족하고 **최근 등록한 제품이 보이는지**를 봐야 함.

## 6. 판정

- `[STOP]` 없음 — shade finish 예외를 쓰는 화면·RPC 가 아직 없어서 위험 경로 자체가 닫혀 있음. DB 검증(최대 1개·form 거부)이 먼저 깔린 상태.
- `[FOLLOW-UP]` 모기가 낸 "예외라고 강조하는 편집 UI" 아이디어 = `.planning/todos/pending/shade-finish-exception-edit-ui.md` 로 보존 (어드민 mixed-finish 일괄 지정 UI 와 같은 설계 사이클 후보).
- `[FOLLOW-UP]` REVIEW.md 교훈 명문화 — "조회 문자열을 바꾸는 변경은 mock 테스트 말고 로컬 서버 실쿼리 1회 필수". 하네스 룰 승격은 모기 판단.

`[DETAIL]` 안 읽어도 되는 것: 마이그 내부 구조(진단 블록 순서·BEGIN/COMMIT), helper 이름 정리, 시나리오 SQL 원문.

원문 위치:

- `docs/wiki/decisions/D-108.md` → 집합 개정 + 비모순 절 (모기 질문의 답이 그대로 있음)
- `.planning/quick/260716-c3a-d-108-resolved-finishes-resolver-shade-f/260716-c3a-REVIEW.md` → findings 9건 표 + 증거
- `docs/blog/agent-journey/milestone/TRAVELOG-16-d108-quick-review-mock-trust.md` → 이 흐름이 하네스적으로 어떻게 돌았나

---

<details>
<summary>이해 체크 (주관식 — PR 게이트)</summary>

**D1. 핵심 버그 — "테스트는 전부 통과했는데 실환경에서 죽는다"가 이번에 왜 가능했어?** 무엇이 초록불을 켜줬고, 수정 후에는 뭘 추가로 해서 재발을 막았나?
(→ 어디 보면: 이 카드 1절 다섯·여섯 번째 READ + 3절 "죽은 쿼리 vs 산 쿼리")

**D2. 위험한 오해 — 수정 화면 입력칸에 "최종 마감(resolved)"을 채우면 안 됐던 이유.** VDL 빈티지 로즈로 설명하면, 열었다 저장만 해도 무슨 일이 생겼을까?
(→ 어디 보면: 이 카드 1절 세 번째 READ)

**D3. 리뷰 구조 — verifier 는 finder 가 낸 후보에게 뭘 하라고 지시받아?** 그리고 그 방식 때문에 실제로 몇 건이 떨어져 나갔지?
(→ 어디 보면: 이 카드 2절 "2단 — verifier" 문단)

</details>

## 코드로 보기 모기 질문

- 죽은 버전, 산 버전 코드 보는데. 근데 저렇게 컬럼 이름을 하드코딩하는것이 조금… 근데 스프링 jpa에서 그 매퍼? 네이티브쿼리할때도 그렇게 하긴 하지만. 이정도는 괜찮나? 그냥 컬럼 검증 절차만 추가하면 잡히는 문제인가?
- resolved_finishes의 개념 갑자기 문득 생각났는데. 내가 dogfood한 사례에서 페리페라, 티르티르 이런 애들은 라인 안에서도 shade별로 finish가 다 다른데 이거 구현할때 모순되지않아? 미친.. 아니면 라인 traints에 매트, 쉬머, 글리터 다 들어가는거야..?
- 대표 finish 정렬 질문도.. 지금 그냥 등록한 순서대로 해서 첫번째로 선택한게 걍 primary가 되는 그런 느낌인거임? 뭔가 is_primary 이런컬럼도 없어?

## 이해 체크 답

- D1. 그게 mock테스트여서..ㅠ 코드리뷰해서 실제로 서버요청 보내서 200받았다고하네. 수정후에는 뭘..? 그그ㄹ러게;
- D2. 지금은 product_lines에만 인서트,업데이트가 되고 있어서 최종마감이 shades가 아니라 lines에 들어가게되;;
- D3. 근데 DB 검증함수의 성능지적 .. 지적한걸 스스로 반박해보게 시켜서 1개 떨어져나갔대.
---

## 해설 — 채점 + 모기 질문 답 (2026-07-16 Claude append. 게이트 판정: 통과)

### 채점

- **D1 절반 정답.** "mock 이라서"까지 맞음. 비어 있던 뒷부분 — 수정 후 추가한 것 = (1) 로컬 서버에 진짜 요청(curl)을 쏴서 옛 쿼리 400 → 새 쿼리 200 증명, (2) 재발 방지 todo 2개: typed client 완료 조건에 "없는 컬럼을 일부러 넣고 tsc 가 잡는지 실증" + select 문자열을 로컬 DB 에 실쿼리로 돌리는 스모크 통합 테스트 (`select-embed-smoke-integration-test`). 모기가 채팅에서 "통합테스트 추가해야 하나?" 물었던 게 정확히 이 답이었음.
- **D2 정답.** 입력칸에 resolved(shimmer)가 채워지면 저장이 line 쪽으로 가서 빈티지 로즈의 예외가 라인 기본값 matte 를 shimmer 로 덮고, 그 라인의 다른 매트 색들까지 전부 shimmer 상속으로 연쇄 오염.
- **D3 정답 (한 끗 정정).** "스스로 반박"이 아니라 finder 와 **별개의 독립 에이전트**가 반박을 시도함 — 찾는 사람/검증하는 사람 분리가 핵심. 성능 지적 1건 탈락은 정확.

### 코드로 보기 질문 답

**Q1. 컬럼 이름 하드코딩 괜찮나? 검증 절차만 추가하면 잡히나?**
직감 맞음 (JPA 네이티브쿼리 비유 정확). supabase-js 는 결국 문자열 쿼리라 하드코딩 자체는 못 피함. "검증 절차 추가하면 잡히는 문제" = 맞고, 그 절차가 오늘 todo 로 들어감 — typed client 를 물리면 select 문자열을 컴파일에서 검사 (JPA 로 치면 메타모델/QueryDSL 급), 스모크 테스트가 실DB 대조 잔여 방어.

**Q2. 페리페라·티르티르는 shade 별로 finish 다 다른데 모순 아냐? line 에 매트·쉬머·글리터 다 넣어?**
모순 아니고 그 케이스가 이 구조의 존재 이유. 그런 제품은 **line finish 를 비워두고** shade 별로 지정 (dogfood 사례 3·4 판정: M 그룹→matte, S→shimmer, G→glitter). resolved_finishes 는 shade 에 값이 있으면 line 을 아예 안 봄 → 충돌 없음. line 에 3개 다 넣는 게 아님 (DB 가 line 은 최대 2개라 들어가지도 않음). 단 shade finish 를 입력하는 어드민 UI 가 아직 없어서 이 등록은 현재 불가 — dogfood 여섯 사례 공통 병목이고 후속 todo 2개(`shade-finish-exception-edit-ui` + mixed-finish 일괄 지정)가 그 자리. 마죠마죠처럼 line 에 2개 넣는 건 그 UI 없는 동안의 과도기 표현.

**Q3. 대표 finish = 등록 순서? is_primary 컬럼 없어?**
지금 대표(카탈로그 카드 칩 1개) = `line_traits.sort_order` 첫 값 = 에디터가 처음 고른 것. 그리고 `shade_traits.is_primary` 컬럼은 **실제로 있음** (`20260619000000_trait_schema_mirror.sql:62`) — 다만 finish 대표 선정에는 현재 안 쓰이고, 어디서 소비되는지는 이번 세션에서 추적 안 함 (미확인 사실로 남김). "대표를 명시적으로 지정" 니즈가 생기면 그때 이 컬럼을 살릴지 결정하면 됨.
