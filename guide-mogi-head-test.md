# 0번 모기 머가리 테스트 — 외부화가 추론 성능에 미치는 영향

다음 과외 세션에서 새 리드 카드를 재료로 아래 가설을 가볍게 검증한다.

> 모기의 전반적인 추론능력보다 여러 조건의 동시 유지와 참조 대상 결합이 상대적으로 큰 병목이며, 정보를 표·코드·구체 값으로 외부화하면 추론 성능이 상당히 회복된다.

이것은 IQ 검사나 의학적 진단이 아니다. 코드베이스 비숙련, 문서의 모호함, 설명 품질, 피로를 작업기억 문제로 오인하지 않는다.

## 재료 선택

- 이미 익숙해진 `owner_uid` 귀속 문제 대신 처음 보는 새 카드의 작고 독립적인 기능을 고른다.
- 테이블 2개, 행위자 2명, 이벤트 2개, 규칙 5~7개 정도가 적당하다.
- 실제 문서에 빠진 주어·값의 출처·모순이 있으면 먼저 **입력 결함**으로 기록한다. 모기의 오류로 채점하지 않는다.

## 기본 실행: 기계식 러너

새 정식 세션은 프롬프트만으로 아래 순서를 진행하지 않고 [`head-test/runner.html`](./head-test/runner.html)을 기본 실행 표면으로 쓴다. 사용법과 케이스 작성법은 [`head-test/README.md`](./head-test/README.md)가 정본이다.

러너 `head-test-1.0`은 아래를 코드로 잠근다.

- 정보 출처·값의 주인·이벤트·전후 값을 가진 canonical typed fact 5개에서 문단 조건과 표 조건을 함께 렌더한다.
- 네 종류 질문을 모두 제출하기 전 다음 조건과 피드백을 열지 않는다.
- 데모를 제외한 서로 다른 새 측정 카드 전체에서 문단/표 선행 순서를 전역 교대하고, 실제 시작한 배정과 제외 결과를 원장에 남긴다.
- 초기 A/B 각 블록에 동일한 4~5분을 주어 총 활성 측정을 8~10분으로 제한한다.
- 지연 해제 전 접근, 요약 없는 지연 답변 전 표 노출, 완료 전 채점을 막는다.
- 피로 중단과 입력 결함 신고가 들어오면 즉시 실행을 잠그고 이후 답변을 판정에서 제외한다.
- 원답 수정 이벤트·제출 시각·단계 전이·중단 사유를 실행 JSON으로 보존한다.
- 참가자 케이스와 reviewer-only 정답 키를 분리하고 조건·문항별 구조 입력만 계산한다.

케이스는 세션 전에 마스터가 만들고 validator를 통과시킨다. 모순 탐지의 의미, 입력 결함의 정당성, 실제 난도 동등성, 피로 원인은 자동 판정하지 않는다. 현재 지연 구간은 기억 간격과 직전 회상 효과가 섞이므로 표 지원의 인과효과가 아니라 서술 관찰로만 기록한다. 유효한 서로 다른 새 카드 실행이 최소 두 번 쌓이기 전에는 잠정 결론을 갱신하지 않는다.

아래 20분 진행 절은 러너가 없을 때의 수동 원형이다. 새 정식 실행에서는 러너 프로토콜과 섞지 않는다.

## 20분 진행

### A. 문단 조건

1. 과외냥이가 규칙을 긴 문단으로만 설명한다. 표나 상태 전이 요약은 아직 주지 않는다.
2. 모기가 아래 네 종류에 각각 답한다.
   - 사실 회상: 어느 테이블에 무슨 컬럼이 있는가?
   - 상태 추적: 특정 이벤트 뒤 어느 값이 어떻게 바뀌는가?
   - 새 사례 추론: 처음 제시하지 않은 상황의 결과는 무엇인가?
   - 모순 탐지: 설명·코드·제품 규칙이 충돌하는 지점은 무엇인가?

### B. 외부화 조건

3. 같은 종류의 정보를 아래 형식으로 계속 보이게 둔다.

| 테이블 | 컬럼 | 값의 주인 | 변경 이벤트 | 변경 전 | 변경 후 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

4. 이름과 값만 바꾼 동급 사례로 같은 네 종류의 질문에 다시 답한다.
5. 다른 주제를 5~10분 본 뒤 원래 규칙을 다시 적용한다. 한 번은 요약 없이, 한 번은 표를 보며 답한다.

문단 문제와 표 문제의 난도가 완전히 같지 않을 수 있으므로 한 세션 결과만으로 확정하지 않는다. 가능하면 다음 세션에서는 제시 순서를 뒤집어 순서·연습 효과를 확인한다.

## 기록할 것

| 조건 | 사실 회상 | 상태 추적 | 새 사례 추론 | 모순 탐지 | 조건 누락 | 역할·시점 교환 | 소요/재질문 |
|---|---:|---:|---:|---:|---:|---:|---|
| 문단 |  |  |  |  |  |  |  |
| 외부화 |  |  |  |  |  |  |  |
| 지연 후·요약 없음 |  |  |  |  |  |  |  |
| 지연 후·표 있음 |  |  |  |  |  |  |  |

- 질문 횟수 자체를 낮은 이해력으로 채점하지 않는다.
- 무엇을 물었는지 구분한다: 빠진 참조를 정확히 찾은 질문 / 이미 명시된 조건을 잃은 질문 / 새로운 모순을 발견한 질문.
- 사실을 기억하지 못한 것과, 사실을 눈앞에 두고도 적용하지 못한 것을 따로 기록한다.
- 정답 직후의 재진술과 시간이 지난 뒤의 독립 적용도 구분한다.

## 예측과 잠정 판정

### 작업기억·컨텍스트 유지 병목 가설을 지지

1. 문단 조건에서 조건 누락과 역할·시점 교환이 늘어난다.
2. 표를 보이면 새 사례 추론과 모순 탐지가 크게 회복된다.
3. 시간이 지난 뒤 표 없이는 성능이 낮아져도, 표를 다시 주면 빠르게 정상화된다.

### 가설에 반대

1. 모든 주체·값·규칙을 표로 보여줘도 같은 혼동이 지속된다.
2. 조건 한두 개의 짧은 문제에서도 인과관계 적용에 실패하거나 정정 직후 동일한 오류를 반복한다.
3. 외부화해도 성능 변화가 작고, 변수 유지가 아니라 규칙 적용·모순 판단 자체에서 계속 막힌다.

## 세션 종료 기록

- 외부화 효과: 큼 / 일부 / 없음 / 판정 불가
- 가장 자주 난 오류:
- 모기가 스스로 발견한 입력 결함·모순:
- 설명 직후 수정 여부:
- 지연 후 유지 여부:
- 코드베이스 비숙련이나 문서 품질로 설명되는 부분:
- 다음 세션에서 다시 확인할 예측:

최소 두 번의 서로 다른 새 카드에서 비슷한 패턴이 반복될 때만 잠정 결론을 갱신한다.
