# 모기 이해 관찰 가이드 — 자유 학습 우선

목적은 모기의 지능을 시험하는 것이 아니라, **어떤 설명과 외부화가 실제 이해를 돕는지 가볍게 관찰하는 것**이다.

2026-08-13 PR #504 첫 정식 실행에서 상세 절차는 모두 작동했지만, 한 줄짜리 변경을 어려운 PostgreSQL 시험으로 바꿔 목적을 잃었다. 따라서 기계식 A/B 러너를 기본 흐름에서 내리고 아래 세 가드레일만 남긴다.

## 약한 가드레일 세 개

이 셋은 정해진 순서를 수행시키는 프로토콜이 아니다. 과외냥이가 잊지 않고 확인할 최소 경계다. 상황상 하지 못한 항목은 실패가 아니라 `미관찰`로 남긴다.

### 1. 실제 diff와 설명의 핵심을 맞춘다

카드의 주변 맥락을 전부 시험 재료로 만들지 않는다. 먼저 실제 diff와 변경 핵심 한 문장을 본다.

생소한 용어가 너무 많거나 실제 변경보다 설명이 더 어렵다면 측정을 만들지 않고 일반 과외로 진행한다.

### 2. 자유 문답 뒤 이해가 이어지는지 본다

질문 횟수, 용어 질문, 재진술 요청을 실패로 채점하지 않는다. 빠진 주어·출처·선택지·전제는 입력 문제로 다룬다.

과외냥이는 필요한 용어를 먼저 정의하고, 표·코드·예시 등 도움이 되는 표현을 자유롭게 사용한다. 정해진 설명 순서나 시간 제한은 없다.

### 3. 원하면 새 사례 하나에 적용해본다

원래 사례를 외워 다시 말하게 하지 않는다. 모기가 원하고 피로하지 않을 때만 이름이나 값이 다른 작은 사례 하나를 제시한다.

이 단계의 목적은 점수가 아니라 다음을 구분하는 것이다.

- 설명을 이해했는가
- 새 사례에도 같은 규칙을 적용할 수 있는가
- 표나 구체 값이 있을 때 무엇이 쉬워지는가
- 여전히 빠진 전제나 모순이 있는가

## 짧은 기록 — 매 세션 기본

한 번의 세션으로 지능·인지능력·의학적 상태를 판단하지 않는다. 세션 기록은 아래 네 항목을 기본으로 한다.

- 처음 막힌 지점:
- 설명 추가 없이 바로 연결한 지점:
- 설명·외부화 뒤 달라진 점:
- 아직 미관찰이거나 다음에 확인할 점:

해당 사례가 없으면 `없음`, 시행하지 않았으면 `미관찰`이라고 적는다. 막힌 사례만 모으지 않고, 모기가 설명 추가 없이 상태를 연결하거나 설명의 잘못된 전제를 바로잡은 사례도 같이 남긴다.

질문한 내용과 실제 답변보다 넓은 결론을 붙이지 않는다. **관찰되지 않은 이해를 추론해서 채우지 않는다.** 예를 들어 새 사례 적용을 하지 않았다면 `새 사례 전이 성공 여부: 미관찰`이라고 기록한다. `대화상 충분히 이해한 것으로 보임`처럼 관찰하지 않은 결과를 에이전트가 대신 완성하지 않는다.

## 세션 종료 기록 기본값 (모기 개정 2026-08-20)

모기가 과외 세션 종료를 알리면 `head-test/<날짜>-<주제>-session-end-analysis.md`에 종료 관찰을 남긴다. 과거 관찰일지는 당시 판독의 이력이므로 새 형식에 맞춰 다시 쓰지 않는다.

매 세션 종료 기록에는 다음만 넣는다.

1. 헤더에 도메인 친숙도, 세션 길이(시간 미측정이면 그대로 표시), 모기가 명시한 컨디션을 기록한다.
2. 위 `짧은 기록` 네 항목을 작성한다. 짧은 세션은 사례가 없으면 `관찰 사례 0개`라고 명시한다.
3. 필요한 원문은 최소한으로 인용한다. 원문 수집 절이 필요하면 제목을 `학습 마찰·재확인·새 발견`으로 두고, 이 절에는 원인 해석을 섞지 않는다.

질문이나 재확인이 실제 설계 결함·입력 결함·새 지식 공백을 드러냈다면 이를 조건 손실과 구분한다. 피로가 명시된 뒤의 중단과 생략은 독립 추론 실패로 채점하지 않는다.

## 선택적 심층 분석과 누적 검토

아래 상황에서만 심층 분석을 한다.

- 모기가 명시적으로 원했을 때
- 같은 패턴이 반복돼 설명 방식을 바꿀지 결정해야 할 때
- 종료 기록이 4~5개 쌓여 누적 방향을 검토할 때

종료 기록 4~5개를 묶은 누적 검토는 `head-test/<날짜>-learning-observation-cumulative-review.md`에 저장한다. 새 문서는 `reviewed: false`로 시작하고, 다음 구간에서 과거 문서를 덮어쓰지 않고 새 날짜의 누적 검토를 만든다.

누적 검토 문서에는 다음을 순서대로 넣는다.

1. 분석 대상 세션 로그 4~5개의 목록
2. 이번 구간의 결론
3. 아래 세 축별 변화와 근거
4. 기존 미관찰 항목의 `종료/자연 관찰/확인 필요` 분류
5. 다음 세션에서 실제로 바꿀 것 1~2개

심층 분석에서 `주어/시점/참조/전제` 암묵 슬롯 코딩은 선택 사항이다. 슬롯 수를 점수나 객관적 인지 부하 단위로 쓰지 않고, 막힌 사례만 골라 인과를 추정하지 않는다. 쓴다면 설명 추가 없이 연결한 사례와 함께 본다.

누적 검토는 다음 세 축을 따로 판독한다.

1. **설명 입력의 품질:** 실제 diff와 핵심이 맞았는지, 주어·시점·값의 주인·이벤트 전후가 명시됐는지 본다.
2. **모기의 자기조절:** 필요한 지도·표·워크북을 스스로 선택했는지, 피로할 때 범위를 줄이거나 멈췄는지 본다.
3. **새 사례 적용:** 모기가 원하고 피로하지 않을 때만 이름이나 값이 다른 작은 사례 하나를 본다. 하지 않았으면 `미관찰`로 남긴다.

이 세 축의 변화를 지능·일반적 기억력·인지능력의 변화로 바로 바꿔 말하지 않는다. 이전 관찰의 미관찰 항목은 실제로 다시 본 세션에서만 갱신하고, 매 종료 문서에 기계적으로 반복하지 않는다. 이 분석은 학습 로그이지 Review Gate Quiz나 의학적 평가가 아니다.

## 기본 진행

평소 카드 과외가 곧 기본 진행이다.

```text
실제 diff와 설명 핵심 일치 확인
→ 모기가 원하는 만큼 자유 문답
→ 필요하면 다른 작은 사례 하나 적용
→ 관찰 네 항목 기록
```

하드 타이머, 규칙 개수, 질문 종류, A/B 순서, 지연 검사는 기본 요구가 아니다. 세션이 피곤하거나 재미없어지면 그냥 멈춘다.

## 기계식 러너의 위치

[`head-test/runner.html`](./head-test/runner.html)은 폐기하지 않지만 **기본 과외 도구가 아니다**. 모기가 통제된 A/B 실험을 명시적으로 다시 원할 때만 별도 실험으로 사용한다.

러너의 구조와 한계는 [`head-test/README.md`](./head-test/README.md), 첫 정식 실행 실패는 [`head-test/2026-08-13-pr504-first-formal-run-failure.md`](./head-test/2026-08-13-pr504-first-formal-run-failure.md)에 남겨둔다.
