# 모기 리드 리뷰 — PR #330 팔레트 증거 (Phase 43)

## 1. 승인용 요약

- `[READ]` 이 PR 은 "같은 라인 2칸 이상이 한 장에 모인 사진"을 비교발색이 아니라 **팔레트 증거**로 저장·표시한다. 마이그 2개 + 등록 판정 + 상세시트/LinePage UI + 디올 seed 정정 + 어드민 4구 일괄 등록 (D-100 구현).
- `[READ]` **숨은 변경** (계획에 없던 코드리뷰 수리 2건 — 저장 동작이 바뀜):
  - WR-01: 어드민 일괄 등록에서 빈 행·중복 위치 slug 저장이 이제 **에러로 거부**된다. 전: 이름 빈 shade 가 조용히 DB 에 생김 → 후: 저장 전 차단.
  - WR-03: 서버 팔레트 가드가 더 엄격해졌다. 전: id/slug 가 어긋난 데이터나 해석 안 되는 매핑이 섞인 사진도 팔레트로 통과 가능 → 후: 거부.
- `[READ]` 43-01 deviation: 이 RPC 실행권을 PUBLIC 까지 회수 (보안 강화 방향). 나머지 함수 전체는 PR #331.

## 2. 직접 봐야 할 불변식

- 어드민 manual 지정은 어떤 자동 규칙도 못 덮는다.
- 팔레트 증거는 auto 값을 덮지만, 팔레트끼리는 first-come 고정.
- 팔레트가 아닌 사진으로 팔레트 클레임하면 서버가 거부한다 (`not_palette_evidence_swatch`).

## 3. 증거 `[EVIDENCE]`

- vitest 61 · tsc · build 클린 / RPC 우선순위+위조 거부 7케이스 로컬 DB 실측 / 디올 seed 로컬 실측 (라인 2 + 칸 8 + 고아 참조 0). 원문: `43-VERIFICATION.md` · `43-REVIEW.md`.

## 4. 모기가 해야 할 일 `[DECIDE]`

- [ ] **이 PR 먼저** 머지 → `supabase db push` → UAT 4건 (PR 본문 체크리스트 — 디올 칸은 첫 등록 전 배지 안 뜨는 게 정상)
- [ ] 그 다음 #331 머지 (순서 바뀌면 supabase CLI 가 push 거부)

## 5. 판정

- `[STOP]` 없음. `[FOLLOW-UP]` 43-REVIEW Info 5건 (비차단) · 판정 빈틈 thread `palette-judgment-signal` (호수 비교샷 오포획 — QA 빈도 보고 결정) · PR #331.

`[DETAIL]` 이번 승인에서 안 읽어도 되는 것: SQL 함수 본문, JSX 위치, i18n 키 이름, TDD Audit 표.

---

<details>
<summary>Appendix B. 이해 체크 (주관식 — 답은 세션에서)</summary>

**B1. 막는 것/못 막는 것** — 숨은 변경 WR-03 때문에, 팔레트 증거로 인정되는 사진의 범위는 원래 계획보다 넓어졌을까 좁아졌을까? 그 방향이 위험할까 안전할까?
(힌트: §1 숨은 변경 → 어디 보면: `43-REVIEW.md` WR-03)

**B2. 가짜 리뷰 댓글 반박** — 다음 댓글에 반박해봐: *"팔레트 칸에 나중에 단독 발색이 등록되면 카드 대표가 단독샷으로 자동 교체될 테니, '모임 사진 고정' 결정이 안 지켜지는 것 아닌가요?"*
(→ 어디 보면: PR 본문 Key Decisions 결정 A / `D-100.md` supplement)

채점은 답하면 세션에서: 정답 / 부분 이해 / 다시 보기.

</details>

## Appendix A. 승인 중 생긴 질문 (2026-07-08, 모기 → Claude 답)

### Q1. 어드민 manual 이거 대표사진 말하는거지? 근데 어케 구현했는지 궁금. 걍 if문 분기?

- 답: 맞아, 대표사진 얘기. `shade_images.representative_source` 컬럼 값이 `'manual'` 이면 어드민이 손으로 지정한 대표라는 뜻. 구현도 네 감 그대로 — RPC 안에서 plpgsql `IF` 분기야 (마이그 `20260707100000` 136~162행): 기존 대표의 source 값을 보고 "교체(upgraded) / 유지(kept)" 를 IF 로 가른다. 추가로 manual 은 이 RPC 의 *입력값으로는 아예 거부*돼 (42행) — manual 지정은 어드민 direct 경로 전용이고, 기존 대표가 manual 이면 무조건 kept.
- blocker 여부: 아님 (이해 질문).

### Q2. "팔레트 증거는 auto 를 덮는다" — jargon이야, 다시 설명해줘

- 답 (풀어서): 칸의 카드 대표사진은 자동으로 정해질 수 있어 — 단독샷이 오면 `auto_single`, 여러 라인 섞인 비교샷이면 `auto_comparison_fallback`. 규칙은: **이미 자동으로 정해진 대표가 있어도, 모임 사진(팔레트 증거)이 새로 오면 그걸로 교체된다. 반대로 이미 모임 사진이 대표면, 나중에 오는 자동 후보(단독샷 등)는 교체 못 한다.** 어드민이 손으로 지정한 것(manual)은 그 무엇도 못 건드림. 한 줄로: 손 지정 > 모임 사진 > 자동.
- 카드 반성: 이 줄은 내(Claude) 카드가 jargon 을 jargon 으로 쓴 사례 — 가이드 위반. 다음 카드부터 결과 문장으로 쓴다.
- blocker 여부: 아님.

### Q3. "팔레트 클레임"? 발색지도에서 비교노트 등록하는 거 말하는 거임?

- 답: 아니야, 비교노트·발색지도랑 무관해. **트윗 발색 등록(게시) 플로우** 얘기야. "클레임" = 등록 시 클라이언트가 서버에 "이 사진은 팔레트 증거야" 라고 *주장하며* 저장 요청하는 것. 서버는 그 주장을 그대로 믿지 않고 DB 에서 재검증해 — 진짜 같은 라인 2칸 이상 사진이 아니면 거부(`not_palette_evidence_swatch`). 누가 API 를 직접 두드려 거짓 주장해도 서버 선에서 막힌다는 뜻.
- blocker 여부: 아님.

### Q3-후속. 같은 라인 2칸 이상 사진인지 어떻게 판별함??

- 답: **사진 픽셀을 보는 게 아니라, 등록할 때 남긴 매핑 기록을 본다.** 등록 화면에서 "이 사진은 이 칸들 발색이에요" 하고 칸을 고르잖아 — 그게 DB 에 `swatches` 행 (사진 URL 보관) + `swatch_items` 행들 (사진 ↔ 칸 연결, 칸당 1행) 로 저장돼. 서버 판별 순서:
  1. 그 사진 URL 을 담은 `swatches` 행을 찾고
  2. 딸린 `swatch_items` 를 하나씩 칸(shade)으로 해석 — `shade_id` 우선, 없으면 `shade_slug` (WR-03 수리 부분: 하나라도 해석 안 되면 그 자리에서 탈락)
  3. 해석된 칸이 **서로 다른 것 2개 이상**인지 세고
  4. 그 칸들의 `shades.product_line_id` 가 **전부 대상 칸과 같은 라인**인지 확인
  넷 다 통과해야 팔레트 증거 인정, 아니면 거부.
- 그러니까 판별의 재료 = **등록자가 남긴 칸 매핑** (D-099 "등록 스냅샷만 진실" 과 같은 결). AI 가 사진을 알아보는 게 아니야 (그건 D14 로 금지된 방향).
- blocker 여부: 아님 (이해 질문).

## Appendix B-채점. 이해 체크 답안 채점 (2026-07-08, Claude)

### B1 — "더 좁아진 거고, 지금이 안전한 거 아님?"

- **판정: 정답.** 방향(좁아짐)과 위험 평가(전이 위험, 후가 안전) 둘 다 맞음.
- 보충 한 가지: "화면에 안 뜨잖아" 는 반만 맞아 — 이 가드는 화면 문제가 아니라 **서버가 클라이언트 주장을 재검증하는 방어선**이야. 바뀌기 전엔 정상 화면 플로우로는 안 생기는 위조·드리프트 데이터가 API 직접 호출로 통과할 수 있었고, 그 구멍을 닫은 것. 정상 등록은 전후 동일하게 통과.

### B2 — "내가 결정해놨음, 팔레트 본통 사진 무조건 pin, 팔레트 아닌 애들만 단독 교체"

- **판정: 정답.** 가짜 댓글의 핵심 오류(자동 교체가 팔레트에도 적용된다는 가정)를 본인이 잠근 결정 A 로 정확히 반박함. 팔레트 아닌 칸만 D-099 의 fallback→단독 업그레이드가 유지된다는 경계도 정확.
- 보충 한 가지: "무조건 pin" 의 예외 하나만 기억 — **어드민 manual 지정은 팔레트 증거도 덮을 수 있어.** pin 이 불가침인 건 자동 규칙들에 대해서만.

### 총평

- 정답 2 / 부분 이해 0 / 다시 보기 0. 오늘 세션에서 정정받은 축(잠금 방향·서버 가드)이 그대로 답에 재현됨 — 카드가 학습 장치로 작동.
- 다음 [READ] 후보: Q2/Q3 에서 나온 jargon 축 ("source 값 3+1종이 각각 어떤 등록에서 생기는지") — 다음 팔레트 카드에 쉬운 표로 고정.
- 미답 잔여: **#331 카드 (rpc-public-revoke) B1·B2** — 답하면 이어서 채점.