# 모기 리드 카드 — Phase 47 plan 승인 (옛 trait 복사 구조 제거)

> 종류: PLAN Read Card (실행 전 승인). 원문 SSOT = `.planning/workstreams/milestone/phases/47-remove-legacy-trait-compatibility-mirrors-and-rls-surfaces/`의 47-CONTEXT + 47-RESEARCH + 47-01~03-PLAN.
> 작성: 2026-07-13 Codex 세션 (plan-checker 통과 직후). 이 카드는 repo에 없음 — 모기 개인 gist 전용.
> **2026-07-13 갱신 (Claude 세션)**: gstack pre-landing 리뷰 반영 (`47-REVIEW.md` R1~R17). 추가분 = 3절 불변식 2개 (삭제 버튼 · 머지~push 창구), 4절 할 일 2개 (UAT 환경 · 실패 시 복구), 5절 STOP 1개 + FOLLOW-UP 1개, 주관식 B4. 기존 문장은 그대로 둠.

## 1. 승인용 요약

- `[READ]` 지금 DB에는 제품의 제형·마감·질감을 적는 **새 장부와 옛 장부가 같이 있다.** 새 장부는 `traits` 계열이고, 옛 장부는 `forms`, `finishes`, `textures`, `shade_textures`와 세 종류 추천 규칙 테이블이다. PR #248에서 읽기를 새 장부로 옮겼고 PR #253에서 쓰기도 새 장부로 옮겼지만, 혹시 옛 코드가 남아 있을 때를 대비해 두 장부가 서로 베껴 쓰도록 연결해뒀다.
- `[READ]` 그 임시 연결이 이제 안전망보다 부담이 됐다. 옛 장부가 남아 있으니 **누가 읽고 쓸 수 있는지 정하는 문, 두 장부를 맞춰주는 자동 장치, 서버 함수 권한**도 함께 남는다. 보안 검사를 할 때 새 구조만 보면 끝날 일을 옛 구조까지 계속 확인해야 해서 할 일이 증식한다.
- `[READ]` Phase 47은 새 기능을 만드는 작업이 아니다. **앱이 옛 장부를 보는 마지막 경로를 먼저 새 장부로 옮기고, 두 장부 내용이 완전히 같은지 확인한 다음, 옛 장부와 그 전용 보안 장치를 지운다.** 사용자 화면과 저장 결과는 그대로여야 한다.
- `[READ]` plan은 2단계다. **Wave 1**에서 관리자 화면·제품 등록·shade 불러오기 등 남은 옛 참조를 제거한다. **Wave 2**에서 삭제 직전 내용 차이가 0건인지 검사하고, 완성된 삭제 migration을 한 번만 적용한 뒤, 옛 물건이 실제로 사라졌고 새 장부 권한이 맞는지 다시 검사한다.
- `[READ]` LAUNCH-03 전체 RLS 감사를 끝냈다고 주장하지 않는다. Phase 47은 trait 영역의 중복 보안 표면을 없애고 그 영역의 증거를 제공할 뿐이다. 다른 테이블 권한까지 포함한 전체 감사는 LAUNCH-03에서 따로 끝낸다.

## 2. 새로 나오는 말

| 말 | 쉬운 뜻 | 이번 예시 |
|---|---|---|
| compatibility mirror | 새 장부와 옛 장부가 서로 같은 내용을 복사해두는 임시 연결 | `line_traits`에 마감을 쓰면 `product_lines.finish_id`에도 따라 쓰기 |
| canonical / 정본 | 앞으로 진짜로 믿고 읽고 쓰는 한쪽 장부 | `traits`, `line_traits`, `shade_traits`, `category_trait_rules` |
| active-path audit | 지금 실제 앱이 옛 장부를 부르는 코드가 남았는지 찾는 검사 | 관리자 화면이나 제품 등록 코드에서 `shade_textures` 등이 나오면 중단 |
| catalog check | DB 안에 어떤 테이블·열·자동 장치·함수가 실제로 존재하는지 확인하는 검사 | 삭제 뒤 `forms` 테이블과 mirror trigger가 정말 없는지 확인 |
| intended-role smoke | 손님·로그인 유저·관리자 역할로 짧게 써보며 허용/차단이 맞는지 확인 | 일반 유저는 허용된 trait 저장만 되고, 관리자 전용 변경은 막히는지 확인 |
| fail-closed | 검사 결과가 애매하거나 다르면 삭제를 진행하지 않는 방식 | 새 장부와 옛 장부가 한 줄이라도 다르면 migration 중단 |

## 3. 직접 봐야 할 불변식

- **옛 장부를 먼저 지우지 않는다.** Wave 1에서 앱의 마지막 옛 참조를 새 장부로 바꾼 뒤 Wave 2로 간다. 순서를 거꾸로 하면 관리자 화면이나 제품 등록이 없는 테이블을 불러 깨질 수 있다.
- **삭제 전 내용 차이는 0건이어야 한다.** 새 장부에만 있거나 옛 장부에만 있는 제형·마감·질감·추천 규칙이 하나라도 있으면 삭제하지 않는다. 조용히 합치거나 한쪽을 버리지 않는다.
- **삭제 migration은 완성한 뒤 한 번만 적용한다.** 절반짜리를 먼저 적용하고 같은 파일에 삭제 SQL을 나중에 더하면, DB는 이미 실행한 파일로 기억해서 뒤에 추가한 삭제가 실행되지 않는다.
- **검사용 가짜 데이터만 되돌린다.** 옛 구조를 삭제한 migration 자체를 되돌리면 안 된다. 역할별 권한 시험은 migration 적용 뒤 별도 SQL에서 하고, 시험용 행만 마지막에 없앤다.
- **옛 이름이 코드에서 안 보이는 것만으로 끝이 아니다.** DB 안에서도 옛 테이블·열·mirror 자동 장치·전용 권한이 사라졌는지 확인하고, 새 장부의 필수 검사와 권한은 살아 있는지 확인한다.
- **사용자에게 보이는 분류 의미는 바꾸지 않는다.** finish는 제품 라인을 나누는 기준이 아니고, line 공통값 우선·shade 예외, 드라이한 공식 어휘라는 D-107~D-109 원칙을 그대로 유지한다.
- **(2026-07-13 리뷰 추가, R2) 어드민의 trait "삭제"는 진짜 삭제(DELETE)가 아니라 비활성화(`is_active=false`)만이다.** 이유를 풀면: 새 장부 `traits`를 가리키는 연결 테이블 세 개(`line_traits`, `shade_traits`, `category_trait_rules`)는 전부 "부모가 지워지면 나도 따라 지워짐"(ON DELETE CASCADE)으로 만들어져 있다. 그래서 어드민 화면에서 예를 들어 "매트" 마감 한 줄을 진짜 DELETE 하면, 매트를 쓰던 **모든 제품라인의 연결, 모든 shade의 연결, 매트가 들어간 추천 규칙이 에러도 경고도 없이 한꺼번에 사라진다.** 제품 자체는 남지만 "이 제품은 매트"라는 정보가 카탈로그 전체에서 증발한다. 지금까지 이 사고가 안 났던 건 옛 장부에서 삭제하면 자동 장치가 그걸 비활성화로 바꿔줬기 때문인데, Phase 47이 어드민을 새 장부 직접 조작으로 바꾸면 그 안전장치가 사라진다. 그래서 47-01에 "비활성화만, 공용 삭제 버튼(AdminTable의 DELETE 경로)을 traits에 배선 금지"를 못박았다.
- **(2026-07-13 리뷰 추가, R3 — 모기 확정) dev 머지 후 원격 DB 반영(push) 전까지는 어드민에서 어휘·추천 규칙을 편집하지 않는다.** 이유를 풀면: 새 장부에서 옛 장부로 베껴 쓰는 자동 장치는 라인/shade 연결에만 있고, **어휘(`traits`)와 추천 규칙(`category_trait_rules`)에는 없다.** 그 사이에 어드민에서 어휘나 규칙을 만들면 새 장부에만 적히고, migration의 "두 장부 완전 일치" 검사가 그 차이를 보고 스스로 멈춘다 (멈추면 그나마 다행이고, 라인/shade 쪽 일부 경로는 조용히 값이 빠진다). 또 push 직전에 열려 있던 앱 탭을 전부 새로고침한다 — 오래된 탭이 옛 방식으로 규칙을 저장하면 자동 장치가 새 장부 규칙을 통째로 지우고 옛 장부 기준으로 다시 만들어서, 새 장부에만 있던 정보가 날아간다. main은 아직 트친 공개 전이라 이번엔 릴리즈 순서 강제는 없음 — **공개 뒤에는 main 릴리즈를 먼저 하고 push 해야 한다** (서비스 중인 앱이 옛 테이블을 부르는 채로 DB가 먼저 바뀌면 카탈로그가 죽는다).

## 4. 모기가 해야 할 일 `[DECIDE]`

- [ ] **(지금)** 아래 이해 체크 4문항 답 → 오해 정정 후 Phase 47 plan 승인
- [ ] **(실행 중)** 삭제 전 대조 검사에서 0건이 아닌 결과가 나오면 자동 수정하지 말고 어떤 데이터인지 보고받기
- [ ] **(UAT 때, 2026-07-13 리뷰 추가 R11)** UAT 항목 1~3·5는 **migration이 적용된 로컬 docker를 보면서** 한다 — `app/.env.local`을 로컬 Supabase로 잠깐 바꿨다가 UAT 끝나면 되돌리기 (env는 모기 본인만 만짐). 원격(QA)을 보고 하면 옛 테이블이 아직 살아 있어서 "없는 테이블 오류" 검사가 무조건 통과해버려 UAT가 헛돈다. push 끝난 뒤 원격 기준으로 1·2·5만 한 번 더 반복.
- [ ] **(실행이 중간에 실패하면, 2026-07-13 리뷰 추가 R10)** 로컬 DB를 `supabase db reset`으로 원격과 같은 이력으로 되돌렸는지 보고받기 — 안 되돌리면 그 뒤 로컬에서 검증하는 모든 migration이 반쯤 삭제된 스키마 위에서 검증되는 오염이 생긴다.
- [ ] **(PR 머지 후)** 프로젝트 룰에 따라 원격 DB 반영을 진행하고, catalog·역할별 권한 검사 결과 확인

## 5. 판정

- `[STOP]` 새 장부와 옛 장부의 데이터 차이가 1건이라도 나오면 삭제 migration 적용 금지.
- `[STOP]` 앱·관리자·스크립트의 실제 실행 경로에서 옛 테이블이나 `form_id`·`finish_id` 참조가 남으면 삭제 금지.
- `[STOP]` 삭제 뒤 일반 유저가 관리자 전용 trait를 바꾸거나, 정상적인 trait 저장이 막히면 머지 금지.
- `[STOP]` 옛 테이블만 지우고 mirror 함수·trigger·권한 부여가 남으면 완료 아님.
- `[STOP]` **(2026-07-13 리뷰 추가, R2)** 어드민 trait 삭제가 비활성화가 아니라 진짜 DELETE로 배선돼 있으면 머지 금지.
- `[FOLLOW-UP]` user keyword 저장, AI keyword 추출, 새 분류나 picker 변화는 이번에 하지 않는다.
- `[FOLLOW-UP]` Phase 47 결과는 LAUNCH-03의 일부 증거다. 전체 DB 권한 감사 완료 표시는 별도 Phase 6에서 한다.
- `[FOLLOW-UP]` **(2026-07-13 리뷰 추가, R12 — 모기 결정: 범위 밖)** 로그인만 하면 남의 **미발행** 라인/shade의 trait를 바꿀 수 있는 기존 권한 구멍(서버 함수가 "발행됐냐"만 보고 "네 거냐"는 안 봄)은 이번에 일부러 안 고친다. Phase 47 문서의 "least-privilege 증거" 주장에서도 명시적으로 제외해뒀다. 후속 = `.planning/todos/pending/trait-rpc-ownership-guard.md` — LAUNCH-03 닫기 전, main 트친 공개 전에 처리하는 게 목표.

`[DETAIL]` 이번 승인에서 안 읽어도 되는 것: SQL에서 테이블을 지우는 정확한 순서, trigger·constraint의 실제 이름 전체, 생성 타입 파일의 세부 줄, 테스트 fixture UUID.

원문 위치:

- `ROADMAP.md` Phase 47 → 이번 작업의 목표·성공 조건·Wave 순서
- `47-CONTEXT.md` → D-01~D-08, 무엇을 지우고 무엇을 유지하는지
- `47-01-PLAN.md` → 관리자 어휘·추천 규칙을 새 장부로 옮기는 작업
- `47-02-PLAN.md` → 제품 라인·shade가 옛 장부를 보는 마지막 경로 제거
- `47-03-PLAN.md` → 삭제 전 0건 검사, migration 1회 적용, 삭제 후 DB·권한 검사
- `47-REVIEW.md` → 2026-07-13 pre-landing 리뷰 결과 (반영 17건 R1~R17 + 보류 5건) — 이 카드의 갱신 근거
- `D-107.md`~`D-109.md` → 제거 뒤에도 바뀌면 안 되는 trait 의미

---

<details>
<summary>Appendix B. 이해 체크 (주관식 — 답은 세션에서)</summary>

**B1. 핵심 결정 — 왜 바로 지우면 안 되나** — 옛 테이블을 먼저 DROP하지 않고 Wave 1에서 앱의 옛 참조를 먼저 새 장부로 옮기는 이유는?
(→ 어디 보면: 이 카드 1절 네 번째 READ + 3절 첫 번째 불변식)

**B2. 위험한 오해 — 0건의 뜻** — 삭제 전에 반드시 0건이어야 하는 것은 "옛 테이블의 행 개수"인가, 아니면 무엇인가?
(→ 어디 보면: 이 카드 3절 두 번째 불변식 + 5절 첫 번째 STOP)

**B3. 다음 액션 — migration과 권한 시험의 순서** — 삭제 migration과 역할별 권한 시험은 어떤 순서로 실행하며, 무엇만 되돌려야 하나?
(→ 어디 보면: 이 카드 3절 세 번째·네 번째 불변식 + 4절 모기가 해야 할 일)

**B4. 위험한 오해 — 삭제 버튼 (2026-07-13 리뷰 추가)** — 어드민에서 trait 하나를 "삭제"하면 실제로 일어나야 하는 일은 무엇이고, 만약 진짜 DELETE가 나가면 정확히 무엇이 사라지나? (제품 자체가 지워지는 건지 아닌지 포함해서)
(→ 어디 보면: 이 카드 3절 "어드민의 trait 삭제" 불변식 + `47-REVIEW.md` R2)

</details>

### 승인용 요약 모기 질문

(여기에 적어)

### 불변식

(여기에 적어)

### 주관식

- B1. 널포인터에러?라던가. 없는거 불러와서 에러날 수도 있다옹(화면에서)
- B2. 삭제 전 내용차이!!! 옛 테이블에만 글리터리가 있고 막 그러면 안된다네 데이터 똑같아야되
- B3. 모르겠긔 → (해설 받음 2026-07-13: migration 완성 후 1회 적용 → 권한 시험은 별도 SQL → 시험용 가짜 데이터만 되돌림, migration 은 유지)
- B4. traits는 hard delete하는게 아니라 is_delete만 false로 설정해야된다옹. 실제 딜리트하면 제품라인, 쉐이드들의 traits 정보들이 사라지고 빈값이나 널이되는듯 ㅋ → (정정: 컬럼은 `traits.is_active`=false. 널이 되는 게 아니라 연결 행 자체가 CASCADE 삭제 — 제품/shade 는 남고 trait 연결·추천 규칙만 증발. 나머지 통과)
