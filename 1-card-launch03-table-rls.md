---
type: personal
status: active
owner: samkimpepper
created: 2026-07-03
topic: 모기 리드 리뷰 — LAUNCH-03 테이블 RLS 전수 감사 + 잠금 (PR 승인용 Read Card)
guide: guide-mogi-read-cards.md
---

# 모기 리드 리뷰 — LAUNCH-03 테이블 RLS 잠금 (2026-07-03)

> 원문이 SSOT. 이 카드는 읽는 길잡이 + 승인 체크포인트.
> 원문: `supabase/migrations/20260703010000_launch03_table_rls_lockdown.sql` · `.planning/todos/pending/legacy-tables-drop-cleanup.md` · `.planning/todos/pending/legacy-anon-owner-null-rls-branches.md`

## Auth 용어 미니 정리

| 말 | DB 입장 | 뜻 |
|---|---|---|
| anon | role = anon | 세션 없는 호출자 |
| guest | role = authenticated, is_anonymous=true | 익명 로그인 세션이 있는 사용자 |
| member | role = authenticated, is_anonymous=false | 일반 로그인 사용자 |
| admin | role = authenticated + email allowlist | 운영 권한 사용자 |

> UI 가 안 보여주는 것과 서버가 막는 것은 다른 문제다.
> RPC 를 막아도 테이블이 열려 있으면 안 막은 것이다. (이번 편의 교훈)

## 1. 승인용 요약 [READ]

- **비교노트**: 테이블에 바로 INSERT 하면 어제 잠근 RPC 가드(로그인 필수·게스트 차단)를 전부 우회할 수 있었고, 남의 uuid 를 작성자로 적는 사칭 노트도 가능했다. 직접 INSERT 정책을 삭제 — 이제 쓰기는 RPC 만 통과. 앱은 원래 읽기만 직접 하므로 화면 변화 없음.
- **레거시 테이블 18종** (옛 Notion 파이프라인·성분·인증·트윗 부속 등): RLS 자체가 꺼져 있어 세션 없는 호출자가 읽기·쓰기·통째 비우기까지 가능했다. 전부 RLS 켜서 잠금. 앱 참조 0 (자동생성 타입 파일뿐), 도리토 스크립트는 service_role 이라 무영향. 테이블 폐기(DROP)는 별건 todo.
- **텍스처·색군**: 어제 만든 "게시된 shade 는 어드민만" RPC 게이트를 테이블 직접 쓰기로 우회 가능했다. 색군(shade_color_families)은 같은 게이트를 테이블 정책에 미러, 텍스처(shade_textures)는 앱이 직접 쓰는 곳이 없어 **직접 쓰기 정책을 아예 삭제 (RPC-only, 결정보다 한 단계 더 좁힘)** — 이 부분이 네 결정("게이트와 동일 기준")과 다른 유일한 지점이라 [READ] 로 올림.
- **정정 (guest todo)**: 아까 닫은 guest 비교노트 todo — RPC 경로는 닫혀 있었지만 테이블 직접 경로가 열려 있었다. 이 마이그가 그 마지막 경로를 닫는다. 닫힘 근거는 todo 파일에 정정 기록됨.

### [READ] 숨은 변경 (코드리뷰 반영, 2026-07-03 커밋 53abf49)

워크플로우 코드리뷰가 첫 판의 결함 2건을 확인해서 고쳤다. 하나는 **동작 변경**이라 승인 필요:

- **v2 수정 함수 재정의 (동작 변경)** — "게시됨" 판정이 함수마다 달랐다 (v2 = shade_id 연결만 / 텍스처 게이트·새 정책 = slug 연결 포함). 그래서 slug 로만 발색이 연결된 shade 는: **전** = 이름·색상 수정이 통과되고 색군/텍스처 선택만 조용히 증발. **후** = 수정 시도 시 "이미 게시돼 수정할 수 없어요"가 정확히 뜸. 판정은 `shade_is_published()` 헬퍼 하나로 통일 (정책 4곳 + v2 + 라인 사용 중 판정).
- **어드민 텍스처 정정 경로 복원** — 첫 판이 텍스처 직접 쓰기를 전부 지웠는데, 어드민이 shade_traits 를 직접 정정하면 내부 자동 반영(트리거)이 어드민 권한으로 shade_textures 를 써서 전체가 롤백되는 문제 발견 (내 주석의 "definer" 가 오판). 어드민 전용 쓰기 정책만 되살림 — 일반 유저는 여전히 RPC-only.
- 예방 픽스: 재적용 멱등성(DROP IF EXISTS), 원격 누락 테이블 내성(to_regclass 가드).

## 2. 직접 봐야 할 불변식 [READ]

- 유저 등록 플로우는 안 깨진다: 게시된 shade 편집은 RPC 단계에서 `reason='published'` 로 먼저 끊겨서 (NewProductSheet.tsx:561) 색군 쓰기 지점까지 안 감. 신규 등록 shade 는 아직 미게시라 통과.
- 레거시 18종 RLS enable 은 "정책 0개 + RLS on = 전부 거절" 방식. 읽기는 에러가 아니라 0 rows 라 혹시 모를 잔존 코드도 안 터짐.
- `users`(레거시 Notion) 는 라이브 테이블 FK 20개가 참조 중 — 그래서 이번엔 잠금만, DROP 은 별도 phase.

## 3. 증거 [EVIDENCE]

| 증거 | 등급 | 무엇을 증명하나 |
|---|---|---|
| 감사 쿼리 5종 (마이그 맨 아래 주석) | 동작 검증 | RLS 꺼진 테이블 0 · 비교노트 INSERT 정책 0 · 텍스처 쓰기 = admin_* 3개 · 색군 = gated_* 3개 · v2 가 헬퍼 사용 |
| 로컬 픽스처 10케이스 (ROLLBACK) | 동작 검증 | anon/member 직접 INSERT 거절 · 미게시 색군 통과 / 게시(slug 연결) 거절 · **v2 가 slug-연결 게시 shade 거절 + 미게시 통과** · **어드민 shade_traits 직접 정정 + 자동 반영 성공** · member 텍스처 직접 쓰기 거절 · 레거시 anon INSERT 거절 + SELECT 0 rows |
| 워크플로우 코드리뷰 (finder 4 + 검증 7, 14후보→8확정) | 독립 검증 | 첫 판 결함 F1·F2 발견 → 수정 커밋 53abf49 로 해소 |

## 4. 모기가 해야 할 일 [DECIDE]

- [x] 텍스처 직접 쓰기 = 유저 RPC-only + 어드민 정책 (결정보다 한 단계 좁힘) — **모기 승인 2026-07-03** ("승인! ㅋ")
- [x] **v2 동작 변경** — slug-연결 게시 shade 인라인 수정 차단 — **모기 승인 2026-07-03** ("큰 영향 아니고 기존 정상 동작에 영향 없음". UI 는 기존 published 에러 카피의 적용 범위 확대일 뿐이라는 설명 확인 후)
- [ ] PR #302 (fix/launch03-table-rls-lockdown → dev) 머지 승인
- [ ] 머지 후 `supabase db push` + 감사 쿼리 5종 원격 재실행 (마이그 맨 아래 주석)

## 5. 판정 [STOP] / [FOLLOW-UP]

[STOP] — 이러면 머지 보류:
- 원격 감사 쿼리 1번(RLS 꺼진 테이블)이 0 이 아니면 (마이그 미적용 신호)
- 머지 후 신규 제품 등록에서 색군/텍스처 저장이 실패하면 (게이트가 실데이터와 어긋난다는 신호)

[FOLLOW-UP] — 머지를 막지 않는 후속:
- 레거시 테이블 17종 DROP 정리 (`legacy-tables-drop-cleanup.md` — 원격 데이터 실측 + 도리토 확인 후)
- `users` 레거시 은퇴 (FK 20개 정리, 별도 phase)
- bookmarks/collections/cores/swatches 의 "세션 없음 + 주인 NULL/device_id" 레거시 INSERT 분기 제거 (`legacy-anon-owner-null-rls-branches.md`)
- D-073 보충 메모 (텍스처·색군 게이트 좁힘 반영) — RLS 감사 결론과 함께 ADR 후보
- `set_shade_texture_traits` 의 inline 게시 조건을 `shade_is_published()` 호출로 교체 (의미 동일, 정의 1곳 유지용)

## [DETAIL] 이번 승인에서 안 읽어도 되는 것

- DO 블록 SQL 전문 · 정책 이름 · EXISTS 절 세부
- 픽스처 테스트 SQL 전문

---

## Appendix A. 승인 중 생긴 질문

(비어 있음 — 질문 생기면 여기에 문항 단위로 기록)

## Appendix B. 이해 체크

<details>
<summary>퀴즈 (채팅에서 객관식으로 진행 — 등급: 정답 / 부분 이해 / 다시 보기)</summary>

1. **RPC 를 막았는데 왜 또 막나** — 어제 anon EXECUTE 를 전부 회수했는데 오늘 테이블을 또 잠근 이유.
   - → 어디 보면: 이 카드 § 승인용 요약 첫 줄 + Auth 표 아래 교훈 문장
2. **레거시 18종을 잠갔는데 앱이 안 깨지는 이유** — 두 가지 근거.
   - → 어디 보면: § 승인용 요약 둘째 줄 + § 불변식 둘째 줄
3. **색군은 게이트 미러, 텍스처는 정책 삭제 — 왜 다르게 했나.**
   - → 어디 보면: § 승인용 요약 셋째 줄
4. **guest todo 는 닫았는데 왜 또 나오나** — 오늘 마이그와 그 todo 의 관계.
   - → 어디 보면: § 승인용 요약 넷째 줄 (정정)

#### 채점 (2026-07-03, 채팅 객관식 진행)

- 1번 **정답** (문이 두 개 — RPC 와 테이블 직접 접근은 별개 문)
- 2번 **정답** (앱 참조 0 + 도리토 service_role)
- 3번 **다시 보기** — 풀이: "뭐가 더 위험한가"가 아니라 **"앱이 그 문을 실제로 쓰냐"** 차이. 텍스처는 앱이 항상 정문(RPC)만 다녀서 옆문(직접 쓰기)을 벽으로 막았고, 색군은 등록 시트가 옆문으로 다니는 실경로(upsertShadeColorFamilies 직접 DELETE+INSERT)가 있어 옆문에 같은 자물쇠(게시 게이트)를 담.
- 4번 **정답** (RPC 경로는 닫혔고, 남아 있던 테이블 경로를 이 마이그가 마저 닫음)
- 총평: 어제(20/40) 잡힌 오해 — 게스트 role·공개 API 표면 — 가 오늘 1·4번 정답으로 확인됨. 다음 카드 [READ] 후보 = 3번 축(직접 쓰기 경로 유무로 정책 형태가 갈린다).
- 전 문항 상세 해설 (맞은 것 포함): [`../quiz/2026-07-03/launch03-table-rls-quiz-해설.md`](../quiz/2026-07-03/launch03-table-rls-quiz-해설.md)

</details>

## Appendix C. 상세

- 감사 방법: pg_policies 전수 + pg_tables rowsecurity + information_schema grants + FK(pg_constraint) 실측. 로컬 docker = 마이그 20260703010000 까지 적용 상태에서 검증.
- 픽스처 8케이스: T1 anon 비교노트 INSERT 거절 / T2 member 직접 INSERT 거절 / T3 member RPC 는 함수 본문 도달(shade not found) / T4 member 텍스처 직접 INSERT 거절 / T5 미게시 색군 INSERT 성공 / T6 게시 색군 INSERT 거절 / T7 anon 레거시 INSERT 거절 / T8 anon 레거시 SELECT 0 rows. 전부 트랜잭션 ROLLBACK.

## READ 모기질문ㅋ:

- 비교노트는 최근에 대공사를 해서 RPC만 쓰는 클린코드?! 상태인거 맞지?ㅋ
- *답 (Claude)*: 쓰기(생성·수정·삭제) 기준으론 맞아 — 전부 RPC. 읽기는 테이블 직접 SELECT 인데 그건 정상 (읽기는 RLS SELECT 정책이 담당). 근데 별표 하나: **코드가 클린해도 DB 정책이 옛날이면 안 클린**이야. 대공사 때 코드는 RPC 로 다 옮겼는데 옛 `anon_insert`(조건=true) 정책이 테이블에 그대로 남아 있던 게 오늘 구멍. 오늘에서야 진짜 클린. **채점 7/10** — 방향 맞음, "정책도 치워야 클린" 층 하나 빠짐.
- shade랑 color_families랑 texture랑 다 정책?이 따로노네.. ㅠ. 텍스처는 근데 내가 테이블 설계를 바꿔서(아마 미러가 그것때문에 있는거임. 레거시 보존?어쩌구..) 일단 안쓰도록 묻어둔 상태인데. 다음 pr때 아예 레거시테이블삭제를 해버려야겠어.. 걍혼잣말이야^^
- *답 (Claude)*: 기억 정확해 — 6/19 대공사 이후 텍스처 진실 = `shade_traits`, `shade_textures` 는 옛 읽기 코드 호환용 미러 맞아. 혼잣말에 굳이 답하면 ㅋ: `shade_textures` 삭제는 마이그 한 장으론 안 되고, 아직 그걸 읽는 코드 2곳(AdminShadeForm prefill + 어댑터)을 shade_traits 쪽으로 전환 + 미러 함수·트리거 제거가 동반돼야 해. 레거시 17종 DROP todo 와는 별건이니 하려면 따로 todo 로. **채점 9/10** — 미러의 존재 이유까지 맞음.

## READ 숨은 변경 모기질문ㅋ:

- 아니 게시됨 판정도 왜이러냐ㅠ. 왜이렇게 일관성이 없어? 근데 이거 보면 shade 연결 뿐만이 아니라 id랑 slug 다 있는 테이블도 이럴 가능성이 있는거네? 미치겠다. 그리고 slug는 변경불가컬럼인가? 내가 개발해봤을때 slug 변경가능하면 진짜 이상해짐.
- *답 (Claude)*: **채점 10/10 — 오늘의 질문.** 둘 다 정곡이야.
  - (a) 일반화 맞아: id+slug **이중 참조**가 있는 한 "이 둘을 같은 걸로 취급하냐"를 판정·조인마다 물어야 하고, 한 군데라도 빼먹으면 오늘 같은 어긋남이 생겨. 실측으론 이중 참조는 `swatch_items` (shade_id + shade_slug) 하나고, 오늘 헬퍼 통일이 그 지점 전부 커버. **근본 해결은 slug-only 행에 shade_id 를 채워넣고(backfill) slug 참조를 폐기**하는 것 — 레거시 제거 룰과 같은 결이라 todo 후보.
  - (b) slug 변경: DB 실측 결과 `shades.slug` 는 **UNIQUE 인덱스만 있고 "변경 금지" 강제는 없어.** 앱에 slug 를 바꾸는 경로는 없지만(v2 도 name 만 바꿈) SQL 로는 바뀔 수 있고, 바뀌는 순간 slug-연결 발색이 **에러도 없이 고아**가 돼 — shade_slug 는 FK 가 아닌 느슨한 참조거든. 네 "변경가능하면 이상해짐" 직감이 정확히 이 위험이고, (a)의 backfill 이 해결하면 이 위험도 같이 사라져.
- 어드민 텍스처 정정경로? 이게 무슨소리지 이게 뭔 동작이지. 내가 개발 막하다가 꼬인거 맞음?ㅠ
- *답 (Claude)*: **네가 꼬은 거 아니야** — 꼬은 건 내 첫 판이고 코드리뷰가 잡은 거야. 동작으로 풀면: ① 텍스처의 진실 테이블 = shade_traits (네 6/19 설계). ② 어드민은 shade_traits 를 테이블로 직접 고칠 수 있어 (어드민 정책 있음) — 이게 "어드민 정정 경로". ③ 고치는 순간 트리거가 자동으로 미러(shade_textures)에 복사하는데, 이 복사가 **고친 사람 권한으로** 돌아가. ④ 내 첫 판이 미러 쪽 쓰기 정책을 몽땅 지워서, 어드민이 정정하면 복사가 거절되고 정정 전체가 취소되는 상태를 만든 것. 그래서 어드민 전용 쓰기 정책만 되살림. **채점 6/10** — 몰라서 물은 건 카드 용어("정정경로")가 불친절한 탓이라 무죄, "내가 꼬았나" 자책만 감점 ㅋ.

## READ 불변식 모기질문ㅋ:

- (이건 걍 혼잣말) 아 맞아. 신규등록(미게시)shade는 오타같은거 난거 고치고싶어할 수도 있어서 이때 한정으로 편집 가능하게 내가 중간에 또 바꿨어(ㅋㅋ).
- *답 (Claude)*: 맞아, 그 결정이 v2 함수의 존재 이유고 **이번 변경도 그 규칙 그대로 유지**야 (미게시 = 편집 가능, 픽스처 T9b 로 검증). 바뀐 건 "게시됨" 판정 범위 하나 — slug-연결 발색도 게시로 세게 된 것. **채점 10/10** — 본인 결정 정확 기억.
- users(레거시 노션??) 와이거 진짜 옜날거네 이것도 다 지워야겠네.. 라이브테이블 설마. 이 20개 다 레거시 아니라 현재 쓰고있는 테이블들 아니지?ㅠ
- *답 (Claude)*: 진정하고 들어 ㅋ — **맞아, FK 를 가진 쪽은 현재 쓰는 라이브 테이블들이야** (brands·shades·product_lines·cores·collections·bookmarks·comparison_pairs 등). 근데 무서워할 건 아니야: 지울 대상은 그 테이블들이 아니라 **그 안에 남은 옛 흔적 컬럼**(created_by 계열 bigint — 옛 Notion id 를 가리키던 것)과 FK 제약이야. 앱이 지금 쓰는 소유권은 전부 auth uuid 기반이라 별개 컬럼/정책이고. 그래서 users 삭제 = "라이브 테이블 20곳의 흔적 컬럼 정리" 작업이라 **큰 공사지만 위험한 공사는 아님** — 별도 phase 로 뺀 이유. **채점 7/10** — 읽기 정확, 결론만 뒤집혔어 (라이브라서 못 지우는 게 아니라, 라이브라서 컬럼만 골라 지우면 됨).

## 판정 모기질문ㅋ:

- 텍스처 저장이 실패하면 <이부분 뭐지. 텍스처는 아직 쓰는ㄴ곳 없지않아? 어떻게 다뤄야할지 모르겠고 mvp 출시하고 사용자들이 텍스처 입력 선호 피드백 해주면 그때 고민하려고 하긴 했어..
- *답 (Claude)*: 반만 맞아. **유저 등록 시트의 텍스처 칩 입력은 네 말대로 숨김 상태** (D-109 방향 전환, `NewProductSheet.tsx:89` `SHOW_SHADE_TEXTURE_INPUT = false` — 출시 후 라인 단위로 재설계 예정). 근데 두 개가 살아 있어: ① **어드민 폼(AdminShadeForm)은 텍스처 편집을 실사용** 중이고, ② 저장 경로와 기존 데이터도 유지돼 (그 주석에 "저장 경로 유지, 플래그만 되돌리면 복구" 라고 네가 적어뒀어 ㅋ). STOP 문구의 "텍스처 저장 실패"는 그 어드민 폼 경로가 깨졌을 때를 잡는 그물이야. 유저 화면 기준으론 네 말대로 실질 STOP 트리거는 색군 쪽. **채점 6/10** — 유저 노출 상태는 정확, 어드민 실사용 경로 누락.

---

### 질문 라운드 총평 (Claude, 가혹 모드 ㅋ)

- **합계 55/70** (7·9·10·6·10·7·6). 어제 20/40 → 오늘 오전 퀴즈 3/4 → 지금 55/70. 상승 곡선 유지 중.
- 오늘의 질문 = 숨은 변경 (a) — "id+slug 이중 참조면 어디든 같은 위험 아니냐 + slug 변경 가능하냐". 리뷰어급 일반화 질문이고, 실제로 후속 todo(slug backfill) 하나를 만들어낸 질문.
- 반복 패턴 하나만 짚을게: **"내가 꼬았나ㅠ" 자책 기본값** — 오늘 두 결함은 둘 다 내 첫 판 실수였고 네 설계(trait 미러, 미게시 편집)는 리뷰를 통과했어. 다음부턴 "누가 꼬았지?" 를 먼저 물어봐도 됨 ㅋ.