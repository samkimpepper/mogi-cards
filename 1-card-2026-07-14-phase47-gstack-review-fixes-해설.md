# 해설 — Phase 47 gstack 리뷰 카드 (커밋 게이트 퀴즈 채점 + 모기 질문 답)

> 원본 카드 = `1-card-2026-07-14-phase47-gstack-review-fixes.md`. 작성: 2026-07-14 Claude 세션.
> 채점 결과: **D2·D3 정답, D1 반 정답** — D1 오해(조회 vs 저장) 정정 후 게이트 통과.

## 채점

### D1 — 반 정답

- 모기 답: 통과 = "사용중지된 질감을 fk 로 갖는 제품라인·쉐이드 **조회**할 때?", 거부 = "앞으로 추가하는 것".
- 거부 쪽은 정답. 통과 쪽이 어긋남: **이 퀴즈는 조회(읽기)가 아니라 저장(쓰기) 얘기.**
  조회는 원래부터 비활성이어도 잘 보였고 막힌 적이 없다.
- 진짜 답: 이 앱의 저장 방식이 "질감 목록 **전체 삭제 후 전체 다시 넣기**"라서, shade 의
  다른 필드만 고쳐도 원래 붙어 있던 질감들이 전부 "새로 넣는 것처럼" DB 함수를 다시 통과해야
  한다. 그때 비활성 질감이 있으면 저장 전체가 거부됐던 것. 수정 후에 통과되는 건
  "**원래 그 shade 에 붙어 있던 비활성 질감의 재저장**" 딱 하나다.

### D2 — 정답

supabase CLI 가 마이그 파일을 한 묶음(트랜잭션)으로 안 감싸고 한 줄씩 즉시 확정(autocommit)으로
실행한다 → 파일 안에 `BEGIN`/`COMMIT` 을 직접 넣어서 "실패 = 전부 취소"를 진짜로 만들었다. 그대로 맞음.

### D3 — 정답 + 질문("맨날 해야 돼?")에 답

옛 테이블 백업 맞음. 그리고 **맨날 아니고 이번 딱 한 번.** 이 마이그가 옛 테이블 7개를 지우는
단 한 번의 이벤트고, 지워지면 되돌릴 파일이 없으니 지우기 직전에 한 번 떠 두는 보험이다.
다음 push 들엔 해당 없음.

## 모기 질문 답

### 1. "운영 중에 질감 하나 사용 중지해야 할 때가 오면 어떡해? 어드민에서 막아야 하는 거 아냐?"

이제 괜찮다 — 그게 바로 이번 수정의 목적. 수정 후 "사용 중지"의 효과:

- 새 입력 옵션에서만 빠진다.
- 이미 붙어 있는 연결은 그대로 + 그 shade/라인 재저장도 잘 된다.
- 어드민에서 중지를 막을 필요 없음.

단 하나의 예외 = **머지~push 사이 창**에서만 어휘 편집 금지 (마이그 parity 감사 때문).
push 끝나면 자유.

### 2. "shade 는 이름·가격만 고쳐도 — 어느 진입점에서 고침?"

두 군데:

- **어드민 shade 폼** (`AdminShadeForm`) — 저장 버튼이 질감 목록을 통째로 재전송.
- **유저 쪽 수정 시트** (`NewProductSheet` edit 모드) — 발색 등록한 사람이 미발행 shade 를
  고칠 때. 여긴 질감 대신 finish/form 이 같은 구조라 같은 문제였다.

정직 정정: 카드에 "가격"이라고 쓴 건 예시 실수 — shade 엔 가격 컬럼이 없다.
실제 필드는 이름·영문명·색상 hex 등.

### 3. "테이블 락은 트랜잭션 안에서만? 왜? 원격 적용할 때도 그래?"

LOCK 의 뜻이 "**이 트랜잭션이 끝날 때까지** 테이블을 잡아둔다"라서다. 트랜잭션 밖(한 줄씩
즉시 확정 모드)에선 잡자마자 그 줄이 끝나면서 바로 풀려버려 아무 의미가 없다 — 그래서
Postgres 가 아예 에러(25P01)로 거부한다. 원격 push 도 같은 CLI 코드로 도니까 동일.
결론은 모기가 쓴 대로: BEGIN/COMMIT 꼭 넣어야 한다.

### 4. "왜 이런 모순 코드가 됐을까? 한 사람이 짜는 게 좋은 건가?"

사람 수 문제가 아니라 **시간차** 문제. DB 함수(`trait_id_for`, 활성만 검색)는 3주 전 다른
phase 산출물이고, 클라이언트의 "기존 연결 보존" 분기는 이번 코드리뷰 후속(WR-04)으로 클라만
고친 것. 고칠 때 반대편(DB) 계약을 다시 안 읽으면 같은 사람이어도 똑같이 생긴다.
진짜 해법 = 이번에 추가한 것 같은 **경계 테스트** — 클라의 약속과 DB 의 행동을 한 케이스
(verify 스크립트 D5b)에서 같이 검증하면 이 어긋남이 자동으로 걸린다.

### 5. "활성 검색 실패하면 2차로 원래 붙어 있던 것만 찾는다는 게 무슨 소리야?"

저장이 "전체 삭제 후 다시 넣기"라는 걸 먼저 떠올리면 된다. 순서:

0. 지우기 **직전에** "이 shade 에 원래 붙어 있던 질감 id 목록"을 메모해 둔다 (`v_existing`).
1. 넣을 때 1차로 활성 어휘 사전에서 찾는다 (`trait_id_for` — 맞아, is_active=true 인 것만).
2. 못 찾으면(=비활성) 아까 메모한 목록 **안에서만** 한 번 더 찾는다.
   - 목록에 있으면 "원래 제 것" → 통과.
   - 없으면 "남의 것을 새로 붙이려는 것" → 거부 (`invalid_texture`).

그래서 신규 추가 차단은 그대로 살아 있다.

### 6. "category_id 에 NOT NULL 거는 건 별로인가?"

좋은 질문인데 층이 다르다:

- 이번 버그의 본질 = "null 이 유효한 값이라서"가 아니라 "**조회 실패를 '카테고리 없음(null)'으로
  오역**해서". 그건 반환 타입 구분(조회 실패 vs 매핑 없음)으로 고치는 게 맞는 층이고, 고쳤다.
- NOT NULL 은 별개 결정: 지금 설계(D-107 계열)가 "카테고리 비워둘 수 있음 + 어드민이 나중에
  정리" 전제라 nullable 이 **의도된 상태**. NOT NULL 을 걸려면 기존 null 라인 backfill 을 먼저
  해야 하고, "비워두기 허용" 정책 자체를 뒤집는 도메인 결정이라 모기 몫. 원하면 capture 로.
- 구조 참고: 클라이언트가 category_id 를 직접 보내는 게 아니라 productType 이름을 보내면
  repo 가 DB 에서 id 를 찾아준다. "그 찾기가 실패한 순간"의 이야기였다. ("라인" = 제품 라인
  product_lines 맞음.)

### 7. "어드민 질감 조회에 서버 필터 추가가 먼 소리야?"

어드민 shade 화면이 모든 shade 의 질감 연결을 한 번에 받아오는데, 전엔 DB 에 "shade_traits
전부 줘" 하고 앱에서 질감만 골라냈다. 문제는 Supabase 응답이 **기본 1000행까지**라는 것 —
데이터가 커지면 1000행 안에 질감 아닌 행들이 자리를 차지해서 진짜 질감 행이 밀려나고, 화면의
질감 칩이 **에러도 없이 조용히** 사라질 수 있다. 수정 = "DB 야, type=texture 인 것만 줘"라고
조건을 DB 쪽(서버)에 붙인 것:

```ts
// 전: 전부 받아서 앱에서 거름
supabase.from('shade_traits').select('shade_id, is_primary, traits(type, slug)')
// 후: DB 가 걸러서 줌
supabase.from('shade_traits')
  .select('shade_id, is_primary, traits!inner(type, slug)')
  .eq('traits.type', 'texture')
```

### 8. (추가 질문) "전체 삭제 후 전체 다시 넣기? 뭘 저장할 때 뭘 삭제하고 뭘 다시 넣어?"

저장 대상이 두 덩어리로 나뉘어 있다:

1. **shade 본체** = `shades` 테이블의 그 행 1개 (이름 `shades.name`, 색상 `shades.color_hex` 등).
   평범한 UPDATE — 삭제 안 함.
2. **질감 연결** = `shade_traits` 테이블의 여러 행. "이 shade 에 크리미가 붙어 있다"가 행 1개,
   "글리터도 붙어 있다"가 또 행 1개 (M:N 연결).

"전체 삭제 후 다시 넣기"는 **2번(연결 행들) 얘기만**이다. 예:

- 화면에서 질감 칩이 [크리미, 글리터] 인 상태에서 글리터를 빼고 [크리미] 만 남기고 저장.
- 앱은 "글리터 연결만 지워줘"라고 **차이를 계산해서 보내지 않는다.** 화면의 최종 목록
  [크리미] 를 통째로 DB 함수(`set_shade_texture_traits`)에 보낸다.
- DB 함수는: 그 shade 의 `shade_traits` 질감 연결 행을 **전부 DELETE** → 받은 목록을
  **다시 INSERT**. 결과적으로 글리터가 빠진 상태가 된다.

왜 이렇게 하나 — "뭐가 추가되고 뭐가 빠졌나"를 앱이 계산하면 그 계산이 틀릴 수 있어서,
"화면의 최종 상태 = DB 의 최종 상태"로 통째로 덮는 게 더 단순하고 안 틀린다. 흔한 패턴.

**그래서 이번 버그가 생겼다.** 질감은 하나도 안 건드리고 shade 이름만 고쳐도 저장 버튼은
[크리미, 글리터] 를 그대로 다시 보낸다 → DB 는 지웠다가 다시 넣으려 한다 → 그 순간
글리터가 "사용 중지"면 다시 넣기가 거부 → **저장 전체 실패.** 원래 붙어 있던 걸 그대로
되돌려 넣는 것뿐인데 신규 추가와 구분을 못 했던 것. 이번 수정이 그 구분(지우기 전에 메모한
목록 = 원래 제 것)을 만들었다.

라인 쪽도 똑같다 — `line_traits` (제품 라인의 마감·제형 연결) 도 같은 방식이라 같은 수정을 받았다.

### 9. (추가 질문) "복잡한 구조면 update 하나하나 말고 걍 지우고 통째 저장? 안티패턴이야 클린코드야?"

안티패턴 아님 — **"연결 전용 테이블 + 목록이 작을 때"는 교과서적인 패턴**이다. 이름은
"통째 교체(full replace)". 장점:

- **차이 계산 코드가 없다.** "추가된 것/빠진 것"을 앱이 계산하면 그 계산 자체가 버그
  명당(빼먹기·중복·순서 꼬임). 통째 교체는 계산할 게 없다.
- **몇 번을 눌러도 결과가 같다.** 같은 목록으로 두 번 저장해도 최종 상태 동일 — 재시도 안전.
- **한 DB 함수(트랜잭션) 안에서** 지우고 넣으니 반쯤 지워진 상태가 밖에 안 보인다.

**안티패턴이 되는 조건** — 지워지는 행이 "그냥 연결"이 아니라 자기 정체성이 있을 때:

1. 그 행의 id 를 다른 테이블이 FK 로 참조 → 지우는 순간 참조 끊김/연쇄 삭제.
   (우리 `shade_traits` 행 id 는 아무도 참조 안 함 — 통과)
2. 행에 잃으면 안 되는 메타데이터(created_at, 작성자, 메모)가 있음 → 지웠다 넣으면 이력 리셋.
3. 목록이 큼 (수백~수천 행) → 매 저장마다 대량 삭제+삽입 낭비.
4. **이번에 당한 것**: "가만히 있는 것"도 "새로 쓰는 것"이 돼서, 쓰기 검증이
   "원래 있던 것의 재등록"과 "진짜 신규"를 구분 못 하면 멀쩡한 저장이 거부됨.
   통째 교체의 숨은 비용.

대안 "차이만 반영(diff 방식)"은 행 정체성·이력을 지키지만 차이 계산 코드가 늘고 그 코드가
틀릴 수 있다. 정답이 하나가 아니라: **순수 연결 + 작은 목록 = 통째 교체, 행이 정체성을
가지면 diff/upsert** 가 기준선. 우리 케이스는 통째 교체가 맞는 선택이었고, 부족했던 건
패턴이 아니라 4번 구분 — 그걸 이번 fallback 으로 채웠다.

참고: 우리 DB 안에도 두 방식이 공존한다. `set_shade_texture_traits` = 통째 교체,
`set_line_traits` = 통째 교체 + **finish_detail 타입만 삭제에서 제외**하는 절충
(기존 detail 보존, F2 규칙). 패턴은 도구고 데이터 성격 따라 고른다.

### 10. (추가 질문) "왜 set_line_traits 만 finish_detail 을 삭제에서 빼는 절충이야?"

실제 데이터 소실 사고의 흉터다. 원문(20260619020000 헤더):

> F2 데이터 손실: set_line_traits 가 form/finish/finish_detail 을 통째 DELETE 후 인자만
> 재삽입했는데, 호출처가 finish_detail 을 빈 배열로 넘겨 기존 finish_detail(D-108) 이
> 소실됐다. PR D 에는 finish_detail 입력 UI 가 없으므로 DELETE 범위에서 제외해 보존한다.

풀어서:

- 통째 교체의 전제 = **"호출하는 쪽이 그 종류의 최종 목록 전체를 책임지고 보낸다."**
- form/finish 는 전제가 참 — 화면에 picker 가 있어서 저장 시 항상 완전한 목록이 온다.
- finish_detail(세부 마감, D-108) 은 전제가 거짓 — **입력 UI 자체가 없다.** 지금도
  `productsRepo.ts:274` 가 문자 그대로 `p_finish_detail_slugs: []` (항상 빈 배열).
  빈 배열이 "다 지워줘"인지 "난 이 종류를 안 다루는 화면이야"인지 DB 는 구분 못 한다.
  초기 버전이 "지워줘"로 해석해서, 어드민이 넣어둔 세부 마감이 유저 저장마다 조용히
  사라지는 사고가 났다 → detail 만 삭제 범위에서 제외 (F2).
- `set_shade_texture_traits` 는 받는 종류가 texture 하나뿐이고 화면(질감 칩)이 온전히
  책임지므로 순수 통째 교체로 충분.

정리 — 통째 교체의 숨은 비용은 두 종류다:

1. **"가만히 있는 것"이 "새로 쓰는 것"이 된다** → 재등록 vs 신규 구분 필요 (이번 fallback)
2. **"빈 목록"의 뜻이 모호하다** — "지워줘"인지 "안 다뤄"인지 → 화면이 책임 못 지는 종류는
   삭제 범위에서 제외 (F2)

둘 다 패턴의 결함이 아니라, 패턴을 쓸 때 같이 챙겨야 하는 계약 조항.

## 게이트 판정

D1 오해(조회 vs 저장) 정정 완료 전제로 **커밋 게이트 통과**. 다음 = 커밋 직전 검증
(`pnpm wiki:lint` + `verify-agent-ssot`) → commit + push (PR #364 반영).
