---
id: guide-mogi-subjective-understanding-checks
title: "모기 주관식 이해 체크 — 객관식이 쉬울 때 쓰는 진단형 승인 질문"
type: guide
created_at: 2026-07-04
created_by: ChatGPT
updated_at: 2026-08-12
updated_by: Codex
last_verified_at: 2026-07-04
last_verified_by: Claude
status: active
tags: [guide, read-card, quiz, subjective-check, review, approval, human-in-the-loop, agent]
relations:
  - id: guide-mogi-read-cards
    label: extends
  - id: guide-mogi-review-packet
    label: related
  - id: guide-execution-plan-review-patterns
    label: related
audit_log:
  - action: created
    at: 2026-07-04
    by: ChatGPT
    note: "객관식 이해 체크가 모델별로 너무 쉬워지는 문제를 해결하기 위해 주관식 진단·잘못된 리뷰 댓글 반박·채점 중심 운영 가이드를 작성."
  - action: promoted
    at: 2026-07-04
    by: Claude
    note: "wiki guide 로 승격. 객관식 퀴즈 생성 가이드(v1/v2)는 승격하지 않고 docs/_personal/review-card/ 실험 기록으로 남김 — 해당 relation(supersedes-part) 제거."
  - action: updated
    at: 2026-07-04
    by: samkimpepper
    note: "docs(wiki): 모기 리드 카드 · 주관식 이해 체크 가이드 승격"
  - action: updated
    at: 2026-08-12
    by: Codex
    note: "이전 AskUserQuestion 선택에 앵커링되지 않도록 선택지·선택 결과를 숨긴 백지 재질문 절차 추가."
---

# 모기 주관식 이해 체크

이 문서는 Read Card 뒤에 붙는 **이해 체크**를 객관식 중심에서 주관식 진단 중심으로 바꾸는 가이드다.

객관식 퀴즈는 모델이 만들면 정답 보기가 너무 깔끔하고 길고 정확해져서, 문서를 깊게 이해하지 않아도 키워드만 보고 맞힐 수 있다. 이 문제를 해결하려고 객관식을 계속 어렵게 만들기보다, **모기가 직접 짧게 설명하고 에이전트가 채점하는 방식**으로 바꾼다. (객관식 생성 규칙을 보강하며 6라운드 실험한 기록은 `docs/_personal/review-card/` 의 quiz 가이드 v1/v2 에 있다 — 개인 기록, repo 미추적.)

## 한 줄 원칙

> 객관식 문제를 더 어렵게 만들려고 애쓰지 말고, **주관식 진단 + 잘못된 리뷰 댓글 반박 + 에이전트 채점**으로 이해를 확인한다.

## 왜 객관식만으로 부족한가

모델은 객관식 문제를 만들 때 보통 이렇게 한다.

```text
본문에서 중요한 문장 찾기
→ 그 문장을 가장 정확한 보기로 바꾸기
→ 나머지 보기를 살짝 틀리게 만들기
```

이 방식은 문서 요약에는 좋지만, 오해 탐지에는 약하다.

문제:

- 정답 보기가 제일 길고 구체적이다.
- 정답 보기가 원문 문장을 거의 다시 말한다.
- 오답은 너무 빨리 탈락한다.
- 파일명, todo명, 컴포넌트명 같은 키워드가 정답에만 몰린다.
- 모기가 실제로 뭘 이해했는지보다 “키워드를 찾았는지”만 확인된다.

따라서 객관식은 워밍업으로만 쓰고, 진짜 이해 확인은 주관식으로 한다.

## 이전 선택을 이유부터 묻지 않는다

카드에는 마스터가 AskUserQuestion으로 물었던 질문, 당시 선택지, 모기가 고른 답이 함께 기록될 수 있다. 다음 과외에서 "왜 그 답을 골랐어?"라고 시작하면 이미 선택한 답을 방어하는 사후합리화를 유도할 수 있다.

따라서 이전 선택을 다시 확인할 때는 아래 순서를 쓴다.

```text
기록된 질문·선택지·선택 결과를 과외냥이만 읽음
→ 선택지와 기존 선택을 숨김
→ 같은 판단의 핵심을 중립적인 주관식으로 다시 질문
→ 모기가 백지 답을 냄
→ 그 뒤에만 이전 선택을 공개하고 비교
```

운영 원칙:

- 기록된 선택은 당시의 역사적 결정이지 정답이 아니다.
- 새 질문에는 이전 선택의 표현, 선택지 문구, "전에 골랐던" 같은 단서를 넣지 않는다.
- 먼저 "왜"를 묻지 않는다. 우선 현재라면 무엇을 해야 한다고 보는지 답하게 한다.
- 새 답과 옛 선택이 같으면 독립적인 재현으로 본다.
- 둘이 다르면 새 답을 오답 처리하지 않는다. 현재 생각이 바뀌었는지, 질문의 숨은 조건을 다르게 읽었는지, 원래 카드 입력이 불완전했는지 확인한다.
- 사실 문제라면 코드·원문 근거로 평가하고, 제품 선택 문제라면 옛 선택과의 일치 여부로 채점하지 않는다.

예:

```text
나쁨: 전에 "필수 체크"를 골랐는데 왜 그게 맞다고 생각했어?
좋음: 이 CI가 실패한 PR은 머지할 수 있어야 할까? 모기 기준을 말해줘.
```

---

# 전체 구조

Read Card 끝에는 아래 순서로 이해 체크를 둔다.

```md
## 이해 체크

### A. 객관식 워밍업
- 최대 1~2문제
- 쉬워도 됨
- 다음 액션 실수 방지용

### B. 주관식 진단
- 2~4문제
- 키워드로 찍을 수 없음
- 막는 것/못 막는 것, 증명하는 것/못 증명하는 것, STOP/FOLLOW-UP 판단을 묻는다

### C. 잘못된 리뷰 댓글에 답하기
- 일부러 그럴듯하지만 틀린 리뷰 댓글을 제시
- 모기가 직접 반박 답변 작성

### D. 에이전트 채점
- 정답 / 부분 이해 / 다시 보기
- 빠진 핵심
- 위험한 오해
- 다음 Read Card [READ] 후보
- 후속 seed/todo/thread 후보
```

## 객관식의 역할

객관식은 완전히 버리지 않는다. 다만 역할을 낮춘다.

| 종류 | 역할 | 난이도 |
|---|---|---|
| 객관식 워밍업 | 문서의 핵심 단어와 다음 액션 확인 | 쉬워도 됨 |
| 주관식 진단 | 인과관계와 경계 이해 확인 | 키워드로 못 찍어야 함 |
| 잘못된 리뷰 댓글 반박 | 실제 PR 승인 상황에서의 판단력 확인 | 가장 중요 |
| 채점 | 빠진 핵심과 후속 분리 | 에이전트가 담당 |

객관식은 “읽었는지”를 보는 정도다.  
승인 이해 여부는 주관식과 반박 답변으로 본다.

---

# 주관식 진단 질문 유형

Read Card마다 아래 유형 중 2~3개를 고른다.

## 1. 막는 것 / 못 막는 것

가장 추천하는 기본형이다.

```md
Q. 이번 변경은 무엇을 막고, 무엇은 못 막나?

- 막는 것:
- 못 막는 것:
```

좋은 답은 “막는 것”만 쓰지 않고, 한계를 같이 쓴다.

예:

```md
Q. L56의 REVOKE INSERT는 무엇을 막고, 무엇은 못 막나?

내 답:
- 막는 것:
- 못 막는 것:
```

기대 핵심:

```text
막는 것:
- RLS 정책이 나중에 실수로 다시 생기거나 RLS가 꺼졌을 때 comparison_notes direct INSERT가 다시 열리는 것.

못 막는 것:
- SECURITY DEFINER RPC 내부 로직 버그.
- 다른 테이블 INSERT.
- 앱이 저장 실패를 삼키는 문제.
```

## 2. 증명하는 것 / 증명하지 못하는 것

테스트나 감사 쿼리 결과를 볼 때 쓴다.

```md
Q. 이번 테스트나 감사 쿼리는 무엇을 증명하고, 무엇은 증명하지 못하나?

- 증명하는 것:
- 증명하지 못하는 것:
```

예:

```md
Q. 감사 쿼리 5종이 통과했다. 이 결과가 증명하는 것과 증명하지 못하는 것은?

내 답:
- 증명하는 것:
- 증명하지 못하는 것:
```

기대 핵심:

```text
증명하는 것:
- RLS가 꺼진 테이블이 남아 있지 않은지.
- 특정 정책/권한이 기대 상태인지.
- 마이그가 의도한 DB 잠금 상태가 적용됐는지.

증명하지 못하는 것:
- 앱이 저장 실패를 사용자에게 제대로 보여주는지.
- 정상 UX가 실제로 자연스러운지.
- 원격 DB에 아직 db push가 적용되지 않은 상태에서 안전한지.
```

## 3. STOP / FOLLOW-UP / 정상

승인 판단을 연습하는 문제다.

```md
Q. 아래 상황은 STOP인가, FOLLOW-UP인가, 정상인가? 이유도 써라.

상황:
- 

판정:
- STOP / FOLLOW-UP / 정상

이유:
- 
```

예:

```md
상황:
- PR은 머지됐지만 아직 supabase db push 전이다.
- QA 원격에서 감사 쿼리를 돌렸더니 예전처럼 열린 항목이 보인다.

판정:
- 

이유:
- 
```

기대 핵심:

```text
정상일 수 있다.
PR 머지와 원격 DB 적용은 별개다.
db push 후 같은 감사 쿼리를 원격에서 다시 돌려야 한다.
```

## 4. 서버 권한 문제 / 앱 UX 문제 / 제품 결정

문제의 성격을 분류하는 훈련이다.

```md
Q. 이 이슈는 서버 권한 문제, 앱 UX 문제, 제품 결정 중 어디에 가깝나?

이슈:
- 

판정:
- 

이유:
- 
```

예:

```md
이슈:
- 세션 있는 사용자가 남의 shade에 발색을 붙여 그 shade를 게시 상태로 만들 수 있다.

판정:
- 

이유:
- 
```

기대 핵심:

```text
순수 보안 버그로 바로 막을지보다 제품 결정에 가깝다.
누구나 shade에 발색을 등록할 수 있다는 기존 제품 설계에서 나온 문제다.
다만 미게시 초안에 남이 발색을 붙일 수 있게 둘지는 후속 결정으로 봐야 한다.
```

## 5. 정상 플로우가 왜 안 깨지는가

가드를 조였을 때 정상 경로가 유지되는지 확인한다.

```md
Q. 이 가드를 조였는데 정상 앱 플로우가 왜 안 깨지나?

답:
- 
```

예:

```md
Q. 대표 사진 URL 검증을 추가했는데 정상 등록 플로우가 왜 안 깨지나?

답:
- 
```

기대 핵심:

```text
정상 플로우는 발색 등록이 먼저 성공해서 swatches.image_urls에 사진이 이미 들어간다.
그 다음 대표 사진 채움이 돌아서, 해당 URL이 실제 등록된 발색 사진인지 검증을 통과한다.
```

## 6. 이번 PR이 새로 만든 위험인가, 기존부터 있던 위험인가

PR blocker 판단에 중요하다.

```md
Q. 이 위험은 이번 PR이 새로 만든 것인가, 기존부터 있던 것인가?

위험:
- 

답:
- 

그래서:
- STOP / FOLLOW-UP / 정상
```

예:

```md
위험:
- 세션 있는 사용자가 남의 shade에 발색을 붙여 게시 상태로 만들 수 있음.

답:
- 기존 제품 설계에서 이미 가능했던 위험.
- 이번 RLS 잠금 PR이 새로 만든 우회는 아님.

그래서:
- 제품 결정 follow-up.
```

## 7. 이 변경이 바꾸는 동작 / 바꾸지 않는 동작

숨은 변경을 확인할 때 좋다.

```md
Q. 이번 변경이 실제로 바꾸는 동작과 바꾸지 않는 동작을 써라.

- 바꾸는 동작:
- 바꾸지 않는 동작:
```

예:

```md
Q. shade_is_published() helper 도입이 바꾸는 동작과 바꾸지 않는 동작은?

- 바꾸는 동작:
- 바꾸지 않는 동작:
```

기대 핵심:

```text
바꾸는 동작:
- slug로만 발색이 연결된 shade도 게시됨으로 판정되어 수정이 막힘.

바꾸지 않는 동작:
- 미게시 shade 편집 가능 규칙.
- 게시된 shade 편집 차단이라는 제품 원칙.
```

---

# 잘못된 리뷰 댓글에 답하기

이 방식이 가장 효과적이다. 객관식 보기 대신, 실제 PR에서 달릴 법한 **그럴듯하지만 틀린 리뷰 댓글**을 주고 모기가 직접 답하게 한다.

## 템플릿

```md
## 잘못된 리뷰 댓글에 답하기

리뷰어:
> <그럴듯하지만 틀린 코멘트>

모기 답:
> 
```

채점 기준:

```md
기대 핵심:
- 

부분 이해:
- 

다시 보기:
- 

자주 나올 오해:
- 
```

## 예시 1 — REVOKE INSERT 이중 잠금

```md
리뷰어:
> comparison_notes INSERT 정책을 지웠으면 끝 아닌가요?
> REVOKE INSERT까지 하는 건 과잉으로 보여요.

모기 답:
> 
```

기대 답:

```text
정책 삭제로 지금은 막히지만, 테이블 INSERT 권한이 남아 있으면
나중에 RLS가 꺼지거나 정책이 다시 생겼을 때 direct INSERT가 다시 열릴 수 있다.
그래서 REVOKE INSERT는 지금 뚫린 구멍 fix가 아니라 하드닝이다.
다만 SECURITY DEFINER RPC 내부 버그나 다른 테이블 INSERT까지 막는 것은 아니다.
```

채점 포인트:

| 등급 | 기준 |
|---|---|
| 정답 | RLS 정책과 테이블 GRANT가 다른 층임을 말하고, direct INSERT 하드닝이라고 설명 |
| 부분 이해 | “두 겹 잠금”은 말했지만 무엇을 못 막는지 누락 |
| 다시 보기 | 지금 실제로 뚫린 구멍을 막은 것이라고 설명하거나 RPC까지 막는다고 설명 |

## 예시 2 — anon GRANT

```md
리뷰어:
> 어제 anon EXECUTE를 전부 회수했는데,
> shade_is_published 함수에 anon GRANT를 다시 주면 구멍을 다시 여는 것 아닌가요?

모기 답:
> 
```

기대 답:

```text
모든 anon EXECUTE가 같은 위험은 아니다.
이 함수가 데이터 변경 없이 공개 가능한 게시 여부만 판정하는 읽기 helper라면
anon 실행 가능은 의도된 예외로 문서화할 수 있다.
단, 함수가 mutation을 하거나 private data를 읽는다면 같은 결론이 아니다.
```

채점 포인트:

| 등급 | 기준 |
|---|---|
| 정답 | 공개 가능한 read helper와 상태 변경 RPC를 구분하고, 의도적 예외/allowlist로 설명 |
| 부분 이해 | “읽기 전용이라 괜찮다”만 말하고 예외 조건을 누락 |
| 다시 보기 | 모든 anon EXECUTE는 무조건 금지라고 하거나, authenticated와 anon 차이를 무시 |

## 예시 3 — WARNING 로그

```md
리뷰어:
> db push 로그에 "원격에 없는 테이블을 건너뜀" WARNING이 떴으니
> 마이그가 실패한 것 아닌가요? 롤백해야 하는 것 같은데요.

모기 답:
> 
```

기대 답:

```text
WARNING은 에러가 아니라 마이그를 멈추지 않는 신호다.
의도는 없는 테이블을 조용히 skip하지 않고 드러내는 것이다.
다만 그 테이블이 정말 원격에 없어도 되는 레거시인지, 아니면 오타나 rename 때문에 빠진 것인지는 확인해야 한다.
감사 쿼리 통과와 별개로 로그를 눈으로 확인할 필요가 있다.
```

채점 포인트:

| 등급 | 기준 |
|---|---|
| 정답 | WARNING은 실패가 아니지만 확인이 필요하다고 설명 |
| 부분 이해 | 실패는 아니라고 했지만 오타/rename 확인 필요를 누락 |
| 다시 보기 | 무조건 rollback 또는 완전 무시 중 하나로 판단 |

## 예시 4 — SQL 감사와 앱 UX

```md
리뷰어:
> 감사 쿼리 5종이 통과했으니 저장 실패를 앱이 삼키는 문제도 해결된 거 아닌가요?

모기 답:
> 
```

기대 답:

```text
아니다.
감사 쿼리는 DB의 RLS/정책/권한 상태를 확인한다.
앱이 색군/텍스처 저장 실패를 console.warn만 찍고 사용자에게 성공처럼 보이는 문제는 앱 저장 흐름 문제라 감사 쿼리로 잡히지 않는다.
이건 SQL 마이그 blocker라기보다 앱 UX/데이터 정합성 후속이다.
```

채점 포인트:

| 등급 | 기준 |
|---|---|
| 정답 | 감사 쿼리가 증명하는 것과 앱 UX 실패를 구분 |
| 부분 이해 | 앱 문제라고는 했지만 감사 쿼리의 한계를 설명 못 함 |
| 다시 보기 | 감사 쿼리가 앱 저장 실패까지 증명한다고 봄 |

## 예시 5 — UI gate와 서버 gate

```md
리뷰어:
> 일반 유저 화면에는 이 버튼이 안 보이니까 서버에서 막지 않아도 되지 않나요?

모기 답:
> 
```

기대 답:

```text
아니다.
UI에서 버튼이 안 보이는 것은 UX일 뿐이고,
RPC나 테이블은 개발자도구나 직접 API 호출로 화면 없이 호출될 수 있다.
서버/RLS/RPC 가드가 최종 권한 경계다.
```

채점 포인트:

| 등급 | 기준 |
|---|---|
| 정답 | UI gate와 서버 gate를 분리 |
| 부분 이해 | “서버에서도 막아야 한다”만 말하고 이유가 약함 |
| 다시 보기 | UI 숨김을 권한 보장으로 봄 |

---

# 에이전트에게 이해 체크를 만들게 하는 프롬프트

## 기본 프롬프트

```text
docs/wiki/guides/mogi-subjective-understanding-checks.md를 읽고,
아래 Read Card의 이해 체크를 만들어줘.

이번에는 객관식 중심으로 만들지 마.
객관식은 최대 1~2문제만, 주관식 진단을 3문제 이상 만들어.

목표:
- 키워드 매칭으로 맞히는지 확인하는 게 아니라,
  모기가 실제로 인과관계와 경계를 이해했는지 확인하는 것.

주관식 문제는 아래 형식 중에서 골라:
1. 무엇을 막고 / 무엇은 못 막는가
2. 무엇을 증명하고 / 무엇은 증명하지 못하는가
3. 이 상황은 STOP / FOLLOW-UP / 정상 중 무엇인가
4. 서버 권한 문제 / 앱 UX 문제 / 제품 결정 중 무엇인가
5. 정상 플로우가 왜 안 깨지는가
6. 이번 PR이 새로 만든 위험인가, 기존부터 있던 위험인가
7. 이 변경이 바꾸는 동작 / 바꾸지 않는 동작은 무엇인가

반드시 포함:
- 잘못된 리뷰 댓글에 답하기 1~2개
- 각 주관식의 채점 기준
  - 정답에 반드시 들어가야 할 핵심
  - 부분 이해로 볼 답
  - 다시 보기로 볼 답
  - 자주 나올 오해

객관식은 쉬워도 되지만,
주관식이 진짜 이해 확인이어야 한다.

문제 생성 후,
"이 문제를 키워드만 보고 찍을 수 있는가?"를 스스로 검토해.
찍을 수 있으면 다시 써.
```

## 강한 프롬프트

객관식이 계속 너무 쉬우면 이 버전을 쓴다.

```text
아래 Read Card로 이해 체크를 만들어줘.

중요:
객관식 문제를 어렵게 만들려고 하지 마.
주관식 진단과 잘못된 리뷰 댓글 반박이 중심이다.

출력 순서:
1. 이번 카드에서 모기가 오해할 수 있는 지점 3~5개
2. 객관식 워밍업 최대 1문제
3. 주관식 진단 3문제
4. 잘못된 리뷰 댓글에 답하기 2문제
5. 각 문제별 채점 기준
6. 채점 프롬프트
7. 이 이해 체크가 키워드 매칭으로 풀리지 않는 이유

주관식은 반드시 아래 중 하나를 물어라:
- 무엇을 막고 / 못 막는가
- 무엇을 증명하고 / 못 증명하는가
- STOP / FOLLOW-UP / 정상 중 무엇인가
- 서버 권한 / 앱 UX / 제품 결정 중 무엇인가
- 정상 플로우가 왜 안 깨지는가
- 이번 PR이 새로 만든 위험인가, 기존부터 있던 위험인가
- 바꾸는 동작 / 바꾸지 않는 동작은 무엇인가

잘못된 리뷰 댓글은 실제 PR에 달릴 법하게 써라.
너무 바보 같은 댓글은 금지.
```

## 아주 짧은 프롬프트

```text
이 Read Card로 주관식 이해 체크를 만들어줘.

객관식은 최대 1개만.
주관식 3개 + 잘못된 리뷰 댓글 반박 1개를 만들어줘.

각 문제에 채점 기준을 붙여:
- 정답
- 부분 이해
- 다시 보기
- 자주 나올 오해

키워드 매칭으로 맞히는 문제가 아니라,
모기가 직접 인과관계를 설명해야 하는 문제로 만들어줘.
```

---

# 모기가 답한 뒤 채점 프롬프트

모기가 답을 쓴 뒤 에이전트에게 아래처럼 채점시킨다.

```text
내 이해 체크 답안을 채점해줘.

채점 방식:
- 정답 / 부분 이해 / 다시 보기

각 문항마다:
1. 내 답에서 맞은 부분
2. 빠진 핵심
3. 위험한 오해
4. 다음 Read Card 상단에 올릴 한 줄
5. 후속 seed/todo/thread 후보가 있는지

주의:
- 나를 띄워주지 말고, 실제 승인자가 틀리면 위험한 부분을 우선 짚어.
- 답이 애매하면 "부분 이해"로 둬.
- 내가 질문을 잘 던졌다면, 어떤 후속으로 분리하면 좋을지도 말해줘.
- 자책 표현에 끌려가지 말고, 구조적으로 무엇을 오해했는지 봐줘.
```

## 채점 결과 템플릿

```md
## 이해 체크 채점

### Q1
판정: 정답 / 부분 이해 / 다시 보기

맞은 부분:
- 

빠진 핵심:
- 

위험한 오해:
- 

다음 카드 [READ] 후보:
- 

후속:
- 없음 / seed / todo / thread / STOP

---

## 총평

잘 잡은 것:
- 

다시 볼 것:
- 

이번 답변에서 발견한 후속:
- 

다음 승인 메모에 넣을 한 줄:
> 
```

---

# Read Card에 붙일 이해 체크 템플릿

```md
## 이해 체크

### A. 객관식 워밍업

1. <쉬운 행동 확인 문제>

### B. 주관식 진단

**Q1. 막는 것 / 못 막는 것**
- 질문:
- 내 답:
  - 막는 것:
  - 못 막는 것:

**Q2. 증명하는 것 / 증명하지 못하는 것**
- 질문:
- 내 답:
  - 증명하는 것:
  - 증명하지 못하는 것:

**Q3. STOP / FOLLOW-UP / 정상**
- 상황:
- 내 판정:
- 이유:

### C. 잘못된 리뷰 댓글에 답하기

리뷰어:
> 

모기 답:
> 

### D. 채점 기준

<details>
<summary>채점 기준</summary>

Q1 기대 핵심:
- 

Q2 기대 핵심:
- 

Q3 기대 핵심:
- 

리뷰 댓글 기대 핵심:
- 

</details>
```

---

# PR 본문에 넣을 때

PR 본문에는 전체 이해 체크를 접어도 된다.  
상단에는 요약만 둔다.

```md
## 이해 체크 요약

- 객관식 워밍업:
  - 통과 / 다시 보기

- 주관식 진단:
  - 잘 잡은 것:
  - 다시 볼 것:

- 잘못된 리뷰 댓글 반박:
  - 통과 / 부분 이해 / 다시 보기

- 새로 발견한 후속:
  - 
```

전체 문답은 아래처럼 접는다.

```md
<details>
<summary>이해 체크 전문</summary>

...질문/답/채점...

</details>
```

---

# 주관식 답변 예시

## 예시 A — 막는 것 / 못 막는 것

질문:

```md
Q. comparison_notes에 REVOKE INSERT를 추가한 것은 무엇을 막고, 무엇은 못 막나?
```

좋은 답:

```md
막는 것:
- 나중에 RLS가 꺼지거나 INSERT 정책이 다시 생겼을 때, 테이블 직접 INSERT가 다시 열리는 것을 막는다.

못 막는 것:
- SECURITY DEFINER RPC 내부 검증 버그.
- 다른 테이블 INSERT.
- 앱이 저장 실패를 사용자에게 숨기는 문제.
```

부분 이해:

```md
막는 것:
- 이중 잠금이다.

못 막는 것:
- 잘 모르겠음.
```

채점:

```text
부분 이해.
"이중 잠금" 방향은 맞지만, 어떤 문을 막고 어떤 문을 못 막는지 빠졌다.
다음부터는 direct table write / RPC / app UX를 나눠서 써야 한다.
```

## 예시 B — 증명하는 것 / 못 증명하는 것

질문:

```md
Q. 로컬 픽스처가 "게시 shade 색군 직접 INSERT 거절"을 검증했다. 이게 증명하는 것과 증명하지 못하는 것은?
```

좋은 답:

```md
증명하는 것:
- DB 정책이 게시 shade에 대한 직접 색군 쓰기를 거절한다.

증명하지 못하는 것:
- 실제 앱에서 사용자에게 에러가 잘 보이는지.
- 원격 DB에 마이그가 적용됐는지.
- 다른 저장 경로까지 모두 같은 방식으로 막히는지.
```

## 예시 C — STOP / FOLLOW-UP

질문:

```md
Q. PR 머지 후 db push 전 원격 감사 쿼리에서 예전 결과가 보인다. STOP인가?
```

좋은 답:

```md
정상일 수 있다.
PR 머지와 원격 DB 적용은 별개라서, db push 전에는 원격이 예전 상태인 게 맞다.
db push 후에도 같은 결과가 나오면 그때 STOP이다.
```

## 예시 D — 제품 결정 vs 보안 버그

질문:

```md
Q. 세션 있는 사용자가 남의 shade에 발색을 붙여 게시 상태로 만들 수 있다. 이건 보안 버그인가 제품 결정인가?
```

좋은 답:

```md
제품 결정 쪽에 가깝다.
누구나 shade에 발색을 등록할 수 있다는 기존 제품 설계에서 나온 문제다.
이번 PR이 새로 만든 RLS 우회는 아니다.
하지만 미게시 초안까지 남이 게시 상태로 만들 수 있게 둘지는 후속 결정이 필요하다.
```

---

# 채점 등급

숫자 점수보다 아래 세 등급을 권장한다.

| 등급 | 의미 |
|---|---|
| 정답 | 핵심 인과관계와 한계를 맞게 설명 |
| 부분 이해 | 방향은 맞지만 중요한 경계나 한계 누락 |
| 다시 보기 | 반대 개념으로 이해했거나 실제 행동을 잘못할 위험 있음 |

숫자 점수는 자극이 되면 쓸 수 있지만, 자책으로 흐르면 쓰지 않는다.

## 좋은 채점은 무엇인가

좋은 채점은 사람 평가가 아니라 **다음 행동 분류**다.

채점 결과는 아래 중 하나로 이어진다.

| 발견 | 처리 |
|---|---|
| 용어 오해 | 다음 Read Card 상단 glossary 추가 |
| 제품 결정 질문 | thread 또는 ADR 후보 |
| 머지 blocker | STOP 승격 |
| 머지 후 정리 | todo |
| 미래 구조 개선 | seed |
| 문서 문구 문제 | Read Card 개선 |

---

# 자주 나오는 주관식 채점 포인트

## RLS/RPC/권한 카드

반드시 보게 할 구분:

```text
UI gate / RPC gate / table RLS / table GRANT / SECURITY DEFINER / service_role
```

좋은 답은 이 층을 섞지 않는다.

## 마이그 카드

반드시 보게 할 구분:

```text
로컬 적용 / PR 머지 / 원격 db push / 원격 감사 쿼리
```

좋은 답은 “머지됐으니 원격도 바뀜”이라고 하지 않는다.

## 테스트 카드

반드시 보게 할 구분:

```text
grep/tsc/build / SQL 감사 / 픽스처 동작 검증 / 브라우저 UAT
```

좋은 답은 각 증거가 무엇을 증명하고 무엇을 못 증명하는지 말한다.

## follow-up 카드

반드시 보게 할 구분:

```text
STOP / non-blocking todo / seed / product thread / ADR 후보
```

좋은 답은 찜찜함을 전부 머지 blocker로 올리지 않고, 성격에 맞게 분류한다.

---

# 객관식을 계속 쓰고 싶다면

객관식을 완전히 버릴 필요는 없다. 다만 아래 규칙을 지킨다.

- 최대 1~2문제.
- 행동 확인용으로 둔다.
- 정답이 쉬워도 괜찮다.
- 오해 탐지는 객관식이 아니라 주관식으로 한다.
- 객관식이 쉬운 것을 문제로 보지 않는다. 객관식의 역할이 낮아졌기 때문이다.

예:

```md
Q. PR 머지 후 모기가 바로 해야 할 일은?
- db push + 원격 감사 쿼리 재실행
```

이건 쉬워도 된다.  
실제 행동 실수 방지용이기 때문이다.

---

# 최종 요약

- 객관식은 힌트가 너무 많다.
- 모델은 객관식을 만들 때 정답 보기를 본문 요약처럼 만드는 경향이 있다.
- 객관식을 어렵게 만드는 데 계속 힘쓰지 않는다.
- Read Card 이해 체크는 주관식 진단으로 바꾼다.
- 기본 질문은 “무엇을 막고/못 막는가”, “무엇을 증명하고/못 증명하는가”, “STOP/FOLLOW-UP/정상인가”다.
- 잘못된 리뷰 댓글에 답하게 하면 실제 PR 승인 판단이 잘 드러난다.
- 에이전트는 문제를 만드는 것보다 모기의 답을 채점하는 일을 더 잘한다.
- 퀴즈 결과는 평가가 아니라 다음 Read Card, todo, seed, thread, STOP을 고르는 입력이다.
- 기록된 AskUserQuestion 선택은 답을 받은 뒤에만 비교한다. 그 전에는 선택지 없는 백지 주관식으로 다시 묻고, 과거 선택을 정답으로 취급하지 않는다.
