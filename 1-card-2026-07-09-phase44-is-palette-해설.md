# 해설 (쉬운 말) — phase 44 리드카드 퀴즈 + 모기 질문

> 카드: `1-card-2026-07-09-phase44-is-palette-plan.md`. 채점 원문은 카드 맨 아래, 여기는 그걸 풀어쓴 버전.

## 0. 먼저 그림 — 저장소 두 개랑 딱지

등장하는 저장소는 두 개뿐이야.

- **원본 보관함** (`swatches` + `swatch_items`): 유저가 올린 발색 등록 원본. "사진 + 이 사진에 어떤 칸(호수)들이 있는지" 가 들어 있고, **한 번 들어오면 안 사라져**.
- **대표사진 자리** (`shade_images`): 칸 카드마다 "지금 카드에 걸려 있는 사진 한 장" 이 놓이는 자리. **자리가 하나뿐이라** 더 나은 사진이 오면 갈아끼워져. 사진 옆에 **딱지** (`representative_source` 컬럼) 가 붙어 — "이 사진 어떻게 뽑힌 거임?"

딱지는 4종류고, 힘 순서가 있어:

| 딱지 (DB 값) | 쉬운 뜻 | 힘 |
|---|---|---|
| `manual` | 어드민이 손으로 고른 사진 | 제일 셈 — 아무도 못 덮음 |
| `auto_palette_evidence` | 팔레트 모임샷 | 자동 중 제일 셈 |
| `auto_single` | 그 칸만 찍은 단독샷 | 중간 |
| `auto_comparison_fallback` | 임시 사진 (더 나은 거 오면 바뀜) | 제일 약함 |

"fallback" 이라는 말 = 저 맨 아래 딱지. **"일단 이거라도 걸어두는데, 더 나은 사진 오면 자동으로 바뀌는 임시 신분"** 이라는 뜻이야. 대표사진 얘기 맞아.

## 1. B1 해설 — 체크 깜빡한 진짜 팔레트는 어떻게 되나

네 답에서 맞은 것: 모임샷이 임시 딱지로 저장된다는 것.
헷갈린 것: "나중에라도 fallback 될 수 있는 상태" — 방향이 반대야.

장면으로 보면:

1. 어드민이 팔레트 체크를 깜빡한 라인에 누가 모임샷 등록 → 그 사진이 카드 대표로 걸리긴 하는데, 딱지가 **"임시"** 로 붙어. (= 등록되는 순간 이미 임시 신분. 나중에 임시가 "되는" 게 아니라.)
2. 이 상태에서 단독샷이 오면 → 임시보다 세니까 자동 교체. 팔레트 모임샷이 와도 (체크 켠 뒤) → 역시 교체.
3. 어드민이 체크를 켜고 **재판정 버튼**을 누르면 → 1번의 모임샷이 "팔레트 모임샷" 딱지로 **승급**돼서 배지·팔레트 블록·대표사진이 다 정상 팔레트처럼 돌아와.

왜 이 방향이 "안전" 이냐: 실수의 결과를 비교해보면 돼.

- 체크 **깜빡** 실수 → 결과 = 임시 신분. 자동 교체도 되고 재판정으로 회복도 됨. **고칠 수 있음.**
- 반대로 지금 구조 (체크 없음) 의 실수 = 단품 비교샷이 "팔레트 모임샷" 딱지를 받아버림 → 저 딱지는 자동 중 제일 세서 **아무 자동 규칙도 못 덮음. 영구 고정.**

같은 실수라도 한쪽은 고칠 수 있고 한쪽은 못 고쳐. 그래서 "깜빡하면 임시로 떨어지는" 쪽을 고른 거야.

## 2. B2 해설 — 이거 D-100 번복 아니냐는 댓글

네 답 정답. 풀어 쓰면: D-100 결정문 자체에 **"is_palette 같은 컬럼은 지금은 안 만든다. 필요가 진짜 생기면 그때 별도 결정으로 추가한다"** 라는 문장이 이미 있어. 지금이 바로 그 "필요가 생긴 때" (단품 비교샷 오포획). 그러니까 결정을 뒤집는 게 아니라 **결정문이 미리 열어둔 문으로 들어가는 것**. 문서 작업도 새 결정문이 아니라 D-100 에 한 절 덧붙이면 끝이고, 그 승인을 네가 하는 게 44-04 게이트야.

## 3. B3 해설 — 원격 반영 순서 (네가 등판하는 2번)

네 답 "execute 하고 나서 db push" 는 맞는데, 등판이 **두 번**이라는 게 포인트야.

- **1번째 등판 (마이그 쓰기 전!)**: 과거 데이터 정정 (44-03) 을 하려면 "QA 원격에 뭐가 잘못 잠겨 있는지" 랑 "이더앤 멀티 글림바 라인이 DB 에 뭐라고 저장돼 있는지" 를 알아야 하는데, 그건 **QA 원격에만 있어서 에이전트가 못 봐**. 네가 쿼리 2개 돌려서 결과 주고 "이건 진짜 팔레트 / 이건 오포획" 을 확정해줘야 그때부터 정정 마이그를 쓸 수 있어. 쿼리 원문은 `44-03-PLAN.md` Task 1 에 준비돼 있음.
- **2번째 등판 (PR 머지 후)**: `supabase db push` 로 원격 반영 + 이더앤 라인 재판정 눌러서 배지·블록·대표사진 돌아오는지 화면 확인.

그러니까 "아직 마이그 안 만들어졌는데?" 가 맞고, 그중 44-03 마이그는 **네 실측이 와야 만들어질 수 있는** 마이그야.

## 4. 네 질문들 쉬운 답

- **"늦게 켜도 문제 안 되게 하는 게 맞지?"** — 맞아. 그 장치가 재판정 버튼이고, 그 버튼이 일부 케이스를 못 잡는다는 게 이번 리뷰 발견 (아래 5번).
- **"pan_count 뭔소리?"** — "이 팔레트 몇 칸짜리" 를 적는 컬럼. 팔레트 관련해서 만들까 말까 하던 목록 중 이번에 **팔레트 여부 체크만** 만들고, 칸 수 같은 나머지는 계속 안 만든다는 얘기. 이번 작업에서 신경 쓸 것 없음.
- **"강등이 뭐야?"** — 과거에 잘못 "팔레트 모임샷" 딱지를 받아 영구 고정된 카드를, **사진은 그대로 두고 딱지만 "임시" 로 낮춰 적는 것**. 지우는 게 아니라서, 만약 정정이 실수였어도 (알고 보니 진짜 팔레트) 체크 켜고 재판정하면 도로 승급돼. 되돌릴 수 있는 방향만 골라 쓰는 거야.

## 5. 리뷰 발견 (plan 수정 후보) — 장면 3개로

네 재이해에서 한 장면이 빠졌었어. 전체는 이래:

1. 체크 깜빡한 팔레트 라인에 **모임샷** 등록 → 대표 = 모임샷, 딱지 = 임시.
2. 누가 **단독샷** 등록 → 단독샷이 임시보다 세니까 **같은 자리를 갈아끼움**. 이제 대표 = 단독샷, 딱지 = 단독샷. 모임샷은 대표사진 자리에서 흔적이 사라지고 원본 보관함에만 남음.
3. 어드민이 체크 켜고 재판정 버튼 → 지금 plan 의 버튼은 **"임시 딱지 달린 것"** 만 찾는데, 2번에서 딱지가 이미 단독샷으로 바뀌었으니 **찾을 게 0건**. 모임샷은 원본 보관함에 멀쩡히 있는데 버튼이 안 봐.

"돌이킬 수 없음" 은 아니야 — 새 모임샷이 또 등록되거나 어드민이 손으로 고르면 회복돼. 문제는 **버튼 한 방으로 회복** 이 안 되는 것. 고치는 법 = 버튼이 찾는 곳을 "딱지 (지워질 수 있음)" 에서 "**원본 보관함의 매핑** (안 지워짐)" 으로 바꾸기. 진짜 팔레트 자격 심사는 어차피 서버가 다시 하니까 새 로직은 안 생겨.
