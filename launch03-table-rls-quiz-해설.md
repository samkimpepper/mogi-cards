---
type: personal
status: active
owner: samkimpepper
created: 2026-07-03
topic: LAUNCH-03 테이블 RLS 잠금 (PR #302) 퀴즈 해설 — 맞은 것 포함 전 문항
card: ../review-card/mogi-read-review-launch03-table-rls.md
---

# LAUNCH-03 테이블 RLS 퀴즈 해설 (2026-07-03)

> 결과: 정답 3 / 다시 보기 1 (직전 RPC 감사 카드 20/40 에서 상승)
> 원문: `supabase/migrations/20260703010000_launch03_table_rls_lockdown.sql` · PR #302

---

## 1번 — 어제 RPC 잠갔는데 왜 오늘 테이블을 또 잠그나

**내 답: "문이 두 개라서" → 정답**

DB에 쓰기가 들어가는 길이 두 갈래야:

```
클라이언트 ──① RPC(함수) 호출──→ 함수가 대신 씀
클라이언트 ──② 테이블에 직접 INSERT/UPDATE──→ 바로 씀
```

- 어제(#301)는 **①번 문**을 잠갔어 — 세션 없는 호출자가 함수를 실행 못 하게 (anon EXECUTE 회수) + 함수 안에 가드 추가.
- 근데 Supabase 는 anon key 만 있으면 **②번 문(테이블 직접 접근)도 공개 API**야. 이 문의 자물쇠가 RLS 정책인데, `comparison_notes` 의 INSERT 정책이 "조건 = true" (아무나 통과)로 있었어.
- 결과: 함수 가드가 아무리 세도, 공격자는 함수를 안 부르고 테이블에 바로 INSERT 하면 끝. **①만 잠그면 ②로 우회됨.**

오답 해설:
- "어제 revoke 불완전" — 아니야, 어제 revoke 는 함수 대상으론 전수 완료였어. 오늘 건 재작업이 아니라 다른 문.
- "RLS 있어야 RPC 가드 동작" — 반대에 가까워. RPC(SECURITY DEFINER)는 오히려 **RLS 를 안 타고** 동작해. 둘은 독립.

---

## 2번 — 레거시 18종 잠가도 안 깨지는 근거

**내 답: "앱 참조 0 + 도리토 service_role" → 정답**

잠그면 깨질 수 있는 이해관계자가 둘인데, 둘 다 실측으로 무영향 확인:

1. **앱**: 18개 테이블명을 앱 코드에서 전수 grep → 걸리는 파일이 `database.types.ts` 하나뿐. 이건 DB 스키마에서 자동 생성되는 타입 목록이라 "존재를 안다"일 뿐 실제로 읽고 쓰는 코드가 아니야. 즉 화면 어디도 이 테이블을 안 씀.
2. **도리토 파이프라인/스크립트**: service_role 키로 돌아. service_role 은 **RLS 를 통째로 무시하는 관리자 권한**이라, RLS 를 켜든 말든 똑같이 동작해.
*모기질문*: 그럼 레거시 18종 삭제해도 되는거야? 그냥 물어보기만 하는거야!
*답 (Claude)*: 17종은 응, 삭제 가능 — 단 두 가지 확인 후. (1) **원격 실측**: 로컬 0 row ≠ 원격 0 row 라 QA 원격에서 데이터 유무부터 (쿼리는 `legacy-tables-drop-cleanup.md` 에 넣어둠). (2) **도리토 확인**: 파이프라인이 쓰는 테이블인지. 특히 `similarity_scores`(에디터 툴 로드맵)·`shade_embeddings`(AI 파이프라인 후보)는 삭제 말고 잠금 유지가 기본값. 예외는 **`users` 하나** — 라이브 테이블들(brands·shades·cores 등)이 FK 20개로 이 테이블을 참조하고 있어서, 삭제하려면 그 컬럼들 정리가 선행이라 별도 phase 감이야. 요약: "지금은 전부 잠갔고, 17종은 실측+확인 후 삭제 PR, users 는 큰 공사."

오답 해설:
- "테이블이 비어 있어서" — 로컬은 0 row 맞는데, **로컬 0 ≠ 원격 0**. 원격엔 데이터가 있을 수 있고, 그래서 DROP(폐기)은 원격 실측 후로 미룬 거야. 비어서 안전한 게 아니라 **아무도 안 써서** 안전한 것.
- "프런트 try/catch" — 잠금 방식 자체가 "읽기는 에러 대신 0 rows"라 try/catch 필요조차 없어. 그리고 에러를 삼키는 건 안전장치가 아니라 문제를 늦게 발견하게 만드는 쪽.

---

## 3번 — 색군은 자물쇠 추가, 텍스처는 문 자체 제거. 왜 다르게?

**내 답: "어렵다 뭐지" → 다시 보기**

기준은 딱 하나야: **"우리 앱이 그 문(테이블 직접 쓰기)을 실제로 쓰냐?"**

- **텍스처** — 앱 코드 전수 확인 결과, 텍스처 저장은 100% RPC(`set_shade_texture_traits`) 경유 (`adminTextureRepo.ts:136`). 테이블에 직접 쓰는 코드가 한 줄도 없어. 아무도 안 쓰는 문 = **그냥 벽으로 막는 게 제일 안전** (직접 쓰기 정책 삭제). 막아서 불편해질 사람이 없거든.
- **색군** — 등록 시트(NewProductSheet)가 색군 저장을 **테이블에 직접** 해 (`upsertShadeColorFamilies` = 기존 매핑 DELETE 후 새로 INSERT). 이 문을 벽으로 막으면 **제품 등록할 때 색군 저장이 실패**해. 그래서 문은 두되, RPC 게이트와 똑같은 자물쇠를 담: "게시된 shade 는 어드민만, 미게시 초안은 로그인 유저 OK".
- *모기질문*: 근데 이부분 읽으면서 생각난건데, 텍스처처럼 다른 모든 테이블도 테이블 직접 쓰는 코드 없애고 RPC만 쓰도록 수정하면 안되는거야? 그냥 질문임. supabase를 잘 몰라서.. 이게 더 부적절함?
- *답 (Claude)*: 가능하고 부적절한 방향도 아니야 — 실제로 비교노트·비교평가 계열은 이미 그렇게(RPC-only) 가고 있어. 근데 "전부 RPC"로 통일하는 건 과잉이 되기 쉬워. 갈림 기준:
  - **RPC 가 맞는 경우**: 여러 테이블을 한 번에 묶어 저장하거나(노트+증거사진), 복잡한 검증·분기가 필요할 때. 실패가 반쯤 저장되면 안 되는 작업.
  - **테이블 직접 + RLS 가 맞는 경우**: "내 것만 쓰고 지운다" 수준의 단순 저장 (인벤토리, 북마크, 컬렉션). RLS 정책 한 줄이면 끝나는데 RPC 로 만들면 함수 만들고, 가드 넣고, 시그니처 바뀔 때마다 관리하는 비용이 더 큼. 그리고 RLS 는 `pg_policies` 한 방으로 전수 감사가 되는데(오늘 내가 한 것), RPC 는 함수 본문을 하나하나 읽어야 감사됨.
  - Supabase 설계 자체가 "테이블 직접 접근 + RLS 가 기본, RPC 는 특수 작업용"이라 기본 결이 그래. 그리고 지금은 출시 안정화 기간이라, 잘 돌아가는 직접 쓰기 경로를 굳이 RPC 로 옮기는 리팩토링은 위험 대비 이득이 없어.
- *모기질문*: 그리고 upsertShadeColorFamilies는 왜 벽으로 막으면 색군 저장이 실패하는거야?ㅠ
- *답 (Claude)*: 이름이 함정이야 ㅋ — `upsertShadeColorFamilies` 는 DB 함수(RPC)가 아니라 **프런트 TypeScript 함수**고, 안에서 `supabase.from('shade_color_families').delete()/.insert()` 로 **테이블에 바로** 써 (= 옆문으로 감). 옆문을 벽으로 막으면(쓰기 정책 전부 삭제) 네 로그인 토큰으로 하는 그 INSERT 가 무조건 거절(42501)돼서, 등록 시트에서 색군 칩 고르고 저장하는 순간 실패하는 거야. 비교: `upsertShadeTextures` 도 프런트 함수인데 걔는 안에서 `supabase.rpc('set_shade_texture_traits')` 를 불러 — 정문(RPC)으로 가니까 벽 세워도 안 죽는 것. 즉 "함수냐 아니냐"가 아니라 **그 함수가 안에서 정문으로 가냐 옆문으로 가냐**가 갈림길.
- *모기질문2*: 아니 설계 왜이렇게되어있지? 코드에 왜이렇게 일관성이 없지ㅠ. 그럼 upsertShadeColorFamilies도 RPC 쓰도록 바꿔야되는거 아니냐옹? 아니면지금은 출시라 대충 땜질만 하는거야?ㅠ.
- *답 (Claude)*: 일관성 없는 건 사실인데 이유는 "막 짜서"가 아니라 **지층이 달라서**야. 색군 저장은 4월(D-054 시절, "테이블 직접 + RLS"가 그때 표준) 코드고, 텍스처는 6/19 trait SSOT 대공사 때 네가 RPC 로 재설계한 새 지층. 옛 지층을 그때 같이 안 바꾼 것뿐. 그리고 **땜질 아니야** — 오늘 한 건 코드를 안 건드리고 최종 방어선을 DB(RLS)에 세운 건데, 이건 임시방편이 아니라 정석이야 (코드가 어느 지층이든 DB 가 마지막에 막는 구조). 색군을 RPC 로 옮기는 건 "해도 되는 정비"인데 지금 하면 출시 전 검증 비용만 늘고, D-109 가 어차피 출시 후 텍스처 입력을 라인 단위로 재설계할 예정이라 **그때 색군도 같이 trait 계열로 정리하는 게 한 번에 끝나** — 그 시점 후보로 미루는 게 이득.

그리고 "유저가 게시된 shade 색군을 고치려다 자물쇠에 막히면?"이 걱정될 텐데 — 그 경우는 **한 단계 앞 RPC 가 먼저 '이미 게시돼 수정할 수 없어요'로 끊어서** (NewProductSheet.tsx:561~577) 색군 저장 시도까지 아예 안 가. 그래서 유저가 새 에러를 볼 일 없음.

오답 해설:
- "텍스처가 더 민감해서" — 민감도 차이 아님. 위험이 같아도 앱이 쓰는 문이면 잠그는 방식이 달라져야 해.
- "D-073 이 색군만 다뤄서" — D-073 은 색군·텍스처 **둘 다** 다루는 결정이야 (첫 등록자 정책). 그래서 둘 다 이번 좁힘의 "D-073 보충" 대상.

---

## 4번 — 닫은 guest todo 와 이 마이그의 관계

**내 답: "RPC 경로는 닫혔고, 테이블 경로를 이 마이그가 마저 닫는다" → 정답**

타임라인으로:

1. todo 원래 내용 = "게스트가 비교노트를 쓸 수 있다" (5/19 발견)
2. 6/24 마이그가 RPC 안에 게스트 차단 가드 추가 + D-084 로 정책 lock → **RPC 문은 닫힘**. 오늘 오전 이 근거로 todo 닫음 — 닫기 자체는 유효.
3. 근데 오늘 오후 감사에서 **테이블 직접 INSERT 문이 열려 있는 걸 발견** — 게스트는커녕 세션 없는 anon 도, 심지어 남의 이름(uuid)으로도 쓸 수 있었어. RPC 가드가 무의미해지는 우회로.
4. 이 마이그(#302)가 그 마지막 문을 닫음 → 이제 "게스트가 비교노트 못 쓴다"가 **모든 경로에서** 참이 됨. 단, 원격은 db push 전까지 3번 상태 그대로.

"다시 열어야 한다" 오답 해설: todo 를 되돌릴 필요 없는 이유는, 남은 구멍이 **새 발견으로서 이번 마이그 작업에 이어졌기** 때문이야. 기록도 정정본으로 남아 있고 (todo done 파일 § 닫힘). 작업 추적 관점에서 구멍이 붕 뜬 순간이 없으면 재오픈은 불필요.

---

## 한 줄 요약

오늘 퀴즈 전체를 관통하는 문장 — **"쓰기 경로를 전부 세어보기 전엔 잠갔다고 말할 수 없다."** 어제 교훈("UI ≠ 서버")의 서버 내부 버전이야: RPC ≠ 테이블.