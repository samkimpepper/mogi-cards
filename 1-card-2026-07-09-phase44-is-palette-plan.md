# 모기 리드 카드 — Phase 44 plan 승인 (is_palette 팔레트 판정 구분자)

> 종류: PLAN Read Card (실행 전 승인). 원문 SSOT = `.planning/workstreams/milestone/phases/44-product-lines-is-palette-phase-43/` 의 44-CONTEXT + 44-01~04-PLAN.
> 작성: 2026-07-09 Claude 세션 (plan 리뷰 직후). 이 카드는 repo 에 없음 — 개인 gist 전용.

## 1. 승인용 요약

- `[READ]` phase 44 = 같은 라인 **호수 비교샷** (틴트 06호 vs 09호 팔 비교) 이 팔레트 증거로 잘못 잡혀 **카드 대표사진이 그 비교샷에 영구 고정**되는 구멍을, 라인의 팔레트 체크박스 (`product_lines.is_palette`) 로 막는다. plan 4장: 44-01 코어 (컬럼 + 서버·클라 판정 가드) → 44-02 어드민 체크박스 + 늦게 켠 라인 재판정 버튼 / 44-03 원격 오포획 정정 / 44-04 D-100 문서 보충.
- `[READ]` 방향이 안전한 이유: 체크를 깜빡한 진짜 팔레트 = 비교발색 fallback 으로 저장 (단독샷 오면 자동 교체 — 되돌릴 수 있음). 현행 구조의 실수 (단품 오포획) = 영구 고정. 실수 비용이 비대칭이라 명시 체크 쪽이 실패해도 덜 아픔.
- `[READ]` D-100 과의 관계: **번복 아님** — D-100 결정 2 원문이 "니즈가 실제로 생기면 그때 별도 결정으로 추가한다" 를 예약해 둠. 이번이 그 조항의 발동. pan_count 등 나머지는 계속 보류.
- `[READ]` **리뷰 발견 (plan 수정 후보)**: 44-02 재판정이 "fallback 에 남은 사진" 만 후보로 봐서, 모임샷이 **이미 단독샷으로 교체된 칸**은 재판정을 못 잡음. CONTEXT 가 약속한 "라인의 멀티매핑 사진 재판정" 보다 좁음 → 그 칸은 플래그 켜도 배지·팔레트 블록이 안 돌아옴.

## 2. 직접 봐야 할 불변식

- 판정은 "누가 켰나" 가 아니라 **컬럼 값만** 본다 — 나중에 유저 제안 경로가 붙어도 재판정 그대로 재사용.
- 체크 없는 같은 라인 2칸+ = 무조건 fallback (fail-closed).
- 과거 데이터 정정은 삭제 없음 — **강등만** (가역: 진짜 팔레트로 판명되면 재판정으로 재승급).
- RPC 재정의 말미 권한 두 줄 반복 — 빠지면 비로그인도 RPC 실행 가능.

## 3. 증거 `[EVIDENCE]`

- plan 의 코드·문서 인용 전부 실측 대조 통과: 기준선 마이그 (가드 73~104 · 승급 137~146 · 권한 169~174행), 클라 판정 함수 58~73행, adapter 65·216행, AdminPage 126~197행, D-100 결정 2 예약 조항 87행, 디올 slug 원문, 마이그 timestamp 순서. 발견된 실질 문제는 위 리뷰 발견 1건.

## 4. 모기가 해야 할 일 `[DECIDE]`

- [ ] **(지금)** 리뷰 발견: 44-02 재판정 후보를 멀티매핑 사진 기준으로 고치고 실행 (추천) / 그대로 실행 / 보류
- [ ] (실행 중) 44-03 체크포인트 — QA 원격 쿼리 2개 실행 + 이더앤 멀티 글림바 slug·강등 대상 확정
- [ ] (실행 중) 44-04 — D-100 supplement 승인 + 퀴즈
- [ ] (머지 후) `supabase db push` + 이더앤 라인 재판정 실행 → 배지·팔레트 블록·대표사진 UAT

## 5. 판정

- `[STOP]` 원격 실측에서 진짜/가짜 팔레트 구분이 안 되는 행 발견 → 강등 범위 재논의. RPC 권한 실측에서 anon 실행 가능 → 머지 금지.
- `[FOLLOW-UP]` (비차단·소) 44-04 퀴즈 예시 문항 축 중복 · 44-02 후보 쿼리 is_primary 필터 — 리뷰 발견 반영 시 자연 해소.

`[DETAIL]` 이번 승인에서 안 읽어도 되는 것: RPC SQL 본문, acceptance grep 게이트 목록, TableConfig 컬럼 세부, 타입 재생성 커맨드.

원문 위치:

- `44-CONTEXT.md` → 결정 전체 (Implementation Decisions)
- `44-01-PLAN.md` → must_haves.truths (핵심 보장 5줄)
- `44-03-PLAN.md` → Task 1 how-to-verify (네가 돌릴 쿼리 2개 원문)
- `.planning/threads/palette-judgment-signal/README.md` → 왜 옵션 4 인지 근거 3가지

---

<details>
<summary>Appendix B. 이해 체크 (주관식 — 답은 세션에서)</summary>

**B1. 핵심 결정 — 안전 방향** — 진짜 팔레트 라인인데 어드민이 체크를 깜빡하고 모임샷이 등록되면 그 사진은 어떤 상태로 저장되고, 왜 그 방향을 "안전" 이라고 부를까?
(→ 어디 보면: 이 카드 1절 두 번째 READ + `44-CONTEXT.md` 클라 판정 함수 절 마지막 줄)

**B2. 위험한 오해 — 가짜 리뷰 댓글 반박** — 다음 댓글에 반박해봐: *"is_palette 는 D-100 이 명시적으로 '만들지 않는다' 고 잠근 컬럼인데, 이걸 추가하는 건 ADR 번복이라 재개정 절차부터 밟아야 하는 것 아닌가요?"*
(→ 어디 보면: `docs/wiki/decisions/D-100.md` 결정 2 원문 (87행) + thread README 결정 근거 1번)

**B3. 다음 액션 — 원격 반영 순서** — 이번 phase 의 마이그 3개가 QA 원격에 실제 반영되는 시점과 주체는? 그리고 44-03 의 강등 마이그가 **작성되기 전에** 네가 먼저 해줘야 하는 일은 뭐야?
(→ 어디 보면: `44-03-PLAN.md` Task 1 (PREFLIGHT 게이트) + 각 plan 의 Post-merge note)

</details>
