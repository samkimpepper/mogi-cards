# 리드 카드 — PR #498 등록 서버 게이트 (귀속 위조 차단)

> 얇은 카드. 원문: PR #498 (base dev, head `871bbf3`) · 계약 `swatch-ops/contracts/2026-08-11-swatch-registration-gate.md`.
> 상태: READY — 4조건 실측 충족. 머지는 모기 몫. **launch gate 필수 (트친 공개 전).**

## 무엇을 막나

인증 유저가 UI를 우회해 **검증된 타인의 핸들로 발색을 등록**해 그 사람 "내 발색"에 가짜를 꽂는 위조. (#496의 자동 배정과 결합하면 즉시 남의 것이 됐음.)

## 방어 2겹

1. `create_swatch` RPC 가드 — 친절한 조기 거부.
2. **swatches BEFORE INSERT/UPDATE 트리거 — 최후 방어.** RPC를 건너뛴 직접 테이블 POST까지 봉쇄. (워커가 실측으로 찾은 우회로 — 이거 없었으면 PR 보안 효과 0이었음.)

규칙(양쪽 동일): 어드민 통과 / 그 외에는 `handle_verified` **그리고** 핸들이 본인 것일 때만.

## 네가 확인할 것 1건 — 불변식 반전

- [x] **게스트(익명 로그인) 발색 등록이 이제 막힌다.** 기존 테스트의 "게스트 등록 유지" 단언을 P0406 차단으로 뒤집었음. 네 결정 3("미검증 유저 등록 서버 차단")의 직접 귀결이라 의도된 것 — 다만 **등록 퍼널에 영향**이 있으니 알고 머지할 것. (게스트는 user_profiles 행이 없어 항상 미검증.)

## 부수 정보

- 에러 코드는 레포 #277 컨벤션대로 전용 SQLSTATE: **P0406**(미검증) / **P0407**(타인 핸들). 메시지 파싱 금지 원칙 유지, 안내 카피 2건 추가.
- 검증: registration_gate_verify ALL PASS(18) · 음성 대조 2회 · vitest 560/2(Atlas 알려진 예외) · build. SQL 스위트 1건 FAIL은 dev 기준 기존 실패(sw-rp3, 이 PR 무관 — 트리거 DISABLE 대조로 확인).

## 다음

- [ ] PR #498 머지 → prod db push
- [ ] (#496과 순서 무관 — 서로 다른 파일. 단 둘 다 마이그라 push 순서는 머지 순서대로)
