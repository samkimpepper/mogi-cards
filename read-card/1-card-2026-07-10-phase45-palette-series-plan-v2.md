# 모기 리드 카드 v2 — Phase 45 palette_series 계획 재승인

> 종류: PLAN Read Card. 원문 SSOT = `swatch-v2/.planning/workstreams/milestone/phases/45-palette-series-variant-d-115/`와 `docs/wiki/decisions/D-115.md`.
> 갱신 이유: 공통 제품명 저장 위치, 라인페이지 제목 규칙, 기각안, 실제 조회 방식, 저장 후 화면 갱신 전제가 2026-07-10 대화 뒤 달라졌다.
> 이 카드는 구현 명세가 아니라 모기가 승인할 결정과 남은 위험을 쉬운 말로 모은 개인용 카드다.

## 1. 이번 결정 한 줄

디올 백스테이지 유니버설과 로즈골드는 각각 독립 라인으로 유지하되, `palette_series` 하나로 연결한다. 두 라인페이지는 같은 공통 제품명을 제목으로 보여주고, 유니버설·로즈골드는 제목 아래 옵션 칩으로 오간다.

## 2. 이름 세 칸은 역할이 다르다

| 저장 위치 | 들어갈 값 | 쓰는 곳 |
|---|---|---|
| `palette_series.name` | 디올 백스테이지 글로우 페이스 팔레트 | 시리즈에 속한 라인페이지의 공통 제목 |
| 유니버설 `product_lines.name` | 디올 백스테이지 글로우 페이스 팔레트 - 유니버설 | 검색, 중복 방지, 어드민에서 라인 식별 |
| 로즈골드 `product_lines.name` | 디올 백스테이지 글로우 페이스 팔레트 - 로즈골드 | 검색, 중복 방지, 어드민에서 라인 식별 |
| `palette_variant_label` | 유니버설 / 로즈골드 | 화면의 옵션 칩 |

핵심은 공통 제목을 `palette_series.name`에서 한 번만 읽는다는 것이다. 그렇다고 각 `product_lines.name`을 지우지는 않는다. 화면 제목과 내부 식별 이름의 책임을 나눈다.

## 3. 실제 화면 시나리오

### 유니버설 페이지

- URL과 대표사진과 네 칸은 유니버설 것 그대로다.
- 제목은 `디올 백스테이지 글로우 페이스 팔레트`다.
- 제목 아래에는 `유니버설`과 `로즈골드` 칩이 뜬다.
- `유니버설` 칩이 현재 선택 상태다.

### 로즈골드 페이지

- 로즈골드 칩을 누르면 로즈골드의 독립 라인페이지로 이동한다.
- 제목은 유니버설 페이지와 같은 `디올 백스테이지 글로우 페이스 팔레트`다.
- URL과 대표사진과 네 칸은 로즈골드 것으로 바뀐다.
- `로즈골드` 칩이 현재 선택 상태다.

시리즈가 없는 일반 제품은 아무 변화가 없다. 기존 `product_lines.name`이 제목이고 옵션 칩도 없다.

## 4. 왜 product_lines.name을 비우지 않나

처음 떠올린 안은 시리즈 멤버의 `product_lines.name`을 NULL이나 공백으로 두고 공통 이름을 항상 `palette_series`에서 가져오는 것이었다. 공통 제목을 한 곳에서 읽자는 방향은 맞지만, 라인 이름을 비우는 부분은 기각했다.

- 현재 DB에서 `product_lines.name`은 필수값이다.
- 검색과 어드민 목록이 이 이름을 사용한다.
- 같은 브랜드·카테고리에서 공백 이름 두 개를 만들면 현재 중복 방지 규칙과 충돌한다.
- 로그나 오류 화면에서 어느 라인인지 알아보기 어려워진다.

따라서 공통 화면 제목만 시리즈 이름을 사용하고, 라인 이름은 공식 variant 전체 이름으로 보존한다.

## 5. 자동으로 이름을 조립하지 않는 이유

`palette_series.name + palette_variant_label`을 붙여서 `product_lines.name`을 자동 생성하거나 덮어쓰지도 않는다.

브랜드마다 공식 이름의 순서, 괄호, 하이픈, 옵션 표기가 다를 수 있다. 기계적으로 붙이면 그럴듯하지만 실제 공식 명칭과 다른 이름을 만들 수 있다. 어드민은 공통 이름, 기존 라인 이름, 옵션명을 같은 폼에서 보고 확인하되 프로그램은 이름을 고치거나 관계를 추론하지 않는다.

## 6. 이번에 명시적으로 하지 않기로 한 것

| 기각안 | 하지 않는 이유 |
|---|---|
| `product_lines.parent_id` 자기참조 | 유니버설과 로즈골드 중 누가 부모인지 정할 근거가 없다. 둘은 대등한 옵션이다. |
| 시리즈 멤버의 라인 이름을 NULL·공백으로 저장 | 검색·중복 방지·어드민 식별을 깨뜨린다. |
| 시리즈 이름과 옵션명으로 라인 이름 자동 생성 | 브랜드 공식 표기가 하나의 조립 규칙을 따른다는 보장이 없다. |
| 이름 앞부분이 같으면 자동으로 그룹 지정 | 이름 변경과 우연한 유사 이름에 취약하다. 잘못 묶여도 조용히 지나간다. |
| 범용 `line_series`와 `kind` 모델 | 현재 확인된 사례는 팔레트 색 옵션뿐이다. 아직 없는 관계를 미리 모델링하지 않는다. |
| 별도 `/series/...` 페이지 | 기존 라인페이지와 내용이 겹친다. |
| 유니버설 네 칸과 로즈골드 네 칸을 한 화면에 펼침 | 제품 옵션 탐색과 색 비교 역할이 섞인다. 칸 비교는 비교발색·Dupe 축이다. |

기각안은 떠올릴 수 있는 모든 아이디어를 적은 목록이 아니다. 나중에 다시 채택될 가능성이 있거나 데이터 구조를 크게 바꾸는 대안만 이유와 함께 남겼다.

## 7. 계획에서 실제로 달라진 구현 내용

### 공통 제목 데이터도 받아온다

기존 계획은 `product_lines`의 새 FK와 옵션명 컬럼이 `select('*')`로 자동 유입된다는 사실만 보고 있었다. 하지만 공통 제목은 다른 테이블의 `palette_series.name`이므로 `select('*')`만으로 오지 않는다.

수정된 계획은 기존 product_lines 요청 안에 `palette_series(name)` 관계 조회를 추가한다. 서버 요청을 한 번 더 보내는 것은 아니고, 이미 하던 한 번의 요청에서 시리즈 이름도 같이 받는다.

### 라인페이지 제목 규칙이 생겼다

라인페이지는 다음 순서로 제목을 정한다.

1. 시리즈 이름이 있으면 `palette_series.name`
2. 시리즈가 없으면 기존 `product_lines.name`
3. 둘 다 없을 때만 기존 기본 문구

DB의 `product_lines.name` 값 자체를 바꾸는 규칙이 아니라 화면에 어떤 제목을 보여줄지 정하는 규칙이다.

### 공통 제목의 공백을 DB에서 막는다

`palette_series.name`이 사용자 화면의 제목이 되었으므로 NULL뿐 아니라 공백만 입력한 값도 DB CHECK로 거부하도록 계획을 보강했다. slug도 같은 방식으로 공백을 막는다.

### 저장 직후 자동 갱신을 전제하지 않는다

기존 계획에는 어드민 저장 후 전역 캐시 무효화 이벤트로 옵션 칩이 즉시 갱신된다고 적혀 있었다. 실제 AdminTable 저장 경로는 그 이벤트를 호출하지 않는다.

현재 Phase 45는 자동 갱신을 새로 구현하지 않는다. UAT에서는 저장 뒤 라인페이지를 새로고침해 확인한다. 저장 직후 자동 반영은 별도 캐시 배선 작업이다.

## 8. 그대로 유지되는 핵심 불변식

- 시리즈 지정은 공식 상품 근거를 본 어드민이 한다. 이름만 보고 자동으로 묶지 않는다.
- 멤버는 모두 팔레트 표시가 켜져 있어야 한다.
- 한 시리즈의 멤버는 같은 브랜드와 같은 카테고리여야 한다.
- 같은 시리즈에서 옵션명은 중복될 수 없다.
- 시리즈를 지정했으면 옵션명이 필수고, 시리즈가 없으면 옵션명도 없어야 한다.
- 활성 멤버가 두 개 이상일 때만 옵션 칩을 보여준다.
- 늦게 두 번째 옵션이 생기면 기존 라인을 같은 시리즈로 연결하면 된다. 예전 판정 결과를 다시 계산하지 않는다.

## 9. 아직 해결되지 않은 STOP 세 가지

이번 이름 결정과 문서 보강으로 아래 세 문제까지 자동으로 해결된 것은 아니다. 실행 전에 계획을 더 고쳐야 한다.

### STOP 1. 일반 로그인 사용자의 우회 입력

기존 `product_lines` INSERT 정책은 로그인 사용자에게 넓게 열려 있다. 새 시리즈 컬럼도 같은 행에 들어가므로, 어드민이 아닌 로그인 사용자가 새 라인을 만들면서 시리즈 값까지 넣는 경로를 막는 규칙이 필요하다. "시리즈 연결은 어드민 전용"이 문구가 아니라 실제 권한으로 성립해야 한다.

### STOP 2. 연결 뒤 브랜드·카테고리 변경

현재 계획의 트리거 발동 컬럼은 시리즈 ID, 옵션명, 팔레트 표시뿐이다. 이미 연결된 라인의 `brand_id`, `category_id`, `is_deleted`를 나중에 바꿔 규칙을 깨는 경우도 다시 검사하도록 발동 컬럼을 보강해야 한다.

### STOP 3. 디올 backfill 대상 수량

디올 연결 마이그레이션은 대상이 0개나 1개만 잡혀도 조용히 지나갈 수 있다. 정확히 두 라인이 확인되지 않으면 실패하도록 수량 검사를 넣어야 "적용 성공처럼 보이지만 한쪽 칩이 없는 상태"를 막을 수 있다.

## 10. 모기가 승인할 것

- [x] 이름 역할 분리 승인: 공통 화면 제목은 `palette_series.name`, 라인 식별 이름은 `product_lines.name`, 옵션 칩은 `palette_variant_label`.
- [x] 라인 이름 NULL·공백과 자동 이름 조립을 하지 않는 결정 승인.
- [x] MVP에서는 어드민 저장 뒤 새로고침으로 확인하고, 자동 캐시 갱신은 후속으로 분리하는 것 승인.
- [x] 위 STOP 세 가지를 Phase 45 계획에 보강한 뒤 실행하는 순서 승인.

## 11. 실행 전 모기가 나오는 시점

1. 계획의 STOP 세 가지가 보강됐는지 다시 확인한다.
2. 디올 연결 파일을 만들기 전에 QA DB에서 유니버설·로즈골드 두 라인의 실제 slug, 팔레트 표시, 삭제 상태를 확인한다.
3. 머지 뒤 모기가 `supabase db push`를 실행한다.
4. 두 라인페이지를 새로고침해 공통 제목, 옵션 칩 두 개, 각 페이지의 독립 URL·대표사진·네 칸을 확인한다.

## 12. 원문에서 볼 위치

- `docs/wiki/decisions/D-115.md` — 이름 역할, 기각안, 제목 규칙
- `45-CONTEXT.md` — 잠긴 결정과 범위 밖
- `45-01-PLAN.md` — DB 제약, `palette_series(name)` 관계 조회, Product 공급 필드
- `45-02-PLAN.md` — 어드민 이름 도움말과 시리즈·옵션명 입력
- `45-03-PLAN.md` — 라인페이지 제목 선택 규칙, 옵션 칩, 새로고침 UAT
- `45-04-PLAN.md` — 디올 두 라인 사전 확인과 연결 마이그레이션

---

## 13. 이해 체크

답은 한두 줄이어도 된다. 용어를 그대로 옮기기보다 왜 그런지 자기 말로 적는다. 모르겠으면 추측해서 맞히기보다 `모르겠음 + 헷갈리는 지점`을 쓴다.

### A. 객관식 워밍업

**A1. 어드민에서 시리즈 연결을 저장한 직후 칩이 보이지 않는다. 현재 Phase 45 계획에서 먼저 할 일은?**

- [ ] 같은 시리즈를 다시 한 번 저장한다.
- [x] 라인페이지를 새로고침하고, 그래도 안 보이면 문제로 본다.
- [ ] `product_lines.name`을 시리즈 이름과 같게 바꾼다.
(3번 문항은! name이 달라도 안보이는 것까진 아닐걸?)

### B. 주관식 진단

**B1. 이 변경이 바꾸는 동작 / 바꾸지 않는 동작**

유니버설과 로즈골드 페이지의 제목이 이제 같아진다. 그렇다면 이번 변경이 실제로 바꾸는 것과, 여전히 각 variant별로 따로 유지되는 것을 설명해줘.

내 답:

- 바꾸는 동작: 유니버설과 로즈골드 행에 palette_series 아이디가 업데이트되고.(원랜 널.) palette_series 테이블에 새 행 생김. 여기에 진짜로 쓰이는 제품명(공통)이 저장된다옹.
- 바꾸지 않는 동작: 음.. 뭐있지;; 

**B2. 막는 것 / 못 막는 것**

계획된 DB CHECK·유니크·트리거와 STOP 보강까지 모두 들어갔다고 가정하자. 이 안전장치가 막을 수 있는 잘못된 연결 하나와, 그래도 막지 못하는 잘못된 연결 하나를 예로 들어줘. 프로그램이 이름 앞부분을 보고 자동으로 묶게 하는 것이 왜 남은 문제의 해결책이 아닌지도 덧붙여줘.

내 답:

- 막는 것: 카테고리, 브랜드 다른 제품끼리 묶는 것 불가능. 
- 못 막는 것: 와어렵다... 
- 이름 자동 묶기가 해결책이 아닌 이유: 오타일수도 있고, 잘못 입력해서 같은제품아닌데 묶일수도 있고... 

**B3. STOP / FOLLOW-UP / 정상 판정**

아래 세 상황을 각각 `STOP`, `FOLLOW-UP`, `정상` 중 하나로 판정하고 이유를 적어줘. 같은 판정을 여러 번 써도 된다.

1. 어드민이 아닌 일반 로그인 사용자가 직접 API를 호출해 새 라인에 `palette_series_id`를 넣을 수 있다.
2. 어드민 저장 직후에는 칩이 안 보이지만, 라인페이지를 새로고침하면 공통 제목과 칩 두 개가 정상적으로 보인다.
3. 디올 backfill 대상이 유니버설 한 줄만 잡혔는데 마이그레이션은 성공으로 끝난다.

내 답:

1. 판정 STOP / 이유: 지금 어드민만 되도록 계획짜는중(좀 복잡해서;; 잘못된 데이터 못들어가게하려고..)
2. 판정 정상 / 이유: 사용자 많은것도아니고 괜찮..
3. 판정 STOP / 이유: 로즈골드랑 같이해야지. 

### C. 잘못된 리뷰 댓글에 답하기

**C1. 라인 이름은 이제 중복 아닌가요?**

리뷰어:

> 두 페이지가 어차피 `palette_series.name`을 제목으로 쓸 거면 `product_lines.name`은 중복 데이터 아닌가요? 시리즈 멤버는 NULL로 두면 이름 불일치도 사라지고 더 깔끔해 보입니다.

모기 답:

> 그러면 product_lines.name 중복이 생김(옵션들 다 공백이니까). ...ㅜㅜ

**C2. select('*')면 관계 테이블 이름도 오지 않나요?**

리뷰어:

> product_lines를 이미 `select('*')`로 읽고 있으니 `palette_series.name`도 자동으로 들어올 겁니다. 관계 join을 계획에 추가한 건 불필요한 변경 아닌가요?

모기 답:

> 뭔소리여 새 테이블 추가했으니까 조인해야지 

### D. 채점 기준

<details>
<summary>답을 쓴 뒤 펼쳐보기</summary>

#### A1

- 정답: 라인페이지를 새로고침하고 확인한다.
- 다시 보기: 라인 이름을 억지로 맞추거나 같은 데이터를 반복 저장해야 한다고 판단한다.
- 자주 나올 오해: 저장 직후 자동 갱신이 이미 구현됐다고 생각하는 것.

#### B1

- 정답에 반드시 들어갈 핵심:
  - 바뀌는 것은 시리즈 멤버의 화면 제목과 옵션 이동 진입점이다.
  - 각 variant의 URL, 대표사진, 칸 묶음, `product_lines.name`은 따로 유지된다.
  - 일반 라인페이지는 기존 제목과 동작을 유지한다.
- 부분 이해:
  - 제목과 칩 변화는 설명했지만, 각 variant별로 유지되는 URL·사진·칸 또는 라인 이름을 빠뜨린다.
- 다시 보기:
  - 두 라인이 하나의 라인으로 합쳐지거나 여덟 칸이 한 페이지에 합쳐진다고 설명한다.
- 자주 나올 오해:
  - 화면 제목이 같아지는 것과 DB의 두 라인이 하나가 되는 것을 같은 일로 보는 것.

#### B2

- 정답에 반드시 들어갈 핵심:
  - 막는 예: 팔레트 표시가 꺼진 라인, 다른 브랜드·카테고리, 중복 옵션명, 시리즈와 옵션명 짝 불일치, 비어드민 입력 중 하나.
  - 못 막는 예: 같은 브랜드·같은 카테고리지만 공식적으로 전혀 다른 팔레트 두 개를 사람이 잘못 연결하는 것.
  - 이름 자동 묶기는 공식 관계를 증명하지 못하고 우연히 비슷한 이름이나 표기 변경 때문에 더 큰 오연결을 만든다.
- 부분 이해:
  - DB가 막는 구조적 실수는 설명했지만, 의미상 잘못된 연결이 남는다는 한계를 빠뜨린다.
- 다시 보기:
  - DB가 공식 상품 관계까지 완전히 판별한다고 보거나, 이름이 비슷하면 안전하게 자동 연결할 수 있다고 본다.
- 자주 나올 오해:
  - 사람이 판단한다는 말이 아무 안전장치도 없다는 뜻이라고 보는 것.

#### B3

- 정답에 반드시 들어갈 핵심:
  - 1번은 STOP. 어드민 전용이라는 권한 불변식이 실제로 깨진다.
  - 2번은 현재 승인안에서는 FOLLOW-UP 또는 알려진 MVP 정상 동작으로 분류할 수 있다. 단, 자동 갱신이 구현됐다고 주장하면 안 되고 새로고침 UAT가 필요하다.
  - 3번은 STOP. 두 라인이 정확히 연결됐다는 보장 없이 성공처럼 지나간다.
- 부분 이해:
  - 1번과 3번을 막아야 한다고는 했지만 권한 문제와 대상 수량 문제를 구분하지 못한다.
- 다시 보기:
  - 1번을 UI에서 버튼이 없으니 정상이라고 보거나, 3번을 나중에 칩만 확인하면 된다고 넘긴다.
- 자주 나올 오해:
  - 불편한 UX와 데이터·권한 불변식 위반을 모두 같은 심각도의 STOP으로 보는 것.

#### C1

- 정답에 반드시 들어갈 핵심:
  - `palette_series.name`은 공통 화면 제목이고 `product_lines.name`은 검색·중복 방지·어드민 식별용 공식 variant 전체 이름이다.
  - 현재 라인 이름은 NOT NULL이며 공백 두 행은 유니크 규칙과도 충돌한다.
  - 불일치 가능성은 화면이 시리즈 이름 하나만 읽고 어드민이 세 이름을 함께 확인하는 방식으로 관리한다.
- 부분 이해:
  - 검색에 필요하다고만 답하고 중복 방지·식별 또는 역할 분리를 설명하지 못한다.
- 다시 보기:
  - 화면에서 안 보이면 DB에서도 없애도 된다고 본다.
- 자주 나올 오해:
  - 같은 문자열 일부를 저장하면 언제나 잘못된 중복 데이터라고 보는 것.

#### C2

- 정답에 반드시 들어갈 핵심:
  - `select('*')`은 product_lines 자체 컬럼만 가져온다.
  - `palette_series.name`은 관계 테이블 값이므로 `palette_series(name)` 같은 관계 조회를 명시해야 한다.
  - 같은 product_lines 요청 안에 포함하므로 네트워크 요청 횟수는 늘지 않는다.
- 부분 이해:
  - join이 필요하다고 했지만 왜 필요한지 또는 요청 횟수와의 차이를 설명하지 못한다.
- 다시 보기:
  - FK 값이 있으면 관계 행의 모든 컬럼이 자동으로 따라온다고 본다.
- 자주 나올 오해:
  - SQL 관계 조회를 추가하는 것과 별도의 서버 요청을 하나 더 보내는 것을 같은 일로 보는 것.

</details>

### E. 채점 요청문

답을 모두 쓴 뒤 에이전트에게 이렇게 요청한다.

> 이 카드의 이해 체크 답안을 `정답 / 부분 이해 / 다시 보기`로 채점해줘. 각 문항마다 맞은 부분, 빠진 핵심, 위험한 오해, STOP이나 후속으로 새로 분리할 것이 있는지를 적어줘. 표현을 칭찬하기보다 실제 승인 판단을 틀릴 위험부터 봐줘.

### F. 출제 점검

이 문제들은 이름을 외웠는지만 묻지 않는다. 화면 제목과 DB 식별 이름의 경계, DB가 막는 구조적 오류와 사람이 판단할 의미적 오류의 차이, STOP과 UX 후속의 차이를 자기 말로 설명해야 풀 수 있게 구성했다.

(여기서부턴 모기 질문ㅋ)
### 2. 이름 세칸 역할 질문

- 팔레트인 제품명에서 옵션명을 어떻게 파싱할 계획이냐옹? 아 palette_variant_label이구나 그게. 그러면 프론트에서.. 이건 엄청 구체적인 구현 내용이긴 한데, DB에서 프론트에게 전달하는 데이터 클래스?(미안 내가 자바개발자라..)에다가 새로운 컬럼 추가할건지(palette_variant_label), 아니면 그냥 DB쪽에서 가공을 다 해서(뭐 분기로직같은거 다 쑤셔넣어서 최종제품명, 쉐이드명만 반환한다던가 그런거. 뭔말알?ㅠ) 그렇게할건지가 궁금하다옹..
- 근데 아. 아니 팔레트 자매라인 만들려고 이렇게 복잡하게 해야한다고? 약간 현타옴… 그냥 애초에 처음부터 팔레트인 애들 어떻게 할지 생각하고 설계할걸 그랬나.. ㅠㅠ shade 하위에 테이블 또 추가할걸 그랬나 그게 나았을까..

### 3. 실제 화면 시나리오 질문

- 이거 페이지라는게 상세시트 페이지 말하는거야? 근데 제목 아래에 옵션들(유니버설, 로즈골드) 뜨는거.. 흠. 일반 상품들은 안 그러잖아. 통일시키는게 낫지 않나. 둘다 옵션 뜨게하거나 안뜨게하거나..
- 왜 시리즈 없는 일반제품은 옵션칩을 없게 하는게 괜찮다고 생각했어? (시비거는거아님. 정당한 이유있다면 납득하고싶어나도)

### 9. 불변식 질문

- 한 시리즈의 멤버는 같은 브랜드와 같은 카테고리여야 한다. <이거 문제없겠지? 추후에? 괜찮지? 맞아맞아. 괜한걱정임. 아이라이너랑 립스틱이 같은 제품라인일리가 없잖아
- 늦게ㅔ 두번째 옵션 생겨도 문제없지? 맞아맞아. 그냥 palette_series를 추가하는 것일 뿐이니까.

### STOP 2. 질문

- 여기 내용 이해를 못함. 이미 연결된 라인의… 이걸 나중에 바꿔 규칙을 깬다고? 무슨소리지?
## 14. 이해 체크 채점 (2026-07-10 Codex)

전체 판정: **부분 이해**

핵심 방향은 맞았고 반대로 이해한 문항은 없다. 특히 자동 이름 묶기의 위험, 비어드민 입력과 불완전 backfill이 STOP이라는 점은 정확히 잡았다. 다만 화면 제목과 DB 식별 이름의 역할 경계, DB가 막지 못하는 의미상 오연결, STOP과 알려진 MVP 제약을 나누는 이유가 답에서 빠졌다.

### A1

판정: **정답**

맞은 부분:

- 저장을 반복하거나 라인 이름을 바꾸지 않고 먼저 새로고침한다는 현재 계획을 정확히 골랐다.
- product_lines.name이 달라도 칩 노출 자체가 막히는 것은 아니라는 덧붙임도 맞다.

빠진 핵심:

- 없음.

위험한 오해:

- 없음.

다음 카드 [READ] 후보:

- 현재 Phase 45는 저장 직후 자동 갱신을 구현하지 않는다.

후속:

- 자동 갱신은 비차단 FOLLOW-UP.

### B1

판정: **부분 이해**

맞은 부분:

- palette_series 행이 생기고 두 product_lines 행의 palette_series_id가 NULL에서 실제 시리즈 ID로 바뀐다는 DB 변화는 맞게 설명했다.
- 공통 공식 제품명이 palette_series에 저장된다는 점도 맞다.

빠진 핵심:

- 질문의 중심은 DB 행보다 사용자 화면에서 바뀌는 동작과 유지되는 동작이었다.
- 바뀌는 것은 두 페이지가 같은 공통 제목을 사용하고 옵션 칩으로 오갈 수 있게 되는 것이다.
- 바뀌지 않는 것은 각 variant의 독립 URL, 대표사진, 네 칸 묶음, product_lines.name이다.
- 시리즈가 없는 일반 라인페이지는 기존 제목과 동작을 유지한다.

위험한 오해:

- 현재 답만으로는 두 라인이 하나로 합쳐지는지, DB 연결만 생기는지 구분이 드러나지 않는다. 다만 실제로 합쳐진다고 답하지는 않았으므로 다시 보기 수준은 아니다.

다음 카드 [READ] 후보:

- 화면 제목의 통합과 DB 라인의 통합은 다른 일이다.

후속:

- 없음.

### B2

판정: **부분 이해**

맞은 부분:

- 다른 브랜드나 카테고리의 제품을 한 시리즈로 묶는 구조적 오류를 DB가 막는다는 답은 정확하다.
- 오타와 잘못된 입력 때문에 이름 자동 묶기가 오연결을 만들 수 있다는 이유도 정확하다.

빠진 핵심:

- DB가 못 막는 대표 사례는 같은 브랜드와 같은 카테고리지만 공식적으로 전혀 다른 팔레트 두 개를 어드민이 잘못 연결하는 경우다.
- DB는 브랜드·카테고리·옵션명 같은 구조를 검사할 수 있지만, 브랜드 공식 상품 관계의 의미까지 판별하지는 못한다.

위험한 오해:

- 없음. 못 막는 것만 비어 있었고, DB가 모든 관계를 판별한다고 주장하지는 않았다.

다음 카드 [READ] 후보:

- DB는 구조적 오류를 막고, 공식 상품 관계의 의미 판단은 사람이 한다.

후속:

- 어드민 화면에서 세 이름을 함께 보여주는 확인 UX 유지.

### B3

판정: **부분 이해**

#### 1번

판정: **정답**

- 비어드민 사용자가 직접 API로 시리즈 값을 넣을 수 있으면 어드민 전용 권한 불변식이 깨지므로 STOP이 맞다.
- 더 정확한 이유는 “복잡해서”가 아니라 UI를 거치지 않는 직접 API 호출도 DB 권한에서 막아야 하기 때문이다.

#### 2번

판정: **부분 이해**

- 현재 승인안에서 정상 또는 비차단 FOLLOW-UP으로 볼 수 있다는 판정은 맞다.
- 다만 사용자가 적어서 괜찮은 것이 아니다. 저장 직후 자동 갱신을 이번 범위에서 제외했고, 새로고침 UAT로 확인하기로 명시했기 때문에 허용되는 것이다.
- 사용자가 많아져도 새로고침하면 정확한 데이터가 보인다는 사실은 같고, 불편의 우선순위만 높아질 수 있다.

#### 3번

판정: **정답**

- 유니버설과 로즈골드 두 줄이 모두 잡혀야 한다는 방향은 맞다.
- 더 정확하게는 대상이 정확히 두 줄이 아니면 마이그레이션이 실패해야 성공처럼 보이는 반쪽 연결을 막을 수 있다.

위험한 오해:

- UX 불편을 허용하는 근거를 사용자 수로 잡으면, 데이터 정확성과 제품 범위 결정을 섞을 수 있다.

다음 카드 [READ] 후보:

- STOP은 권한·데이터 불변식 위반이고, 새로고침 필요는 현재 범위에서 명시한 UX 제약이다.

후속:

- STOP 1과 STOP 3은 실행 전 계획 보강.
- 자동 갱신은 FOLLOW-UP.

### C1

판정: **부분 이해**

맞은 부분:

- 여러 시리즈 멤버의 라인 이름을 전부 공백으로 만들면 같은 공백 이름끼리 중복 충돌이 생긴다는 핵심 하나를 정확히 잡았다.

빠진 핵심:

- product_lines.name은 현재 NOT NULL이다.
- 검색, 중복 방지, 어드민 목록에서 각 variant를 식별하는 역할도 계속 필요하다.
- palette_series.name은 공통 화면 제목이고 product_lines.name은 공식 variant 전체 이름이므로 역할이 다르다.

위험한 오해:

- 없음. 다만 “중복된다”가 문자열이 반복 저장된다는 뜻인지 유니크 충돌이라는 뜻인지 모호하므로 다음에는 “공백 이름끼리 유니크 충돌”이라고 쓰면 정확하다.

다음 카드 [READ] 후보:

- 같은 단어가 일부 겹쳐도 책임이 다르면 무조건 제거할 중복 데이터는 아니다.

후속:

- 없음.

### C2

판정: **부분 이해**

맞은 부분:

- 새 관계 테이블의 name을 가져오려면 관계 조회가 필요하다는 핵심 판단은 맞다.

빠진 핵심:

- select('*')은 product_lines 자체 컬럼만 가져온다.
- palette_series_id라는 FK 값이 있어도 palette_series 행의 name이 자동으로 따라오지는 않는다.
- palette_series(name)을 기존 요청에 포함하므로 관계 조회는 추가되지만 네트워크 요청 횟수는 늘지 않는다.

위험한 오해:

- 없음.

다음 카드 [READ] 후보:

- FK 값을 받는 것과 관계 행의 컬럼을 함께 받는 것은 다른 일이다.

후속:

- 없음.

## 총평

잘 잡은 것:

- 이름 자동 묶기가 오타와 오연결에 취약하다는 점.
- 비어드민 입력과 한 줄짜리 backfill이 STOP이라는 점.
- 공통 제품명은 palette_series에 저장한다는 데이터 방향.
- 관계 테이블 값을 쓰려면 join이 필요하다는 직감.

다시 볼 것:

- DB에서 바뀌는 행과 사용자 화면에서 바뀌는 동작을 분리해서 설명하기.
- DB가 막는 구조적 오류와 사람이 판단해야 하는 의미상 오류를 분리하기.
- 정상 판정의 이유를 사용자 수가 아니라 명시된 범위와 불변식으로 설명하기.
- product_lines.name의 검색·식별 책임까지 기억하기.

이번 답변에서 발견한 후속:

- 새 STOP은 없다. 카드에 적힌 STOP 세 가지를 계획에 보강해야 한다.
- 일반 제품과 시리즈 제품의 옵션 UI 통일성 걱정은 브라우저 UAT에서 확인할 제품 UX FOLLOW-UP이다.
- 실제 화면이 혼란스럽다는 증거가 나오면 별도 제품 thread 후보로 승격한다.

다음 승인 메모에 넣을 한 줄:

> 공통 제목은 합치지만 두 variant 라인의 URL·사진·칸·식별 이름은 합치지 않는다. DB는 구조적 오연결만 막고 공식 상품 관계의 의미 판단은 어드민에게 남는다.

## 15. 모기 질문 답

### 프론트에는 어떤 데이터 모양으로 전달하나

제품명에서 옵션명을 파싱하지 않는다. palette_variant_label은 DB에 별도 컬럼으로 저장한다.

자바식으로 비유하면, Supabase에서 읽은 행을 프론트의 Product DTO 비슷한 객체로 옮길 때 다음 필드를 추가하는 방식이다.

- linePaletteSeriesId
- linePaletteSeriesName
- linePaletteVariantLabel

DB가 최종 화면 제목을 분기로 계산해 반환하는 RPC를 새로 만드는 구조도 아니다. 기존 product_lines 조회에 palette_series(name) 관계 조회를 포함하고, 프론트 adapter가 위 세 값을 Product 객체에 담는다. 화면에서는 linePaletteSeriesName이 있으면 그것을 제목으로 쓰고, 없으면 기존 product_lines.name에 해당하는 formula를 쓴다.

즉, “이름 문자열에서 유니버설을 잘라내기”가 아니라 “공통 이름과 옵션명을 처음부터 다른 칸으로 전달하기”다.

### 자매 라인 하나 때문에 구조가 너무 복잡한 것 아닌가

복잡해 보이는 이유는 유니버설과 로즈골드가 각각 URL, 대표사진, 네 칸 묶음을 가진 독립 라인이면서 동시에 같은 공식 제품의 옵션이라는 두 사실을 모두 보존하기 때문이다.

shade 아래에 별도 옵션 테이블을 뒀다면 유니버설과 로즈골드 각각이 여러 shade를 가진다는 구조를 표현하기 어렵다. 옵션 하나가 shade 하나가 아니라 네 칸을 가진 팔레트 한 통이기 때문이다. 현재 구조에서는 product_line이 팔레트 한 통, shade가 그 안의 칸, palette_series가 같은 제품의 여러 통을 잇는 관계가 된다.

처음 설계가 완전히 잘못돼서 생긴 보수라기보다, D-100에서 “팔레트 한 통과 그 칸”을 먼저 정확히 모델링한 뒤 실제로 발견된 자매 옵션 관계를 최소 테이블 하나로 보강하는 과정에 가깝다.

### 여기서 페이지는 상세시트인가

아니다. 여기서 말하는 페이지는 /line/$lineId 라인페이지다. 유니버설 팔레트 한 통의 네 칸을 모아 보여주는 화면이다. 개별 shade를 눌렀을 때 열리는 상세시트와는 다르다.

옵션 칩에서 로즈골드를 누르면 로즈골드 상세시트 하나가 열리는 것이 아니라, 로즈골드 네 칸을 보여주는 로즈골드 라인페이지로 이동한다.

### 일반 제품에는 왜 옵션 칩이 없어도 되나

시리즈가 없는 일반 제품에는 이동할 형제 옵션이 없다. 선택지가 하나뿐인데 자기 자신을 가리키는 칩 하나를 보여주면 할 수 있는 행동은 없고 화면 요소만 늘어난다.

통일성은 모든 제품에 같은 칩을 강제로 표시하는 것보다, 실제 선택지가 있을 때 같은 위치와 같은 모양의 선택 UI를 쓰는 방식으로 맞출 수 있다. 유니버설·로즈골드처럼 실제 형제 옵션이 두 개 이상인 제품만 칩이 생기는 것은 점진적 노출에 가깝다.

다만 “칩을 누르면 상세시트가 열릴 것처럼 보이는가”와 “일반 라인페이지와 너무 다른가”는 데이터 모델이 정답을 정할 문제가 아니다. 출시 전 브라우저 UAT에서 칩의 위치, 이름, 이동 결과를 직접 보고 판단할 제품 UX 항목이다. 실제로 헷갈리면 칩 문구나 위치를 조정하고, 구조 변경이 필요할 정도면 별도 thread로 올린다.

### 같은 브랜드·카테고리 불변식은 나중에도 괜찮나

현재 확인된 팔레트 색 옵션 관계에는 맞는 불변식이다. 같은 공식 팔레트의 유니버설과 로즈골드가 서로 다른 브랜드나 전혀 다른 제품 카테고리일 이유는 없다.

다만 이 조건은 필요조건이지 충분조건은 아니다. 같은 디올이고 같은 팔레트 카테고리라고 해서 아무 두 제품이나 같은 시리즈인 것은 아니다. 그래서 공식 상품 관계 확인이 추가로 필요하다.

나중에 정말로 하나의 공식 옵션 시리즈가 여러 카테고리를 가로지르는 실증 사례가 나오면 그때 새 근거로 불변식을 다시 논의하면 된다. 아직 없는 사례를 위해 지금 조건을 느슨하게 만들 필요는 없다.

### 두 번째 옵션이 늦게 생겨도 괜찮나

맞다. 처음에는 유니버설을 일반 라인처럼 두고, 로즈골드가 나온 뒤 palette_series 행을 만들고 두 라인을 연결하면 된다. 과거 판정 결과를 다시 계산할 필요는 없다.

현재 계획에서는 저장 직후 자동 갱신이 없으므로 연결 뒤 라인페이지를 새로고침해야 한다. 또 두 라인이 정확히 연결됐는지 backfill 수량 검사가 필요하다. 이 두 조건을 지키면 늦은 추가 자체는 문제없다.

### STOP 2는 어떤 상황인가

처음 연결할 때는 유니버설과 로즈골드가 둘 다 디올이고 같은 팔레트 카테고리인지 트리거가 검사한다.

문제는 연결이 끝난 뒤다. 예를 들어 로즈골드 행을 나중에 편집하면서 brand_id를 다른 브랜드로 바꾸거나 category_id를 아이섀도 카테고리로 바꿀 수 있다. 시리즈 연결 값은 그대로라면 이제 한 시리즈 안에 서로 다른 브랜드나 카테고리가 섞였지만, 현재 계획의 트리거는 palette_series_id, palette_variant_label, is_palette가 바뀔 때만 다시 실행된다. brand_id나 category_id만 바뀌면 검사가 재실행되지 않는다.

따라서 트리거 발동 컬럼에 brand_id, category_id, is_deleted도 포함해야 한다. 그러면 이미 연결된 행의 소속 정보를 나중에 바꾸더라도 같은 시리즈 규칙을 다시 검사해 잘못된 변경을 거부할 수 있다.

---

## 16. (2026-07-10 저녁 추가) STOP 3개 계획 반영 완료 — 커밋 전 퀴즈용

> §9 에서 "실행 전에 계획을 더 고쳐야 한다" 던 STOP 세 가지가 오늘 전부 계획서에 들어갔다.
> STOP 2 (연결 뒤 브랜드·카테고리 변경) 는 이전에 이미 45-01 에 반영됐고, 오늘은 나머지 둘 —
> STOP 1 (비어드민 우회 입력) 과 STOP 3 (backfill 수량) — 을 반영했다.
> 커밋 전 퀴즈 3개는 아래 16-1 / 16-2 / 16-3 만 읽으면 풀 수 있다.

### 16-1. STOP 1 을 어떻게 막았나 — DB 의 두 검사원 (퀴즈 Q1)

DB 에는 성격이 다른 검사원이 둘 있다.

- **검사원 A = 트리거.** 저장되는 **내용**이 규칙에 맞는지 본다. 예: 팔레트 표시가 꺼진 라인을 시리즈에 넣으면 거부, 다른 브랜드끼리 묶으면 거부. 단, **누가** 쓰는지는 안 본다.
- **검사원 B = RLS 정책.** 이 동작을 하는 **사람**이 해도 되는 사람인지 본다.

문제는 `product_lines` 에 **새 행을 추가하는 문** (INSERT 정책) 이 지금 "로그인만 하면 통과" 로 열려 있다는 것. 새 컬럼 `product_lines.palette_series_id` (이 라인이 어느 시리즈 소속인지) 와 `product_lines.palette_variant_label` (칩에 쓸 옵션명) 이 생기면, 일반 로그인 사용자가 새 라인을 등록하면서 이 두 값을 같이 지정할 수 있다. 내용만 규칙에 맞으면 검사원 A 는 통과시킨다 — A 는 내용만 보니까. 그래서 "시리즈 연결은 어드민 전용" 이라는 약속이 문구로만 남고 실제로는 뚫린다.

고친 것: 그 문의 통과 조건을 교체했다 — **"시리즈 칸 2개가 둘 다 비어 있으면 (일반 등록) 통과, 값을 지정하려면 어드민만"**. 일반 사용자의 평범한 제품 등록은 원래 그 두 칸을 안 쓰므로 아무 영향 없이 그대로 된다.

확인 실험 3개 (로컬 DB, 계획서에 명시):

1. 일반 사용자가 평범한 라인 등록 → 성공
2. 일반 사용자가 시리즈 값을 지정한 등록 → 거부
3. 어드민이 시리즈 값을 지정한 등록 → 성공

### 16-2. STOP 3 을 어떻게 막았나 — 반쪽 연결이면 전체 취소 (퀴즈 Q2)

backfill = 새 구조를 만든 뒤, 기존 디올 유니버설·로즈골드 두 행을 손으로 시리즈에 연결해 주는 마이그레이션.

문제: "이 두 라인을 연결해라" 라는 UPDATE 는 대상을 못 찾아도 에러가 안 난다. 0줄이나 1줄만 바꾸고 조용히 성공 처리된다. 예를 들어 네가 push 하기 전에 어드민에서 라인 이름표 (slug) 가 바뀌어 있으면, 유니버설만 연결되고 로즈골드는 빠진 채 "성공" 으로 남는다 — 나중에 화면 보고서야 칩 한쪽이 없다는 걸 알게 된다.

고친 것: UPDATE 직후 **"방금 몇 줄을 바꿨는지" 를 세서, 정확히 2가 아니면 그 자리에서 에러를 내고 마이그레이션 전체를 취소 (rollback)** 한다. 반쪽 상태가 DB 에 안 남고, 실패가 그 자리에서 눈에 보인다.

확인 실험 (로컬 DB): 0줄 (둘 다 못 찾음) → 에러, 1줄 (하나만 찾음) → 에러, 2줄 (정상) → 통과. 같은 마이그를 두 번 돌려도 같은 두 줄이 다시 세어져 2줄이므로 재실행도 안전하다.

### 16-3. todo 를 아직 안 닫은 이유 (퀴즈 Q3)

이 STOP 들을 추적하는 todo (`phase45-plan-review-stop-guards`) 의 완료 조건에는 계획서 반영만이 아니라 **"실제 마이그레이션을 만들어 로컬 DB 에 적용하고 위 실험들이 통과"** 가 들어 있다. 지금은 계획서에 적은 단계까지고, 실험 통과는 실행 단계의 몫이다. 그래서 계획 반영 항목 3개만 체크하고 todo 는 pending 에 남겼다. 실행이 끝나 실험이 통과하면 그때 done 으로 옮긴다.

### 16-4. 다음 순서

1. 오늘 문서 델타 커밋 (계획서 4개 + todo 체크 — 문서만, 코드 없음)
2. feat 브랜치를 새로 따서 45-01 부터 실행 (로컬 Docker 필요)
3. 45-04 중간에 모기 차례: QA 원격에서 디올 2줄 확인 쿼리 (§11 의 2번)
4. 실행 실험 통과하면 todo 닫기 → 머지 후 모기가 `supabase db push` (§11 의 3번)

이 카드는 repo 밖 개인 폴더에만 있다. repo 안 파일·ignore 설정은 아무것도 안 건드렸다.

