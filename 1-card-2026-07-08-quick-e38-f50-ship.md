# 모기 리드 리뷰 — PR #332 보관함 메모 검색 · PR #333 내 후기 pin (quick 2건 ship 라운드)

> 원문: `.planning/quick/260708-e38-inventories-note/` (PLAN·SUMMARY) · `.planning/quick/260708-f50-my-review-pin/` (PLAN·SUMMARY) · PR #332 (머지됨) · PR #333

## 핸들 용어 미니 정리

| 말 | 뜻 |
|---|---|
| 핸들 클레임 | 유저가 자기 프로필에 트위터 핸들을 적어둔 것 — 아직 아무도 검증 안 함 |
| 어드민 verify (D-063) | 어드민이 "이 유저 = 이 트위터 저자 맞다"를 확인해주는 게이트 — 공개 귀속용 |
| self-view | 본인 화면에서만 보이는 표시 — 남의 화면은 안 바뀜 |

## 1. 승인용 요약

- `[READ]` **#332 (머지됨)**: 보관함 검색이 제품명·태그에 더해 **내가 적은 메모(`inventories.note`)** 로도 카드를 찾고, 걸린 단어를 형광펜 표시. 보유·기록 탭만 — 위시 탭은 카드에 메모가 안 보여서 매칭시키면 "왜 걸렸는지 안 보이는" 노이즈라 제외.
- `[READ]` **#333**: 상세시트 후기 목록에서 내 후기를 맨 위로 pin + "내 후기" 배지. **인기순(기본값)에서만** — 최신 정렬은 네 QA 실측 결정대로 순수 시간순 유지.
- `[READ]` **숨은 변경 (#333)**: pin 작업 중 발견한 **죽은 배선 복구** — "내 후기 포함" 체크박스가 원래 핸들을 못 받아서 무늬만 있었는데, 이번 배선으로 처음 실제 동작함. 체크 해제하면 이제 진짜로 내 후기가 빠진다.

## 2. 직접 봐야 할 불변식

- pin 매칭은 **핸들 클레임만 보고 어드민 verify 는 안 본다** — self-view 전용이라 남의 화면 귀속이 안 변하기 때문. 이 함수(`isMySwatch`)를 공개 귀속·집계·쓰기에 재사용 금지 (코드 주석으로 박아둠, D-063 supplement 프레임).
- 두 PR 다 **DB·서버·권한 변경 없음** — 순수 클라이언트 표시 변경. 마이그·RLS 영향 0.
- 메모 강조 렌더는 HTML 주입 경로 없음 (React 텍스트 노드만) — 메모에 뭘 적어도 스크립트 실행 안 됨.

## 3. 증거 `[EVIDENCE]`

- #332: highlightQuery 단위 테스트 6/6 + `pnpm build` 통과. 화면 human check 는 미실행 (모기 몫).
- #333: reviewPin 6/6 + tsc 0 + 전체 vitest 45/45 (회귀 없음) + QA 라이브 실측으로 "최신 정렬 pin 어색" 잡아서 `045574d` 로 인기순 한정.
- 코드리뷰 (Claude, 2026-07-08): 둘 다 막는 버그 없음. STATE.md 충돌 예고 → 실제 발생 → 두 merge 커밋으로 해소 (e38 `e6b059b`, f50 `3debdab`).

## 4. 모기가 해야 할 일 `[DECIDE]`

- [ ] **#333 머지** — Vercel check fail 은 코드 문제 아님 (samkimpepper 가 Vercel 팀에 없어서 preview 배포만 못 만드는 것, #332 와 같은 조건)
- [ ] 머지 후 화면 확인 2가지: 보관함 보유 탭에서 메모 문구로 검색 → 카드 걸리고 강조 / 상세시트 인기순에서 내 후기 맨 위 + 배지

## 5. 판정

- `[STOP]` 없음.
- `[FOLLOW-UP]` `getMyProfile()` 호출에 실패 처리(catch) 없음 — ExplorePage 기존 패턴과 동일해서 이 PR 탓 아님, 나중에 공통 정리 후보. / Vercel 팀 초대 여부는 Dorito 와 별도 논의.

`[DETAIL]` 안 읽어도 되는 것: splitByQuery 내부 루프, 안정 partition 구현, i18n 키 이름, line-clamp 클래스 조작.

---

<details>
<summary>Appendix B. 이해 체크 (주관식 — 답은 세션에서)</summary>

**A. 워밍업 (객관식 1)** — 이 두 PR 머지 후에 `supabase db push` 를 해야 할까, 안 해도 될까?
(→ 어디 보면: 이 카드 §2 두 번째 줄)

**B1. 막는 것 / 못 막는 것** — 누가 자기 프로필에 **남의 트위터 핸들**을 적어놨다고 치자. 이 상태에서 #333 의 pin 때문에 생기는 일과, 그래도 안 생기는 일을 하나씩:
- 생기는 일:
- 안 생기는 일:
(→ 어디 보면: 이 카드 §2 첫 줄 + `reviewPin.ts` 상단 주석)

**B2. 바꾸는 동작 / 안 바꾸는 동작** — #333 의 "죽은 배선 복구"가 실제로 바꾸는 사용자 동작 하나와, 안 바꾸는 동작 하나:
- 바꾸는 동작:
- 안 바꾸는 동작:
(→ 어디 보면: 이 카드 §1 숨은 변경 줄)

**B3. STOP / FOLLOW-UP / 정상** — #333 의 CI 에서 Vercel check 가 빨간불이다. 판정과 이유:
(→ 어디 보면: 이 카드 §4 첫 체크박스)

**C. 잘못된 리뷰 댓글에 답하기** — 반박해봐:
> "내 후기 pin 이 handle_verified 를 안 본다는 건 D-063 위반 아닌가요? 어드민이 인증해주기 전에는 핸들 매칭 자체를 막아야죠. 미검증 유저한테 '내 후기' 배지가 뜨는 것도 사칭 위험으로 보입니다."

모기 답:
>

<details>
<summary>채점 기준 (답 쓰기 전엔 열지 말기)</summary>

- A 기대: 안 해도 됨 — 두 PR 다 DB 변경(마이그) 없음, 순수 클라이언트.
- B1 기대: 생기는 일 = **그 사람 자기 화면에서만** 남의 후기가 맨 위로 오고 "내 후기" 배지가 붙음. 안 생기는 일 = 남들 화면 변화·공개 귀속·집계·데이터 쓰기 (self-view 밖으로 안 나감). "다시 보기" = 남의 화면에도 배지가 보인다고 답하는 경우.
- B2 기대: 바꾸는 동작 = "내 후기 포함" 체크 해제가 이제 실제로 내 후기를 목록에서 뺌 (전엔 무동작). 안 바꾸는 동작 = 후기 데이터·저장·남의 화면 (표시 로직만 살아남).
- B3 기대: 정상 (머지 가능) — 코드 실패가 아니라 Vercel 팀 권한 문제로 preview 배포를 못 만드는 것. #332 도 같은 조건으로 머지됨. FOLLOW-UP 으로 Vercel 초대 논의는 가능.
- C 기대 핵심: D-063 의 verify 게이트는 **공개 귀속**(남에게 보이는 저자↔유저 연결) 보호용이고, pin·배지는 **본인 화면 한정 표시**라 게이트 대상이 아님. 사칭 최악 케이스 = 자기 화면에서만 남의 후기에 배지가 보이는 것 — 피해자에게도 제3자에게도 아무것도 노출 안 됨. 단 이 매칭 함수를 공개 노출·집계·쓰기에 재사용하면 그때는 진짜 위반 — 그래서 코드에 재사용 금지 주석을 박음. "부분 이해" = self-view 라 괜찮다고만 하고 재사용 금지 경계를 누락.

</details>

</details>
