# 카드 — Memory.md 코어/팩 분리 (PR #472, chore/agent-memory-split → dev)

작성: 냐옹이 (swatch 마스터), 2026-08-06. 원문 SSOT: PR #472 + swatch-ops SPEC.

## 1. 반드시 읽을 결정

- `.agent/Memory.md`(213줄, 매 세션 통째 로드)가 **코어 59줄 + 도메인 팩 12개**(`.agent/memory/`)로 쪼개졌다. 룰은 하나도 안 지워지고 자리만 옮겼다.
- 이제 세션은 코어만 항상 읽고, DB·리뷰·git 같은 도메인 룰은 **그 작업을 할 때만** 해당 팩을 읽는다. 코어가 60줄을 넘으면 `verify-agent-ssot.sh`가 FAIL을 낸다.

## 2. 네가 결정할 것

- **머지 승인 여부.** 그리고 하나 더: CodeRabbit 자동 리뷰는 이 repo 설정상 dev-base PR에서 안 돌아서, "리뷰 스레드 0"은 봇이 통과시킨 게 아니라 **봇이 안 돈 것**이다. 수동 트리거(Actions 분량 소모)를 걸지, 문서 이동 PR이니 그냥 머지할지는 네 선택.

## 3. 위험한 가정

- "팩을 필요할 때 읽는다"는 1단계에선 **약속(컨벤션)일 뿐**이다. 코어의 라우팅 표가 안내하지만, 에이전트가 안 읽고 넘어가는 걸 기계로 막는 건 2단계 hook부터다.

## 4. 증명된 증거 (마스터 독립 재측정 포함)

- 룰 카운트 분리 전 77 = 분리 후 77 (코어 14 + 팩 63) — 워커 보고와 별개로 내가 재측정.
- 코어 59줄 실측. 예산 가드는 62줄 FAIL / 59줄 PASS로 실차단 재현됨.
- origin/dev 충돌 0, 체크 4개 전부 결론 있음, 미해결 스레드 0.

## 5. 안 읽어도 되는 세부

- 팩 내부 룰 본문(글자 그대로 이동, 정렬 diff 0), 링크 깊이 수정 11줄, `memory-status` 커맨드·harness thread 문서의 참조 수리 2건.
