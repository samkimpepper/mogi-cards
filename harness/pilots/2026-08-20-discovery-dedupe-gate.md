---
reviewed: false
status: completed
prospective_runs: 3
completed_runs: 3
outcome: lightweight-rule-promoted
---

# 발견물 중복 확인 관문 파일럿

## 상태와 한 줄 목적

이 파일럿의 prospective 실행은 3건으로 종료됐다. 2026-08-27 모기와 세 기록을 다시
보고, 전체 검색 절차가 아니라 아래 결과만 `guide-tutor-preamble.md` 절대 규칙 2에
경량 영구 승격했다.

- 새 메모 직전에는 현재 마스터 메모와 열린 운영 트래커만 짧게 확인한다.
- 같은 원인·같은 영향은 중복 작성하지 않고, 같은 원인·새 영향은 기존 이슈 delta로
  작성한다.
- git 이력이나 과거 세션 원문은 현재 두 곳만으로 동일성을 가릴 수 없거나 모기가 과거
  전달을 기억할 때만 확인한다. 전수 검색은 기본 관문이 아니다.

아래 본문은 **앞으로 자연 발생하는 발견물 후보 3건**에 적용했던 임시 파일럿의 원문과
실행 이력이다.

과외 중 같은 미결 원인이 다른 코드에서 다시 나타나는 것은 막지 않는다. 새 마스터 전달 메모를 쓰기 직전에 기존 열린 항목과 연결해, `동일 영향의 중복 전달`과 `기존 원인이 만든 새로운 영향`을 구분하는 것이 목적이다.

## baseline — 실행 횟수에 포함하지 않음

2026-08-20 PR #524 과외에서 self-thread 사진 중복을 새 정책 공백처럼 다시 전달한 뒤, 2026-08-18 PR #520 과외에서 이미 같은 원인을 발견해 운영 트래커 `sw-xvv`로 라우팅했다는 사실을 확인했다.

이 사건은 관문을 적용하기 전에 일어났으므로 실행 1회로 세지 않는다. 파일럿을 만들게 된 baseline으로만 둔다.

## 언제 실행하는가

다음 조건을 모두 만족할 때만 실행한다.

1. 과외 중 버그·개선거리·미결 질문 후보를 발견했다.
2. 제품 저장소를 고치지 않고 `master-delivery-memos.md`에 append하려는 순간이다.
3. 아직 prospective 실행 기록이 3건 미만이다.

질문을 받을 때마다, PR을 열 때마다, 세션을 시작할 때마다 선제적으로 실행하지 않는다. 발견물 후보가 없으면 이 문서를 읽지 않는다.

## 확인할 곳

발견물의 테이블·주체·이벤트·결과를 짧은 검색어 2~4개로 나눈 뒤 아래 순서로 읽기 전용 확인한다.

1. 현재 `master-delivery-memos.md`
2. 그 파일의 git 이력 — 처리 후 비워진 과거 원문까지 확인
3. `../swatch-ops/.beads/issues.jsonl`의 열린 운영 트래커 항목

정확한 단어 일치만으로 같은 문제라고 판정하지 않는다. 어느 테이블·행·값의 주인인지, 어떤 이벤트에서 무엇이 잘못되는지를 대조한다.

과거 세션 JSONL 전수 검색은 기본 관문이 아니다. 모기가 “전에 얘기했다”고 기억하거나, 위 세 곳만으로 동일성을 가릴 수 없을 때의 원문 복구 수단으로만 쓴다.

## 세 가지 판정과 행동

### A. 신규 원인 또는 기존 항목 없음

- 프리앰블의 원래 발견물 형식으로 `master-delivery-memos.md`에 append한다.
- 판독 SHA와 근거를 그대로 남긴다.

### B. 기존 원인 + 같은 영향

- 같은 내용을 새 발견물처럼 다시 append하지 않는다.
- 사용자에게 기존 이슈 ID·상태·과거 처리 근거를 알려준다.
- 기존 항목이 여전히 열린 채 재현됐다는 사실이 새 운영 증거라면, 그 증거만 별도 추가할 필요가 있는지 구분한다.

### C. 기존 원인 + 새로운 영향 또는 새로운 소비처

- “새 정책 공백”으로 복제하지 않는다.
- 기존 이슈 ID를 명시하고, 이번 코드에서 새로 드러난 영향·근거·판독 SHA만 delta로 append한다.
- 기존 항목이 있다는 이유만으로 새 버그 가능성을 닫거나 설명을 중단하지 않는다.

## 실행 기록

각 prospective 실행 뒤 아래만 append하고 `completed_runs`를 1 올린다. 발견물 자체를 메모하지 않은 B 판정도 관문 실행이므로 1회로 센다.

```text
### 실행 N — YYYY-MM-DD · 재료

- 후보: 무엇을 새 발견물로 쓰려 했나
- 검색: 현재 메모 / git 이력 / 열린 트래커에서 무엇을 찾았나
- 판정: A 신규 / B 같은 영향 / C 새 영향
- 행동: 새 메모 / 중복 생략 / 기존 이슈 delta 연결
- 마찰: 모기가 표현한 흐름 방해 또는 도움, 없으면 미관찰
- 오판 가능성: 표현 차이·근거 부족 등 남은 경계
```

## 중단과 사후 판단

- `completed_runs: 3`이 되면 그 다음 발견물부터 자동 적용하지 않는다.
- 모기가 중단을 선언하면 3건 전에도 즉시 멈춘다.
- 3건을 채웠다고 프리앰블의 영구 규칙으로 자동 승격하지 않는다.
- 세 기록을 보고 중복 전달을 실제로 줄였는지, 새 영향을 누르지 않았는지, 검색 마찰이 감당 가능한지를 모기와 다시 판단한다.

## prospective 실행 기록

### 실행 1 — 2026-08-21 · PR #524 `shade_images`·ROI 역할 충돌

- 후보: 폐기된 어드민 갤러리의 잔여 모델 때문에 `shade_images`가 대표 자리·사진 정체성·갤러리 항목 역할을 겸하고, 대표 교체 때 ROI 오염·FK 실패·비대표 노출이 반복된다는 구조 부채를 새 발견물로 쓰려 했다.
- 검색: 현재 메모에는 관련 미처리 항목이 없었고 git 이력에도 `sw-ber` 전달 원문이 없었다. 열린 운영 트래커 `../swatch-ops/.beads/issues.jsonl`에서 `sw-ber`가 같은 `shade_images`/`swatch_regions` 원인과 ROI 오염·`23503` 영향을 이미 담고 있으며, 수리 후보에 `swatch_regions` 폐기 판단도 포함한 것을 확인했다.
- 판정: B 같은 영향. 구조 냄새를 새 이슈로 복제하지 않는다.
- 행동: 중복 발견물은 생략했다. 대신 모기가 기존 선택지 중 “MVP에서 ROI 폐기, 나중에 사진 정체성과 대표 포인터를 분리해 재설계”를 명시적으로 골랐으므로, 그 소유자 결정만 `master-delivery-memos.md`에 기존 `sw-ber` 업데이트용으로 append했다.
- 마찰: 모기가 폐기된 어드민 이미지 탭과 같은 설명이 반복되는 것이 거슬리고 중복 냄새가 난다고 명시했다. 관문은 새 이슈 복제를 막았지만 검색 자체의 흐름 방해 정도는 아직 미관찰이다.
- 오판 가능성: `sw-ber`는 현재 ROI-FK 사용자 작업 롤백을 직접 제목으로 삼고 있고, 대표·갤러리·사진 정체성의 장기 분리 전체를 별도 구조 이슈로 명시하지는 않는다. 이번에는 모기의 폐기 결정이 ROI 문제를 먼저 제거하므로 같은 이슈에 연결했으며, ROI 폐기 뒤에도 비대표 갤러리 모델이 독립적으로 살아 남아 새 영향을 만들면 그때 별도 delta 여부를 다시 판단한다.

### 실행 2 — 2026-08-26 · PR #535 ROI 폐기 뒤 `shade_images` 현재고 모델 제안

- 후보: ROI 폐기 뒤에는 옛 대표를 비대표로 남기는 생성 경로가 사라지는데도 `shade_images.is_primary=false`와 화면 `extraImgs` 소비가 남아, 사진 이력과 현재 대표 상태의 책임이 계속 섞인다는 개선 후보를 새 발견물로 쓰려 했다.
- 검색: 현재 메모는 비어 있었고, git 이력과 열린 트래커 `sw-ber`에는 같은 구조 원인의 ROI 오염·삭제 실패·비대표 노출과 “ROI 폐기 뒤 독립 영향이면 delta 재판단” 경계가 있었다. PR #535 HEAD 생성 경로는 대표 행만 만들고, 원격 QA는 97행 전부 대표·비대표 0·여러 행 shade 0으로 실측됐다.
- 판정: C 기존 원인 + 새로운 영향. ROI 롤백 수리와 별개로, 출처 없는 비대표 갤러리 상태와 현재 대표 모델의 책임 혼합이 남는다.
- 행동: 새 정책 공백으로 복제하지 않고 기존 `sw-ber`를 밝힌 delta만 `master-delivery-memos.md`에 append했다. `shades` 직접 포인터와 별도 현재 대표 표 중 구현안은 잠그지 않았다.
- 마찰: 모기가 현재고·현재고 이력 비유로 원하는 상태 모델을 바로 외부화했다. 중복 검색 자체의 흐름 방해 정도는 미관찰이다.
- 오판 가능성: 현재 데이터와 생성 경로에 비대표 행이 없어도, 아직 문서화되지 않은 “추가 공식 발색 사진” 요구가 있을 수 있다. 제거 착수 전 제품 요구와 원격 데이터 출처를 다시 확인해야 한다.

### 실행 3 — 2026-08-27 · sw-1yt 브랜드 머지의 제품라인 identity 충돌

- 후보: 브랜드 머지에서 양쪽 브랜드 아래에 DB identity가 충돌하는 제품라인이 있으면 단순 `brand_id` 재지정이 실패하는데, 기존 계약에는 선검사·사용자 표시·차단 동작이 없다는 계약 공백을 새 발견물로 쓰려 했다.
- 검색: 현재 메모에는 sw-1yt 역방향 과외 요청과 기본 원자성·사이클 문제만 있었고, git 이력에도 제품라인 identity 충돌 영향은 없었다. 열린 트래커 `sw-1yt`와 기존 계약은 같은 `AdminMergeRows` 수리를 담지만 브랜드 양쪽 제품라인 충돌은 다루지 않았다.
- 판정: C 기존 원인 + 새로운 영향. 별도 이슈가 아니라 열린 `sw-1yt`의 계약 delta다.
- 행동: 모기 원문 노트와 분리해 충돌 조건·차단 필요·판독 SHA를 `master-delivery-memos.md`에 append하고, 마스터가 기존 계약을 보강하도록 `sw-1yt`를 명시했다.
- 마찰: 모기가 필기노트 질문 중 “맥 코스메틱”과 “맥” 양쪽에 같은 제품라인이 있을 수 있음을 직접 발견했고, 자동 연쇄 병합은 “빈대잡으려다가 초가삼간 다태우는” 범위라고 경계를 정했다. 중복 검색 자체의 흐름 방해는 미관찰이다.
- 오판 가능성: 모기가 제외한 “제품라인 합치는 정책”이 브랜드 머지 중 자동 병합만을 뜻하는지, 기존 `product_lines` 직접 머지 원자화까지 제외하는지는 최종 확답이 없어 마스터 확인 경계로 남겼다.
