---
reviewed: false
status: pilot
planned_sessions: 3
completed_sessions: 1
remaining_sessions: 2
---

# 코드 학습 노트 참조 완결성 감사 파일럿

## 상태와 한 줄 목적

이 문서는 영구 운영 규칙이 아니라 **조건에 맞는 코드 학습 노트를 다루는 과외 세션
3회**에만 적용하는 임시 파일럿이다.

과외냥이가 초안을 만든 뒤 별도의 읽기 전용 감사냥이가 정확한 SHA의 실제 소스를
독립적으로 읽고, 이미 내용을 아는 작성자만 복원할 수 있는 생략을 찾는다. 감사냥이는
문서를 고치지 않고 근거가 있는 누락 보고만 돌려준다. 이 보고는 **최종 학습 자료가
아니라 과외냥이가 다시 편집할 사실 근거**다.

## 카운트 규칙

- 한 과외 세션에서는 감사 대상 노트가 여러 개여도 최대 1회만 센다.
- 감사냥이의 완료 보고가 실제로 도착하고 과외냥이가 그 보고를 읽은 뒤에만
  `completed_sessions`를 1 올리고 `remaining_sessions`를 1 내린다.
- 적합한 노트가 없거나, 감사 작업이 시작되지 않았거나, 작업이 실패해 보고가 도착하지
  않으면 횟수를 차감하지 않는다.
- `completed_sessions: 3`이 되면 다음 세션부터 자동 실행하지 않는다. 세 기록을 모기와
  검토하기 전에는 영구 규칙이나 상시 커스텀 에이전트로 승격하지 않는다.
- 모기가 중단을 선언하면 남은 횟수와 관계없이 즉시 멈춘다.

## 언제 실행하는가

아래 중 하나에 해당하는 `code-diff-notes/` 초안을 새로 만들거나 크게 고친 세션에만
실행한다.

- SQL·repo·UI·테스트처럼 여러 파일 층의 역할을 한 흐름으로 연결한다.
- 두 개 이상의 테이블·행이 상태 전이에 참여한다.
- 지정·해제·복귀·fallback·재선정·동시성처럼 조건에 따라 결과가 갈린다.
- 파일 역할 지도, 예시 상태, `모기 박스`, 한 줄 요약에 생략된 참조가 생기기 쉽다.

간단한 한 파일 diff, 철자·문구 수정, 일반 질문 답변에는 실행하지 않는다. 모든 문장을
감사하는 상시 문장 교정기도 아니다.

## 감사 입력

과외냥이는 새 문맥의 읽기 전용 감사냥이에게 다음을 명시한다.

1. 감사할 초안의 정확한 경로
2. PR 또는 fix의 base·HEAD SHA
3. 판독할 실제 소스 파일 경로
4. 제품·운영 저장소와 `mogi-cards` 모두 수정하지 말라는 경계
5. 감사 범위와 아래 고정 출력 형식

감사냥이는 현재 checkout의 우연한 상태로 추측하지 않는다. 초안에 적힌 판독 HEAD의
blob을 `git show <SHA>:<path>`처럼 읽어 근거를 고정한다. 근거를 찾지 못하면 설명을
완성하지 않고 `미확인`으로 표시한다.

## 감사 범위

다음 구역만 우선해서 본다.

- 변경 파일 역할 지도
- 예시 상태와 이벤트 전후 설명
- `모기 박스`의 질문과 힌트
- SQL → repo → UI → 테스트처럼 층을 건너는 연결 문장
- 마지막 한 줄 요약

raw diff 자체, 전체 파일명 목록, frontmatter, 날짜 같은 메타데이터는 기본 감사 대상이
아니다. 다만 설명이 raw diff와 반대로 적혔으면 사실 불일치로 보고한다.

## 감사 결과와 모기용 설명의 경계

참조가 완결된 문장과 사람이 먼저 이해하기 쉬운 문장은 같지 않을 수 있다. 감사냥이가
찾은 고유명사·행 조건·반환 필드를 한 지도나 요약에 모두 넣으면 사실은 정확해져도
사용자 사건이 구현 명세에 묻힐 수 있다.

감사냥이는 이 순서를 설계하거나 최종 문장을 쓰지 않는다. 최종 답변의 소유자인
과외냥이가 확인된 사실을 아래 순서로 다시 합성한다.

1. 첫머리에는 함수명과 파일명 없이, 사용자가 무엇을 하면 화면이나 저장 상태가 어떻게
   바뀌는지를 2~3문장으로 쓴다.
2. 서로 다른 사용자 이벤트와 결과를 별도 흐름으로 나눈다. 예: `수동 지정`과
   `해제 후 자동 복귀`.
3. 함수명·RPC 이름·파일 경로·행 조건·불투명한 테스트 변수는 마지막 근거 구역이나
   해당 raw hunk 해설에 둔다.

기술 이름을 문서 전체에서 없애라는 뜻은 아니다. 먼저 의미를 이해하고, 필요할 때 그
의미를 증명하는 구현 근거로 내려갈 수 있게 층을 나누라는 뜻이다. 이 합성을 위해 별도의
세 번째 에이전트를 추가하지 않는다.

## 참조 완결성 판정

`지정`, `해제`, `복귀`, `변환`, `반환값`, `두 개`, `해당`, `결과`, `처리한다` 같은
단어는 검색 후보일 뿐 자동 반려 사유가 아니다. 감사냥이는 각 문장에서 필요한 범위의
다음 슬롯이 실제로 묶여 있는지를 본다.

```text
어느 테이블·행·컬럼인가
→ 그 값이나 동작의 주인은 누구인가
→ 어떤 이벤트와 조건인가
→ 변경 전 값·행은 무엇인가
→ 변경 후 값·행은 무엇인가
→ RPC·함수·파일처럼 고유한 대상 이름은 무엇인가
```

표, 파일 역할 지도, `모기 박스`, 캡션은 따로 훑어 읽히므로 그 항목 안에서 참조가
완결돼야 한다. 이어지는 해설 문단은 바로 앞 문장에 정확한 테이블·행·이벤트가 이미
고정돼 있으면 같은 이름을 기계적으로 반복하지 않아도 된다.

위 슬롯을 모든 개요 문장에 한꺼번에 넣지는 않는다. 첫 사용자 시나리오가 의미를
고정하고 뒤의 근거 구역이 정확한 이름과 행 조건을 받치면, 개요는 그 연결을 다시
구현 명세처럼 열거하지 않아도 된다.

## 감사냥이 고정 출력

전체 문서를 자유롭게 재작성하지 않는다. 발견마다 아래 한 묶음만 반환한다.

`모호한 원문`은 초안에서 문자 그대로 복사한다. 빈칸·질문·예시 값을 감사자가 임의로
채우거나 평서문으로 바꾼 인용은 finding으로 인정하지 않는다.

```yaml
- 초안 위치: <소제목·문장>
  모호한 원문: <그대로 인용>
  literal하게 남는 질문: <문장만 읽으면 정해지지 않는 대상·조건>
  빠진 슬롯: <대상 | 테이블·행 조건 | 컬럼 | 이벤트 | 변경 전후 | 고유명사>
  소스에서 확인한 사실: <근거로 확인한 최소 사실>
  최소 교체안: <빠진 참조만 보충한 문장>
  근거: <HEAD SHA · 파일 경로 · 함수/심볼 · 줄 위치>
  판정: 확인됨 | 미확인
```

발견이 없으면 `PASS — 감사 범위에서 참조 누락 없음`과 실제 판독한 SHA·파일만
반환한다. “모기가 물을 가능성이 높다”처럼 모기의 미래 반응이나 능력을 추측하지 않는다.

## 과외냥이의 후처리

1. 감사 보고를 실제 소스와 다시 대조한다. 감사자의 대체 문장을 그대로 복사하지 않는다.
2. 확인된 누락은 최종 원고가 아니라 사실 근거로만 사용한다. 감사 결과를 순서대로
   붙여 넣거나 모든 고유명사를 같은 지도·요약·`모기 박스`에 몰아넣지 않는다.
3. 첫머리의 사용자 사건 2~3문장과 이벤트별 흐름을 먼저 읽는다. 감사 반영 뒤 이 의미가
   구현 이름에 묻혔다면, 사실을 버리지 말고 기술 근거를 뒤 구역으로 재배치한다.
4. 함수명·RPC 이름·파일명·테스트 변수는 정확한 근거가 필요한 구역에 둔다. 사용자가
   처음 읽는 핵심 설명보다 앞세우지 않는다.
5. 의미·정책 판단이 달라지는 수정은 모기에게 보고한 뒤 결정한다.
6. 감사냥이는 파일을 수정하지 않는다. 파일 수정과 최종 학습 자료의 구성·문장 책임은
   과외냥이에게 있다.
7. 실행 뒤 이 문서의 카운터와 아래 기록을 갱신한다.

## 실행 기록 형식

```text
### 세션 N — YYYY-MM-DD · 재료

- 감사 초안:
- 판독 SHA·소스:
- 보고: PASS 또는 발견 N건
- 실제로 유용한 누락:
- 과잉 지적·오판:
- 모기가 표현한 도움·마찰: 없으면 미관찰
- 다음 회차에서 한 가지만 바꿀 것:
```

## 종료 뒤 판단할 것

- 다중 파일 역할 지도와 상태 전이의 빠진 대상을 실제로 잡았는가
- `모기 박스`를 literal하게 읽을 때 생기는 해석 부담을 줄였는가
- 근거 재확인과 조정 비용이 감당 가능한가
- 감사자의 최소 교체안이 과도한 재작성으로 번지지 않았는가
- 함수명과 파일명을 읽기 전에 사용자 사건과 최종 화면 변화를 이해할 수 있는가
- 확인된 사실을 보존하면서 구현 근거를 뒤쪽 층으로 분리했는가
- 영구 규칙, 선택 모드, 폐기 중 무엇이 맞는가

## 실행 기록

### 세션 1 — 2026-08-21 · PR #524 manual 지정·해제·자동 복귀

- 감사 초안: `code-diff-notes/2026-08-21-pr524-manual-set-clear-flow.md`
- 판독 SHA·소스: HEAD `da672a05a9eb36cc158d0f120b2f9f673ba39ec0`; `20260819000000_admin_manual_shade_representative.sql`, `adminRepresentativeRepo.ts`, `AdminRepresentativeCuration.tsx`, `admin_manual_representative_fixture.sql`
- 보고: 감사냥이 보고 8건. 과외냥이 재대조 결과 참조 누락 7건, 오판 1건.
- 실제로 유용한 누락: 파일 역할 지도에서 두 RPC의 고유명사·각 트랜잭션 경계·반환 필드가 빠짐. ROI 조건 없는 예시 박스는 기존 행이 삭제되는지 데모트되는지 답이 하나로 정해지지 않음. 해제 박스의 RPC 이름·반환 필드, `refresh()`가 다시 부르는 repo 함수와 읽는 행 조건, PASS(10)의 호출·shade, 마지막 요약의 지정·해제 함수와 primary 행 조건이 빠졌음을 확인함.
- 과잉 지적·오판: §1의 실제 빈칸 문장 `어드민이 선택한 후보 사진의 UUID = ___ 테이블의 ___ 컬럼`을 감사냥이가 `shade_images 테이블의 media_id 컬럼`이라고 이미 답이 적힌 문장처럼 바꿔 인용함. 원문 인용 계약을 어겼고, 빈칸의 목적도 잘못 해석했으므로 이 finding은 폐기함. 마지막 요약의 대체안은 근거는 맞지만 한 문장에 이름과 행 조건을 너무 많이 넣어 그대로 적용하면 읽기 부하가 커짐.
- 모기가 표현한 도움·마찰: 독립 감사 구조와 3세션 파일럿 제안에는 긍정함. 그러나 감사에서 확인한 §2 누락이 실제 노트에는 아직 반영되지 않은 것을 읽다가 다시 발견했고, `서버가 지정·해제`, `두 RPC`, `반환값`, `실제 DB 행`에는 주어·목적어·고유명사가 빠졌다고 지적함. 즉 감사 보고가 정확해도 과외냥이가 확인된 누락을 노트에 반영하지 않으면 독해 마찰은 그대로 남는다는 사용감이 관찰됨.
- 최종 반영: 모기의 재지적 뒤 유효한 7건을 실제 노트에 최소 수정으로 반영함. §2에는 RPC·repo·UI 함수명과 입력·반환·검증 행을, §3에는 `image-old`의 ROI 부재 전제를, §6에는 해제 RPC의 `action`·`reselected`와 최종 primary 행 조건을, §7에는 `refresh()`의 실제 함수·조회 컬럼을, §8에는 PASS(10)의 호출과 기대 행을, §9에는 지정·해제 함수명이 있는 흐름을 명시함. 오판 1건은 반영하지 않음.
- 다음 회차에서 한 가지만 바꿀 것: 감사 프롬프트에 `모호한 원문은 초안에서 문자 그대로 복사하고, 빈칸·질문을 감사자가 임의로 채워 평서문으로 바꾸지 않는다`를 추가한다.

#### 첫 회차 사후 사용감과 규칙 조정

- 모기는 최종 반영본의 §2를 읽은 뒤, 이전의 의미 중심 설명에서 반대편인 구현 명세서로
  과보정돼 더 힘들어졌다고 표현했다. 특히 migration → repo → UI → fixture 네 파일의
  이름·입력·반환·행 조건이 같은 중요도로 이어져, 핵심 사건을 마지막에 역산해야 했다.
- 첫 회차의 사실 누락 탐지는 유효했지만, 감사용 중간 산출물을 과외냥이가 사용자
  시나리오로 다시 합성하지 않은 것이 새 마찰이었다. `a2.jpg 수동 지정 → 해제 → 자동
  규칙이 a1.jpg 재선정`이라는 의미가 파일과 함수 설명보다 먼저 와야 했다.
- 다음 회차 조정: 기존의 원문 인용 계약을 유지하고, 과외냥이 후처리에 `감사 결과는
  사실 근거로만 사용 → 사용자 사건 2~3문장 → 이벤트별 흐름 → 마지막 구현 근거` 순서를
  추가한다. 별도의 세 번째 에이전트는 추가하지 않는다.
