# 마스터 전달용 메모 (단일 수신함)

과외 세션 발견물을 한곳에 누적하는 마스터 전달용 단일 수신함이다. 메모는 리드 카드나 다른 파일로 옮기지 않고 이 파일에만 append한 뒤 커밋한다.

운영 규칙 (모기 확정 2026-08-14): 마스터가 메모를 확인·처리할 때마다 이 파일을 비운다. 처리 내역은 비움 표식 한 단락으로 남기고, 원문은 git 이력이 보존한다.

(비어 있음 — 2026-08-27 마스터 18차 비움(냐옹이 Gen 15). 처리분 2건, 판독 도장 swatch-v2 a997b6f = 현재 dev 최신 → 판독 유효.
① **[역방향 회신] sw-1yt 예쁜필기노트 1호** — 모기 원문 10줄을 한 글자도 다듬지 않고 계약 §0에 박고(swatch-ops contracts/2026-08-26-sw-1yt-admin-merge-rpc.md v2), 줄↔검증 매핑표(N1~N10 ↔ V1~V9) 작성, 워커 RED→GREEN dispatch(ctx_4489305fcc49). 과외냥이 판독은 참고용으로 원문 아래 분리 수록.
② **sw-1yt delta — 브랜드 머지의 제품라인 identity 충돌 선검사 부재** — 새 이슈 아님, 계약 §2-1에 선검사(변경 0 + 충돌 쌍 반환)·p_dry_run 미리보기(N5)·삭제 행 거부(N8)로 흡수. 자동·연쇄 병합은 범위 밖 명시.
원문 = git 이력.)

## PR #539 N3 의도 delta — 상위 분류 카테고리는 일반 머지 대상이 아니다

모기 원문: “일단 기계적으로 말하면 양방향 다 막아야된다옹. 근데 그건 양방향을 다 막는다!라기보단.. 그냥 부모(립이라는 추상적카테고리.. 그저 립이라고 묶기위한..)가 추상적카테고리라서.. 라고생각함.. 그래서 그 추상적카테고리 대상의 머지는 막아야한다는..” 이어서 현재 기계적 기준을 “자식 행이 하나라도 달린 카테고리는 남길 쪽과 없앨 쪽 어느 선택칸에도 나오지 않게 하고, 자식이 없는 카테고리끼리만 머지를 허용”으로 확인하자 “약간 불안하긴하지만 그게 내뜻 맞는것같아... (내가 추후에 카테고리를 세분화하지 않는한..)”라고 답했다.

근거: PR #539 HEAD `5603d3a`의 `app/src/features/admin/routes/AdminMergeRows.tsx:156-179`는 `duplicate`의 자손을 `canonical` 후보에서, `canonical`의 조상을 `duplicate` 후보에서 빼서 같은 `부모 → 자기 자손` 방향만 선택 순서 양쪽에서 차단한다. `supabase/migrations/20260826170000_rpc_admin_merge_rows.sql:168-187`도 `canonical`이 `duplicate`의 자손인 사이클만 거부한다. 따라서 자식 카테고리를 부모에 합치거나, 자식이 달린 상위 분류 카테고리를 관계없는 카테고리와 합치는 경로는 남아 있다.

왜 문제인지: 예쁜필기노트 N3의 표면 문장 “부모를 자식에 머지하는 경로가 존재하지 않았으면”은 통과했지만, 이번 PR 완독 뒤 모기가 밝힌 실제 정책은 사이클 방지만이 아니라 `립`처럼 자식을 묶는 상위 분류 카테고리를 일반 중복 행 머지의 양쪽 대상에서 제외하는 것이다. 현재 구현은 그 사용자 의도를 고정하지 못하므로, 노트↔구현 지표에서는 **다르게 된 줄 1개: N3**로 기록한다.

범위 경계: 현재 구조에서는 `categories.parent_id`로 나를 가리키는 활성 자식 행이 하나라도 있음을 상위 분류의 기계적 기준으로 삼는 방향이다. 모기는 미래에 카테고리를 세분화하면 이 정책을 다시 볼 수 있다는 불안을 명시했으므로 영구적인 도메인 불변식으로 넓히지 말고, 현행 일반 머지 도구의 가역적·fail-closed 운영 규칙으로 계약을 보강할지 마스터가 판단해 달라.

판독 시점 커밋 SHA: PR #539 HEAD `5603d3a`
