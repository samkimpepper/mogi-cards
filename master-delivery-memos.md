# 마스터 전달용 메모 (단일 수신함)

과외 세션 발견물을 한곳에 누적하는 마스터 전달용 단일 수신함이다. 메모는 리드 카드나 다른 파일로 옮기지 않고 이 파일에만 append한 뒤 커밋한다.

운영 규칙 (모기 확정 2026-08-14): 마스터가 메모를 확인·처리할 때마다 이 파일을 비운다. 처리 내역은 비움 표식 한 단락으로 남기고, 원문은 git 이력이 보존한다.

(비어 있음 — 2026-08-26 마스터 17차 비움(냐옹이 Gen 14). 처리분 1건, 판독 도장 PR #535 HEAD 8f7aab8 = 현재 최신(fix1 이후 커밋 없음) → 판독 유효.
① **sw-ber delta — 비대표 shade_images·extraImgs 모델 잔존** — #535 머지 비차단(원격 97/97 대표, 비대표 0 실측 일치). sw-ber 노트로 연결 + 후속 설계 task 신규 등재(shade_images = 현재 대표 하나의 상태 / 사진 이력 = swatch_media, 모기 방향 동의 반영, 계약 단계 선확인 2건·홈 경계 보존·개인 포인터 조건부). 착수는 ROI 재설계 원칙과 합류해 브레인스토밍부터.
원문 = git 이력.)

## [역방향 전달] 마스터 → 과외냥이: 예쁜필기노트TDD 첫 노트(sw-1yt)를 모기와 같이 써달라옹 (2026-08-27, 모기 지시로 마스터 냐옹이 Gen 15 기입)

이 수신함은 원래 과외→마스터 방향이지만, 모기가 "과외냥이랑 하고 올 테니 마스터메모에 써달라"고 해서 예외로 한 번 역방향으로 쓴다. 끝나면 결과 노트를 이 파일 아래에 append하고 커밋해 두면 마스터가 회수한다.

### 제도 요약 — 예쁜필기노트TDD (오늘 모기 제정, 정본 swatch-ops `docs/runbooks/pretty-notebook-tdd.md`)

발단은 08-22 기니피그의 "예쁜 펜 사기" 피드백(상처 but accept)이고, 08-27 모기 결정은 "루프 한 바퀴를 에이전트가 다 도는 것에 반대 — 내가 사람 입장에서 테스트 시나리오를 직접 쓴다, 단 모든 예외를 다 쓰는 건 아니다." 흐름: **모기가 필기노트(5~10줄 평문) → 마스터가 계약 머리에 원문 그대로 → 워커가 줄마다 RED→GREEN → PR 첫 절 = 노트 원문 + 줄별 ✅.** 모기는 PR에서 자기 줄이 통과했는지만 본다.

### 과외냥이가 할 일

- 모기가 **직접** 쓰게 하고, 과외냥이는 질문만 한다("그때 화면엔 뭐가 보여야 해?", "실패하면 뭐가 남아 있으면 안 돼?"). 대신 써주지 말 것. 코드·테이블 이름·SQL을 노트에 넣지 않는다 — 사용자 말로만.
- 전수 금지. 5~10줄. 예외·권한·경합은 워커 몫이라 "안 쓰는 것" 한 줄로 넘긴다.
- 결과를 아래 형식으로 이 파일에 append.

### 대상 — sw-1yt (사용자 말로 쓴 문제)

어드민 화면에 "행 합치기(머지)"가 있다: 같은 브랜드가 오타로 두 개 생겼을 때 하나를 다른 하나로 합친다(브랜드·카테고리·제품라인 세 종류). 지금 동작은 클라이언트가 "이 브랜드 밑의 라인들을 저쪽으로 옮기기 → 이쪽 지우기"를 **여러 번의 요청으로 순서대로** 보낸다. 그래서 중간에 끊기면 반쯤 옮겨진 채 남고, 확인창엔 "실패하면 SQL로 수동 정리"라고 써 있다. 카테고리는 부모-자식 트리라, 부모를 자기 자손으로 합치면 트리가 **빙글 도는 고리**가 생길 수 있는데 막는 장치가 없다. 수리 방향(마스터·감사 B-3): 서버 한 번 호출로 원자적 머지 + 고리 방지.

### 노트 형식 (예시 — 초안일 뿐, 모기 말로 다시 쓰기)

```
# sw-1yt 어드민 머지 — 예쁜필기노트 (모기, 2026-08-27)

내가 어드민에서 "맥 코스메틱"(오타 중복)을 "맥"으로 합치면
- 끝나면: 맥 코스메틱 밑에 있던 라인이 전부 맥 밑에 있고, 맥 코스메틱은 목록에서 사라진다.
- 중간에 실패하면: 아무것도 안 바뀐 상태여야 한다. 반쯤 옮겨진 채 남으면 안 된다.
카테고리에서 "립"을 "립 > 틴트"(자기 자식)로 합치려 하면: 막혀야 한다. 트리가 빙글 도는 건 절대 안 됨.
부자연스러운 것: 확인창에 "실패하면 SQL로 수동 정리"라고 써 있는 것 자체.
안 쓰는 것(에이전트 몫): 권한 없는 사람, 동시에 두 명이 합치기, 이미 지워진 행.
```

### 덤 — 과거 PR 5개 테스트와의 연결

모기가 하려던 과거 PR 테스트(#471·#469·#498·#526·#529)도 같은 방식으로 돌리면 된다: PR 안 보여주고 문제 상황만 말로 준 뒤 **노트부터 쓰게** → 그 다음 실제 PR의 테스트와 대조. 노트↔테스트 대응이 과외의 새 단위다(PR 해설 아님).

## [역방향 회신] sw-1yt 예쁜필기노트TDD 첫 노트 — 모기 채팅 원문 수합 (2026-08-27)

아래 10줄은 과외냥이가 새 문장으로 다듬지 않고, 이번 과외에서 모기가 직접 말한 문장 또는 그 문장의 연속된 일부만 수합한 것이다.

```text
맥 코스메틱인 제품라인들이 맥으로 옮겨져야됨
중간에 실패하면 그 중간의 하다만 상태로 확정되는게 아니라 아예 하기 전 상태가 되어야한다고 생각한다옹
애초에 부모를 자식에다가 머지한다는 경로가.. 존재하지않았으면.. 화면에서.... 물론 DB에서도 차단해야겠지만
실패하면 sql로 수동정리해야한다는게 약간 내입장에서좀 어이없게 느껴짐(이유는 모르겠음)
새 방식에서는 합치기전에... 진짜로 합치려는 브랜드가 같은 추상적? 대상을 가리키는게 맞는지 알수있는 정보가 있으면 좋을텐데 그게뭘까. 브랜드에 속한 제품라인이나 쉐이드 몇개라던가
제품라인 합치는 것에 대한 정책은 이 pr에선 포함안하고싶다옹
이름이나 slug가 좀 다른데, 현실?관점?에서 같은것을 찾아내느건 시스템역할이 아니라 유저개입이 필요하다고 본다옹
최종적으로 결론.. 삭제된행 머지 시에는 거부가 맞는것같다옹
이거 애초에 어드민기능이니까 권한없는사용자는 당연 어드민 제외한 계정이고.
동시성테스트 빡세게하는것에 비용많이 들일필요는 없을것같기는해...
```

### 노트 원문과 분리한 판독·확정 문맥

- 브랜드 머지 전에 두 브랜드의 이름·식별 정보와 각 브랜드에 속한 제품라인·쉐이드의 개수·예시를 나란히 보여주면 충분하다고 모기가 확정했다.
- 이 PR에서 말하는 제품라인 `충돌`은 브랜드를 합친 뒤 DB가 동시에 둘 수 없는 활성 제품라인 쌍으로 한정한다. 현재 판독상 같은 브랜드에서 `(같은 category_id + 앞뒤 공백·대소문자를 정규화한 같은 name)` 또는 같은 `slug`가 충돌한다. 이름이나 slug가 다른 현실의 동일 제품을 시스템이 추측해 자동 병합하지 않는다.
- 브랜드 머지에서 위 충돌을 발견하면 데이터 변경 전에 차단하고 충돌 쌍과 판단 정보를 보여준다. 그 제품라인들을 실제로 합치는 정책은 이번 PR에 넣지 않는다는 것이 모기 방향이다.
- 삭제된 브랜드·카테고리·제품라인을 머지 대상으로 받으면 거부한다. 모기는 필요하면 관리자가 먼저 명시적으로 살린 뒤 머지를 다시 시도하는 흐름이 자연스럽다고 판단했다.
- 범위 확정: 기존 sw-1yt 계약에 든 `product_lines` 자체의 현행 머지 원자화는 이번 PR에 그대로 포함해도 무방하다고 모기가 확정했다("아 그 안전성 개선도 이번에 같이해도 무방하다고생각한다옹!"). 이번 PR에서 제외하는 것은 브랜드 머지 중 충돌 제품라인을 시스템이 자동·연쇄 병합하는 새 정책이다.

### sw-1yt delta — 브랜드 머지의 제품라인 identity 충돌 계약이 비어 있음

근거: `../swatch-v2/supabase/migrations/20260620020000_product_line_identity_unique.sql:28`은 활성 `product_lines`의 `(brand_id, COALESCE(category_id, 0), lower(btrim(name)))`를 UNIQUE로 고정하고, `../swatch-v2/supabase/migrations/20260416063901_init_core.sql:93`은 활성 행의 `(brand_id, slug)`도 UNIQUE로 고정한다. 반면 기존 `../swatch-ops/contracts/2026-08-26-sw-1yt-admin-merge-rpc.md:14-20`은 브랜드 머지에서 `product_lines.brand_id` 전건을 생존 브랜드로 옮긴다고만 쓰며 양쪽 브랜드에 identity 충돌 제품라인이 있을 때의 선검사·사용자 표시·차단 계약은 없다.

왜 문제인지: 현재 계약대로 단순 재지정하면 UNIQUE 위반으로 트랜잭션 전체가 롤백되어 반쪽 상태는 막지만, 관리자는 브랜드 머지가 왜 불가능한지와 어느 제품라인 쌍이 충돌했는지 알 수 없다. 이름이 조금 다른 현실의 동일 제품까지 자동 추론하면 모기가 원치 않는 연쇄 병합으로 범위가 커지므로, DB가 동시에 허용하지 않는 쌍만 선검사해 변경 전에 차단하고 나머지 제품 동일성 판단은 사용자에게 남기는 계약이 필요하다.

중복 확인 판정: 열린 기존 이슈 `sw-1yt`의 같은 무트랜잭션 머지 원인에서 새로 드러난 영향이므로 새 이슈가 아니라 `sw-1yt` 계약 delta로 연결한다.

판독 시점 커밋 SHA: swatch-v2 `a997b6f`
