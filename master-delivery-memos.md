# 마스터 전달용 메모 (단일 수신함)

과외 세션 발견물을 한곳에 누적하는 마스터 전달용 단일 수신함이다. 메모는 리드 카드나 다른 파일로 옮기지 않고 이 파일에만 append한 뒤 커밋한다.

운영 규칙 (모기 확정 2026-08-14): 마스터가 메모를 확인·처리할 때마다 이 파일을 비운다. 처리 내역은 비움 표식 한 단락으로 남기고, 원문은 git 이력이 보존한다.

(비어 있음 — 2026-09-02 마스터 20차 비움(냐옹이 Gen 18). 처리분 1건: **sw-ypn 필기노트 초안** → swatch-ops `contracts/2026-09-02-sw-ypn-notebook.md`(원문 그대로 + 마스터 정리 제안, 모기 ㅇㅇ 확정 대기). 과외냥이 코드 근거 6건 21d552f에서 독립 실측 일치. 방향 재정렬(홈탭 관리 완결, 지름길은 인터뷰 후) sw-ypn 노트 등재, 줄 3~6은 sw-ykw 재료. 리드 카드의 "수정·비공개·삭제가 프로필에만" 전제는 오류였음 — 카드에 정정 절 추가. 원문 = git 이력.)

## 홈 내 발색샷→내 화장품 다중 보유 전환 브리지 부재

모기 UAT 원문: “내 발색샷을 보관함으로 옮기는것도 하나하나해야돼서 불편” — 한 발색샷에 여러 제품이 연결돼 있을 때 각 제품 상세를 하나씩 열어 `보유함에 추가`를 반복한 사건이라고 재확인했다.

근거: 기존 결정 `../swatch-v2/docs/wiki/decisions/D-085.md`는 발색샷 등록과 `inventory.status='owned'` 전환을 서로 다른 사용자 의도로 보고 자동 cascade를 폐기했다. 현재도 상세 시트가 한 `shade.id`의 상태만 읽고 `toggleInventory(shade.id)`를 호출한다(`../swatch-v2/app/src/features/shade-detail/DetailSheet.tsx:89,481-496`). 한 발색샷은 여러 제품 매핑을 가질 수 있지만 홈 셀은 그중 첫 매핑 제품 상세 하나를 연다(`HomeArchiveGrid.tsx:170-181`).

왜 문제인지: “보유 전환은 명시적이어야 한다”는 정책이 “제품 상세를 한 건씩 열어 반복해야 한다”는 UX까지 요구하지는 않는다. 두 기록의 역할을 분리해 두고 연결 동작을 제공하지 않아, 사용자는 같은 자료를 두 탭에서 다시 관리한다고 느낀다.

소유자 확정 방향: 발색샷 하나에 연결된 제품들과 각 현재 상태(`보유 중 / 위시 / 지난 템 / 미등록`)를 한 화면에서 보여주고, 원하는 제품을 골라 한 번의 확인으로 `보유 중` 전환한다. 자동 전환은 하지 않으며 `지난 템`은 과거 상태를 되살리는 별도 확인을 유지한다. 기존 열린 이슈에서 같은 원인·영향을 찾지 못했으며 `sw-andi`는 그리드 라벨 위계만 다루므로 별개다.

판독 시점 커밋 SHA: `3d17463`.

## PR #540 UAT GIF — 빈 내 화장품 화면이 데이터 없는 조작층과 뜻이 안 보이는 진입점을 모두 노출

영상 근거: `/mnt/c/Users/82109/Pictures/540pr.gif` 0초. `내 화장품 · 0`인데도 상태 3개, 카테고리 5개, 검색, 정렬이 먼저 보이고, 빈 상태 안에는 `탐색에서 제품 찾기`와 `첫 제품 담기`, 화면 우하단에는 라벨 없는 `+`가 동시에 보인다.

코드 근거: 제품이 0개여도 큰 영역 선택, 상태, 카테고리, 검색·정렬은 빈 상태 분기보다 먼저 항상 렌더된다(`../swatch-v2/app/src/features/home/HomePage.tsx:227-280`). 두 빈 상태 CTA는 각각 탐색 탭 이동과 범용 검색 시트 열기로 구현상 다르지만 문구가 그 차이를 설명하지 않고(`HomePage.tsx:276-281`), 전역 `+`는 발색 등록 시트를 여는데 화면에는 아이콘만 보인다(`../swatch-v2/app/src/shared/layout/Shell.tsx:32-53`).

왜 문제인지: 사용자는 검색·정렬할 데이터가 없는데도 네 층의 조작을 먼저 해석하고, 세 진입점 중 무엇이 제품 검색·탐색 구경·발색 등록인지 추론해야 한다. 웹 ChatGPT가 두 CTA와 FAB를 같은 제품 추가 행동으로 오독한 사실도 구현 오해라기보다 현재 화면의 의미 구분이 약하다는 사용성 증거다.

기존 이슈 경계: `sw-ck3`은 카테고리별 결과 수와 0개 비활성화를 다루지만, 전체 제품 수가 0일 때 조작층을 숨기거나 CTA/FAB의 행동 대상을 밝히는 문제는 다루지 않는다. `내 화장품 / 내 발색샷`을 별도 페이지로 분리할지는 이 증거만으로 확정하지 말고 인터뷰 뒤 IA 결정으로 남긴다.

판독 시점 커밋 SHA: `0a1a872`.

## sw-ek3v 델타 — 한 상세 안에서 제품 기록 편집과 발색 게시물 편집이 같은 연필 언어로 보임

영상 근거: 같은 제품 상세에서 상단 `보유 중 · 메모 남기기 · 지난 템 · 삭제 · 연필` 줄을 조작한 뒤, 아래 발색 게시물 카드의 `비공개 · 수정 · 삭제` 줄에서 다시 수정을 연다(약 60~100초). 발색 수정은 등록 시트를 재사용해 `2/4`에서 사진·스레드 선택을 확인하고 `4/4`까지 이동해야 본문을 저장한다.

코드 근거: 상단은 한 `shade.id`의 개인 보유 레코드를 고치는 `InventoryStatusBar`다(`../swatch-v2/app/src/features/shade-detail/DetailSheet.tsx:325-341`). 하단은 한 `swatch.id`의 공개 여부·수정·삭제를 고치는 `SwatchOwnerControls`이고, 수정 버튼은 기존 `SwatchContributionSheet` 수정 모드를 연다(`../swatch-v2/app/src/features/shade-detail/SwatchReviewCard.tsx:242-255`, `SwatchContributionSheet.tsx:69-81,566-570`).

새 영향: 기존 `sw-ek3v`의 “등록 시트 재활용이 싫다”에 더해, 사용자가 지금 제품 보유 기록을 고치는지 발색 게시물을 고치는지 같은 상세 안에서 조작 대상을 다시 판별해야 한다. 수정 전용 시트를 설계할 때 단순히 단계를 줄이는 것뿐 아니라 두 편집 영역의 제목·라벨·시각적 경계도 함께 검토할 필요가 있다.

소유자 확정 수정 계약(2026-09-02): 수정 버튼을 누르면 사진·본문·연결 제품 등 발색 게시물의 수정 대상을 한 편집 화면에서 모두 해결한다. `다음` 버튼이나 단계 전환은 두지 않고, 현재 값을 한꺼번에 펼쳐 필요한 부분만 바꾼 뒤 한 번 저장한다. 신규 등록의 단계형 흐름을 수정에 재사용하지 않는 것이 핵심이며, 특정 필드 변경에 추가 선택기가 필요하면 같은 편집 화면에서 여는 보조 조작으로 둔다. 삭제는 이 편집 화면에 중복하지 않고 현재 상세시트의 기존 삭제 버튼에서 처리한다.

판독 시점 커밋 SHA: `0a1a872`.

## 개인 아카이브 용어 흔들림이 두 영역의 중복감을 키움

근거: 홈의 두 큰 영역은 `내 화장품`과 `내 발색샷`이지만(`../swatch-v2/app/src/locales/ko/pages.json:16-20`), 같은 제품 상태 영역을 `보유함`, `내 보관함`, `보관함 중`으로 부르고 등록 완료 문구는 `보관함 내 발색`이라고 부른다(`../swatch-v2/app/src/locales/ko/sheets.json:3-11,35-37,285-296`). 모기는 UAT 직후 “내발색샷탭.. 내보관함?탭”이라고 되물으며 역할 분리는 필요하지만 중복이 많게 느껴진다고 말했다.

왜 문제인지: 제품별 소유 상태와 사진·게시물 기록은 다른 대상인데, 같은 표면 이름이 흔들리고 `보관함`이 양쪽에 걸쳐 쓰여 그 차이가 화면에서 사라진다. 사용자가 정확한 탭 이름과 역할을 말하지 못한 것은 기억 문제가 아니라 현재 카피가 서로 다른 대상을 일관되게 식별하지 못한 입력 문제다.

판독 시점 커밋 SHA: `3d17463`.
