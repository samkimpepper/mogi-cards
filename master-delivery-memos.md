# 마스터 전달용 메모 (단일 수신함)

과외 세션 발견물을 한곳에 누적하는 마스터 전달용 단일 수신함이다. 메모는 리드 카드나 다른 파일로 옮기지 않고 이 파일에만 append한 뒤 커밋한다.

운영 규칙 (모기 확정 2026-08-14): 마스터가 메모를 확인·처리할 때마다 이 파일을 비운다. 처리 내역은 비움 표식 한 단락으로 남기고, 원문은 git 이력이 보존한다.

(비어 있음 — 2026-08-16 마스터 8차 비움. 처리분 2건 라우팅 내역: ①공개 자격 swatch 단위 원자화(유효·신규 모기 확정) → 결정문 개정 5 §8 신설 + fix2로 워커 지시(행별 anon_read 실측 확인 — 반쪽 발색 노출 실재) ②배치 예산 선차감 재지적 → fix1 반영 HEAD 재실측 결과 기수리(예산은 beginAttempt 시도 직전 차감, claim은 시각만, 기본 배치 50→10행, 미도달 행 예산 무손실) — 인용 라인이 fix1 이전 파일 구조라 낡은 판독 판정, 추가 조치 없음. 원문 = git 이력.)

## 정정: PR #518 배치 선차감 지적은 fix1 이전 HEAD를 현재 상태로 오인한 판독이다

근거: 판독 시점 PR HEAD `c19f702` · `app/api/swatch-media-retry.ts:45-47,91-115` · `app/server/swatchMediaService.ts:124-149` · `supabase/migrations/20260814110000_swatch_media_model.sql:452-480`

이전 메모는 fix1 이전 SHA `75665ad`의 50행 선차감 구조를 현재 PR의 미해결 문제로 잘못 적었다. 현재 HEAD에서는 claim이 `last_attempt_at`만 찍고, 기본 10행을 순회하면서 실제 다운로드 직전 `beginAttempt`가 `retry_count`를 올리므로 실행시간 예산에 닿지 못한 행은 횟수를 잃지 않는다 — 이 항목은 추가 조치 없음이며, 함께 작성한 code-diff 노트도 옛 실패 사례와 일반적인 lease 대안을 설명하는 자료로 정정했다.

재발 방지: fix 커밋이 계속 들어오는 PR은 결함 판정 직전에 `headRefOid`를 다시 확인하고, 근거와 같은 SHA를 판독 시점 도장으로 남긴다. 어제 fix1이 아직 없던 HEAD를 읽은 판정과 오늘 이미 fix1이 들어온 뒤 옛 HEAD를 읽은 오판은 같은 시점 확인 문제의 양면이다.
