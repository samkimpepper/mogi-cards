# 마스터 전달용 메모 (단일 수신함)

과외 세션 발견물을 한곳에 누적하는 마스터 전달용 단일 수신함이다. 메모는 리드 카드나 다른 파일로 옮기지 않고 이 파일에만 append한 뒤 커밋한다.

운영 규칙 (모기 확정 2026-08-14): 마스터가 메모를 확인·처리할 때마다 이 파일을 비운다. 처리 내역은 비움 표식 한 단락으로 남기고, 원문은 git 이력이 보존한다.

(비어 있음 — 2026-09-03 마스터 22차 비움(냐옹이 Gen 19). 처리분 1건: **sw-andi 필기노트 초안** → swatch-ops `contracts/2026-09-03-sw-andi-notebook.md`(원문 그대로) + 계약 `2026-09-03-sw-andi-grid-label.md` §0(정리본 N1~N6, 소유자 판정 ①매핑 전부 나열 ②2열 = "둘다 A"). 노트가 설계를 두 군데 고쳤다: 대표+N 대신 전부 나열, 라벨만이 아니라 그리드 밀도까지. 원문 = git 이력.)

## PR #546 과외 카드가 최종 소유자 결정 전 HEAD를 가리켜 로그인 링크 존속 여부를 반대로 설명함

- 기존 이슈: `sw-1duw`의 2026-09-08 소유자 결정·커밋 `a66e87d`
- 근거: `pr-cards/2026-09-08-pr546-email-otp-login.md:8,20,24,39-40`; `app/src/features/auth/AuthOnboardingGate.tsx:117-123`; `app/src/features/auth/AuthOnboardingGate.otp.test.tsx:109-117`
- 카드가 `d28e3c5`를 최종 HEAD로 고정하고 로그인 메일의 링크 경로가 남아 있다는 질문·샛길을 제공하지만, 현재 PR 브랜치 `a66e87d`는 코드와 링크가 같은 1회용 토큰이라 로그인 메일에서 링크를 제거했다. 2026-09-09 과외에서 실제로 낡은 HEAD의 테스트를 근거로 “링크 경로가 그대로 산다”고 설명했다가 종료 정리 중 시점 불일치를 발견했으므로, 다음 퀴즈 전에 카드의 HEAD·전체 파일 지도·링크 관련 질문을 최종 결정에 맞게 갱신하거나 역사적 콜백(계정 저장 확인 메일·기발송 메일) 범위로 명시해야 한다.
- 판독 시점 커밋 SHA: `a66e87d` (카드 고정 SHA: `d28e3c5`)
