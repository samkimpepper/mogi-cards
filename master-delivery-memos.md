# 마스터 전달용 메모 (단일 수신함)

과외 세션 발견물을 한곳에 누적하는 마스터 전달용 단일 수신함이다. 메모는 리드 카드나 다른 파일로 옮기지 않고 이 파일에만 append한 뒤 커밋한다.

운영 규칙 (모기 확정 2026-08-14): 마스터가 메모를 확인·처리할 때마다 이 파일을 비운다. 처리 내역은 비움 표식 한 단락으로 남기고, 원문은 git 이력이 보존한다.

(비어 있음 — 2026-08-17 마스터 10차 비움. 처리분 2건: 대표사진 재결정 과외 검토 메모 — ①숨김 해제 비복원의 등급 예외 ②홈 공용 fallback UAT 격리. 둘 다 판독 도장 `21ecb0a` = 최신 판독 유효 확인, swatch-ops 결정문 2026-08-17-representative-photo-policy.md **개정 1**로 전량 반영 + 모기 재확인으로 B레인 발사 게이트 해제. plan card 동기 갱신. 원문 = git 이력.)

## #520 최종 리드 카드에 진행 중 문구 2곳 잔존

근거: `pr-cards/2026-08-17-pr520-swatch-media-serving.md:35`는 최종본에서도 `왜 아직 머지 불가인가`라고 쓰고, 같은 파일 `:72`는 fix8 재판독까지 종결된 상태에서 `지금 fix7로 진행 중`이라고 쓴다. 반면 `:12`와 `:62`는 머지 전 필수 0건·보안 축 종결을 명시한다.

왜 문제인지: 카드 상단의 현재 상태와 중간의 진행 문구가 충돌해, 소유자가 fix7이 아직 도는지 또는 머지가 여전히 막혔는지 다시 판독하게 만든다. `:35`는 `fix1 당시 머지 차단 이유`, `:72`는 `fix7로 반영 완료`처럼 역사 시점을 고정하면 최종 상태를 바꾸지 않고 모순만 없앨 수 있다.

판독 시점 커밋 SHA: PR HEAD `9ec4fe3` · mogi-cards `74f72fc`

## #520 과외 결정 — self-thread 사진 중복은 머지 차단이 아니라 별도 제품 설계

모기 결정: 발색샷 계정마다 스레드를 쓰는 방식이 달라서, 지금 `발색 한 건 = 트윗 하나`인지 `스레드 전체`인지 확정하지 않는다. 이 문제를 고치려고 PR #520 코드를 다시 여는 것도 아니다.

과외에서 확인한 현재 사실:

- `swatches.source_url`은 같은 **트윗 주소**의 중복만 막는다. 서로 다른 트윗 주소 사이의 `image_urls` 중복을 막는 DB 제약은 없다.
- 답글 트윗을 미리보기하면 같은 작성자의 부모 트윗을 따라가며 부모 사진도 평탄한 `images` 목록에 합친다(`app/api/tweet-preview.ts`). 따라서 부모 트윗과 답글 트윗을 각각 등록하면, 서로 다른 `swatches` 행이 같은 사진 URL을 갖는 정상 UI 경로가 구조상 가능하다.
- 2026-08-18 QA 읽기 전용 실측에서는 서로 다른 `swatches` 행 사이에 중복된 `image_url`이 **0건**이었다. 현재 실재 데이터 문제라기보다 도달 가능한 제품 경계다.
- fix2의 `발색 스코프 + 같은 URL 생존 사본 보존`은 이런 상태가 생겨도 한 발색의 사진 제거가 무관 shade 대표를 회수하지 않게 하는 안전장치다. 후속 제품 정책과 무관하게 유지해야 한다.

후속 설계에서 결정할 질문: 답글 링크를 붙였을 때 부모 사진은 단순 문맥인가, 자동 선택할 등록 대상인가; 이미 다른 발색에 쓰인 사진을 발견하면 기존 발색 연결·명시적 별도 등록·차단 중 무엇을 제공할 것인가; 부모 사진을 허용한다면 사진별 원본 트윗 출처를 어떻게 보존할 것인가. 발색별 Storage 사본과 삭제 수명주기는 공유하지 않는 현재 경계를 유지하는 편이 안전하다.

PR 판정: 이 미결정은 #520 머지 차단물이 아니다. 현재 HEAD `9ec4fe3`, GitHub 판정 open·mergeable, migration verify·viewport·gitleaks 등 실행된 체크 성공을 재확인했다. 모기는 남은 code diff를 과외로 조금 더 읽은 뒤 머지를 판단할 예정이다. 마스터는 이 항목을 #520 fix 라운드로 되돌리지 말고 머지 후 제품 설계 후보로 분리해 달라.
