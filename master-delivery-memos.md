# 마스터 전달용 메모 (단일 수신함)

과외 세션 발견물을 한곳에 누적하는 마스터 전달용 단일 수신함이다. 메모는 리드 카드나 다른 파일로 옮기지 않고 이 파일에만 append한 뒤 커밋한다.

운영 규칙 (모기 확정 2026-08-14): 마스터가 메모를 확인·처리할 때마다 이 파일을 비운다. 처리 내역은 비움 표식 한 단락으로 남기고, 원문은 git 이력이 보존한다.

(비어 있음 — 2026-08-27 마스터 19차 비움(냐옹이 Gen 15). 처리분 3건, 판독 도장 PR #539 HEAD 5603d3a = 현재 브랜치 최신 → 판독 유효.
① **PR #539 N3 의도 delta** — 실제 정책("자식 달린 상위 분류 카테고리는 머지 양쪽 대상에서 제외, 잎끼리만")을 가역적 fail-closed 운영 규칙으로 sw-1yt fix2 계약에 반영(RPC 거부 + 화면 후보 제외 + W6 재정의), 세분화 시 재검토 주석. 영구 불변식으로 넓히지 않음.
② **예쁜필기노트TDD 첫 절 지표** — 약 2분 / 다른 줄 1개(N3). A/B 비교표 판정 칸에 기입(swatch-ops contracts/2026-08-27-sw-1yt-ab-comparison.md).
③ **sw-cep 정정** — 규칙은 canonical 묶음만 정본, CASCADE는 의도된 동작 → sw-cep 정정 종결. 머지 전 양쪽 규칙 개수 표시는 fix2에 포함.
원문 = git 이력.)

## sw-ypn 필기노트 초안

### 모기 줄 (원문 그대로)

1. 일단 홈탭에서 뭔가 관리가 되어야할것같은데(?) 굳이 프로필탭에 또 들어가서, 거기서 내 발색샷을 또 찾고 거기서 들어가서, 스크롤내리면서 찾으면서 수정까지 해야하는게 너무귀찮다옹 ㅡㅡ 에바 ㅡㅡ
2. 당연히 홈탭-내발색샷에서는 사진내용수정삭제 다 가능해야한다옹!! 공개비공개전환도 가능해야한다옹!
3. 왜냐하면 나는 홈탭은 내 아카이브! 같은 느낌이길 바라거든..
4. 근데 그러면서도 파도타기? 약간.. 아 뭔가 이것저것 구경하는 느낌도버리기싫고(욕심인가)
5. 내가 그 제품에 대해 가지고있는상태.. 뭐 위시인지, 갖고있는지 그런거. (이미 맨위에 그 정보가 있긴해. 메모도있음)
6. 그다음에 대표사진.. 그리고 4축평가(채도 탁기 이런거)에다가.. 그 아래에 발색샷 흠..
7. 아 과거에 올린 발색샷을 한눈에 보이게하는게 맞나.. 아 이건 좀 헷갈리기도하고 이번주 업계종사자분 코덕트친이랑 인터뷰하기로했으니까 좀 피드백을 받는게나을지..

### 확인된 경계·레퍼런스 답

- 소유자 확정: 홈탭 `내 발색샷`에서 사진·내용 수정, 삭제, 공개↔비공개 전환이 모두 가능해야 한다.
- 소유자 보류: 원래 제안된 제품 상세의 `내 발색샷 N개` 지름길 UI와 과거 발색샷을 한눈에 펼치는 방식은 이번 주 업계 종사자·코덕 인터뷰 뒤 결정한다. 지름길을 먼저 구현한다는 2026-08-29 결정을 현재 확정 요구로 취급하지 않는다.
- 현재 코드의 관리 동작은 표면별로 갈려 있다. 홈 `내 발색샷` 그리드는 상세 열기만 제공하고(`../swatch-v2/app/src/features/home/HomePage.tsx:292`, `HomeArchiveGrid.tsx:181`), 상세 카드에는 공개↔비공개·삭제만 있으며(`SwatchReviewCard.tsx:240`, `SwatchOwnerControls.tsx:11`), 사진·내용 수정은 프로필 `내 발색 전체`에만 있다(`ContributionsPage.tsx:120-139`). 따라서 기존 카드의 “수정·비공개·삭제가 프로필에만 있음” 전제는 현재 코드와 다르며, 이 전제로 지름길만 만들면 이미 있는 조작을 중복하면서 수정 진입의 실제 공백을 남길 수 있다.
- 홈과 탐색은 같은 `openSwatchSheet()`를 호출하고, 이 함수는 진입 출처 없이 같은 `DetailSheet` 상태를 만든다(`../swatch-v2/app/src/store/useStore.ts:496-500`, `HomePage.tsx:263`, `ExplorePage.tsx:505`). 홈은 내 아카이브, 탐색은 구경·파도타기라는 서로 다른 첫 목적을 반영하려면 별도 시트 두 벌을 전제하기보다 진입 맥락과 정보 우선순위를 먼저 결정해야 한다.
- 제품 상세의 현행 체크박스는 `내 발색만`이 아니라 `내 후기 포함`이다. 끄면 내 발색을 제외하며, 내 발색만 거르는 상태는 없다(`../swatch-v2/app/src/features/shade-detail/SwatchReviewSection.tsx:42-57,78-88`).
- `0개` 상태에서 과거 X 게시물을 기억·검색·복붙해야 하는 등록 병목도 언급됐지만 이번 노트에는 범위로 넣지 않는다. 같은 원인·영향의 열린 이슈 `sw-99x`(X API 반자동 후보 큐)가 이미 있으므로 언급만 남기고 그 이슈로 분리한다.
- 제품 코드 판독 시점: `21d552f`.
