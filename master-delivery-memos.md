# 마스터 전달용 메모 (단일 수신함)

과외 세션 발견물을 한곳에 누적하는 마스터 전달용 단일 수신함이다. 메모는 리드 카드나 다른 파일로 옮기지 않고 이 파일에만 append한 뒤 커밋한다.

운영 규칙 (모기 확정 2026-08-14): 마스터가 메모를 확인·처리할 때마다 이 파일을 비운다. 처리 내역은 비움 표식 한 단락으로 남기고, 원문은 git 이력이 보존한다.

(비어 있음 — 2026-08-15 마스터 7차 비움. 처리분 3건 라우팅 내역: ①plan card 낡음(유효) → 결정문 개정 2~4 요약·현재 게이트 상태로 카드 갱신 ②#518 PR 카드 부재는 운영 누락 아님(모기 확정: PR 본문이 승인 재료) — 과외냥이 자체 정정으로 종결, 카드 방침 카드에 명기 ③#518 HEAD에 fix1 미반영 실측(유효·중대) → 맞다, fix1은 진행 중(같은 워커, 큐 테이블 선행 + swatch_media 재번호 구조로 착수 보고 수신됨). 결정문 개정 4 문구가 완료처럼 읽히던 것을 지시·진행 중으로 정정하고 'fix1 반영·재검증 전 #518 머지 금지'를 명문화. 원문 = git 이력.)

## PR #518의 공개 자격은 media 행별이 아니라 swatch 전체 단위로 원자화해야 한다

근거: PR #518 HEAD `supabase/migrations/20260814100000_swatch_media_model.sql:135-144`의 `anon_read`가 각 행의 `status IN ('stored', 'pending_upload')`만 검사함 + 2026-08-16 과외 세션 모기 확정

현재 정책은 사진 2장 중 1장이 `stored`, 다른 1장이 `pending_download`이면 성공한 행만 공개 읽기에 남겨 반쪽짜리 발색이 노출될 수 있다. 모기는 사진별 복사·재시도는 독립적으로 유지하되 한 장이라도 표시 불가능하면 발색 전체를 격리하고 모든 사진이 `stored` 또는 안전한 `pending_upload`일 때만 묶음 전체를 공개한다고 확정했으므로, fix1에 swatch 단위 공개 판정과 부분 성공·복구 완료 전환 검증을 포함해야 한다.
