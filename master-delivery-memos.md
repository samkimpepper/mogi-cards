# 마스터 전달용 메모 (단일 수신함)

과외 세션 발견물을 한곳에 누적하는 마스터 전달용 단일 수신함이다. 메모는 리드 카드나 다른 파일로 옮기지 않고 이 파일에만 append한 뒤 커밋한다.

운영 규칙 (모기 확정 2026-08-14): 마스터가 메모를 확인·처리할 때마다 이 파일을 비운다. 처리 내역은 비움 표식 한 단락으로 남기고, 원문은 git 이력이 보존한다.

(비어 있음 — 2026-08-16 마스터 9차 비움. 처리분 1건: 과외냥이의 배치 선차감 지적 자기 정정 — 마스터의 8차 판정(낡은 판독·기수리)과 일치, code-diff 노트도 자료 성격으로 정정됨. 신규 SHA 도장 규칙(프리앰블 규칙 2)의 첫 실사용 — 판독 시점 `c19f702` 명기 확인. 추가 조치 없음. 원문 = git 이력.)

## 대표사진 숨김 해제 비복원 규칙에 등급 예외 재검토 필요

- 근거: `../swatch-ops/docs/decisions/2026-08-17-representative-photo-policy.md:30`, `../swatch-v2/docs/wiki/decisions/D-099.md:116-122`
- 왜 문제인지: 현재 결정문은 숨김 해제 시 대표를 자동 복원하지 않는다고 무조건적으로 잠갔지만, 모기는 백지 재검토에서 현재 대표가 적절한 단독 사진이면 유지하고 현재 대표가 비교발색 fallback뿐인데 다시 공개된 사진이 확실한 단독 사진이면 단독 등급을 우선해야 한다고 구분했다. B레인에서는 이를 과거 자리의 복원이 아니라 후보 풀 재진입 후 등급 업그레이드로 표현해야 하며, 후속 확인에서 단독 판정 신호는 D-099의 기존 `auto_single` 조건(발색 등록 전체에 서로 다른 shade 1개만 매핑)을 그대로 쓰고 그보다 복잡한 사진 내용 판별은 코드 복잡성 때문에 보류한다고 모기가 확정했다.
- 판독 시점 커밋 SHA: `21ecb0a` (`swatch-ops`)

## 내 화장품 공용 대표사진 fallback은 유지하되 UAT 번복 가능하게 격리

- 근거: `../swatch-v2/docs/wiki/decisions/D-118.md:101-104`, `../swatch-ops/docs/decisions/2026-08-14-swatch-media-self-preservation.md:58-62`
- 왜 문제인지: 현재 계약은 `내 화장품`에서 내 발색샷이 없으면 공용 대표사진과 `내 발색 없음` 표시를 쓰지만, 모기는 백지 재검토에서 출처를 밝혀도 개인 홈에 타인 사진이 보이는 감각을 실제 화면 전에는 확신하기 어렵다고 확인했다. B레인은 이 fallback을 다른 홈 표시 로직과 결합하지 말고 UAT에서 불편하면 색상칩으로 쉽게 번복할 수 있는 갈래로 격리하되, 초판은 공용 대표사진 + 명시적 `공용 대표 · 내 발색 없음` 표기를 유지한다.
- 판독 시점 커밋 SHA: `545bf66` (`swatch-v2`), `21ecb0a` (`swatch-ops`)
