---
date: 2026-08-27
session_number: 1
slug: pr539-pretty-notebook-tdd-first-review
scope: user-messages-and-final-answers
codex_session_key: f433f80bbc1f729d
---

# 2026-08-27 세션 01 — PR #539 예쁜필기노트TDD 첫 구현 검토

## Turn 01

Codex turn key: `b5b80308afcd8652`

### 모기

~~~text
안녕 난 모기야 ㅋ 넌 과외냥이라옹 ㅋ
~~~

### 과외냥이

~~~text
맞다옹 ㅋ 난 모기 전용 과외냥이야.

학습 인수인계: 2026-08-25-learning-observation-cumulative-review.md 읽음  
읽은 문서 상태판: http://127.0.0.1:8766

- PR 승인 카드: 미검토 문서 없음
- 설계·SPEC 카드: 미검토 문서 없음
- 코드 학습 노트: 미검토 문서 없음

지금은 미검토 카드가 없네. 새 카드나 궁금한 PR을 지정해줘도 돼. 어느 카드부터 볼까?
~~~

## Turn 02

Codex turn key: `230803ef41f33ea8`

### 모기

~~~text
앗 마스터메모 보자옹! 새로운 제도가 생겼다옹
~~~

### 과외냥이

~~~text
맞다옹. 새 제도는 **예쁜필기노트TDD**야.

모기가 먼저 사용자 말로 5~10줄짜리 기대 동작을 쓰면, 워커가 각 줄을 실패하는 테스트로 만든 뒤 구현하고, PR 첫머리에서 줄마다 통과 여부를 보여주는 방식이야. 모든 예외를 모기가 쓸 필요는 없고, 권한·동시 실행 같은 나머지는 워커 몫으로 분리해. [정본 런북](</home/mogi/code/swatch/swatch-ops/docs/runbooks/pretty-notebook-tdd.md>)과 [마스터 메모](/home/mogi/code/swatch/mogi-cards/master-delivery-memos.md:11) 둘 다 확인했어.

첫 대상은 `sw-1yt` 어드민 머지고, 나는 대신 문장을 쓰지 않고 질문만 할게.

첫 줄부터 모기 말로 적어보자옹:

**“내가 어드민에서 어떤 중복 항목을 무엇으로 합치면…”**

모기는 어떤 실제 예시로 시작하고 싶어?
~~~

## Turn 03

Codex turn key: `b6016bef2eaaa867`

### 모기

~~~text
중복항목? 흠흠. 냐옹이(마스터)는 예시를 맥 코스메틱을 맥으로.. 라고 예시들긴했는데 브랜드 말하는건가
~~~

### 과외냥이

~~~text
응, 그 예시는 **브랜드 합치기**를 말해.

- 합쳐져서 사라질 쪽: `맥 코스메틱`
- 남을 쪽: `맥`
- 합친 뒤: `맥 코스메틱` 아래 제품라인들이 전부 `맥` 아래로 옮겨지고, `맥 코스메틱` 항목은 사라져야 해.

여기서 “중복 항목”은 완전히 같은 글자라는 뜻보다, **같은 대상을 오타나 다른 표기로 두 번 등록한 항목**이라는 뜻이야. 이 기능은 브랜드뿐 아니라 카테고리와 제품라인에도 쓰이고, 마스터가 든 예시는 그중 브랜드 사례야. [마스터 메모](/home/mogi/code/swatch/mogi-cards/master-delivery-memos.md:25)

그럼 첫 장면은 `내가 어드민에서 “맥 코스메틱”을 “맥”으로 합치면`으로 잡으면 될까?
~~~

## Turn 04

Codex turn key: `238d12255525d683`

### 모기

~~~text
좋다옹 그리고 방금 흠... 중복 항목이 어떤 테이블 말하는건지 ㄴ명시했다면 좋았을듯!!! (하네스규칙수정하라는거아님 그냥 기록용..) ㅇㅇ 맞다옹. 근데 그거말고 뭔가 더 생각안나는데 고려해야될케이스있나?
~~~

### 과외냥이

~~~text
맞아. 기록 포인트는 이거야: **`중복 항목`이라고만 해서 대상 데이터가 생략됐고, 모기가 브랜드·카테고리·제품라인 중 무엇인지 다시 확인해야 했음.** 하네스 규칙 변경 제안은 아니고, 이번 설명 입력에서 빠진 대상으로 남겨둘게.

실제 대상 테이블은 `brands`, `categories`, `product_lines` 세 개야. 방금 예시는 `brands`의 두 행을 합치는 경우고, `맥 코스메틱`을 가리키던 `product_lines.brand_id`를 `맥`으로 바꾼 뒤 `맥 코스메틱` 브랜드 행을 숨김 처리하는 흐름이야. [AdminMergeRows.tsx](/home/mogi/code/swatch/swatch-v2/app/src/features/admin/routes/AdminMergeRows.tsx:37)

필기노트에서 추가로 생각할 핵심 장면은 이 정도야.

- 정상 완료: 제품라인들이 남길 브랜드로 모두 옮겨지고 중복 브랜드가 사라짐.
- 중간 실패: 옮기기와 삭제 중 하나라도 실패하면 전부 합치기 전 상태로 돌아감.
- 잘못된 카테고리 합치기: 부모 카테고리를 자기 자식 쪽으로 합치려 하면 거부되고 아무것도 바뀌지 않음.
- 화면 안내: 실패하면 SQL로 직접 수리하라는 경고가 사라지고, 성공인지 거부인지 사람이 알아볼 수 있게 표시됨.
- 안 쓰는 것: 권한 없는 사람·동시 실행·이미 지워진 항목 같은 세부 예외는 워커에게 맡김.

브랜드·카테고리·제품라인을 전부 별도 사례로 쓸 필요는 없어. 브랜드 정상 완료 하나와 실패 복구 하나, 카테고리 고리 방지 하나면 제도의 핵심이 잡혀.

그럼 다음 한 줄을 모기 말로 만들어보자. **`맥 코스메틱 → 맥` 합치기가 중간에 실패하면 화면과 목록이 어떻게 남아 있으면 좋겠어?**
~~~

## Turn 05

Codex turn key: `de7552f652792ac9`

### 모기

~~~text
중간에 실패하면 그 중간의 하다만 상태로 확정되는게 아니라 아예 하기 전 상태가 되어야한다고 생각한다옹
~~~

### 과외냥이

~~~text
좋아. 이건 필기노트의 **중간 실패 장면**으로 그대로 보존하면 돼.

모기가 말한 “하기 전 상태”를 정확히 풀면:

- `맥 코스메틱` 아래 제품라인은 하나도 옮겨지지 않음
- `맥 코스메틱` 브랜드도 그대로 남아 있음
- `맥` 쪽에도 일부만 넘어간 흔적이 없음

즉, 합치기가 전부 끝나거나 전부 취소되는 두 상태만 허용하는 거야.

다음은 카테고리 장면이야. **`립` 카테고리를 그 자식인 `틴트`로 합치려고 했을 때, 모기는 화면에서 어떻게 막혔다는 걸 알 수 있으면 좋겠어?**
~~~

## Turn 06

Codex turn key: `2082c3d9d9821b2d`

### 모기

~~~text
애초에 부모를 자식에다가 머지한다는 경로가.. 존재하지않았으면.. 화면에서.... 물론 DB에서도 차단해야겠지만
~~~

### 과외냥이

~~~text
좋아. 모기가 원하는 건 두 겹이네.

- 화면: `립`을 합칠 대상을 고를 때 자식인 `틴트`가 선택지에 아예 나오지 않아야 함.
- 서버·DB: 화면을 우회해 직접 요청하더라도 합치기를 거부하고, `립 → 틴트` 관계를 포함한 카테고리 트리는 전혀 바뀌지 않아야 함.

화면에서 선택지를 숨기는 건 실수를 예방하고, DB 차단은 잘못된 요청이 실제 데이터까지 망가뜨리지 못하게 막는 역할이야.

이제 현재 확인창에 있는 **“실패하면 SQL로 수동 정리해야 한다”**는 안내를 보자. 새 방식에서는 합치기 전 확인창에 무엇을 보여주면 좋겠어?
~~~

## Turn 07

Codex turn key: `2da06661d669099f`

### 모기

~~~text
실패하면 sql로 수동정리해야한다는게 약간 내입장에서좀 어이없게 느껴짐(이유는 모르겠음)새 방식에서는 합치기전에... 진짜로 합치려는 브랜드가 같은 추상적? 대상을 가리키는게 맞는지 알수있는 정보가 있으면 좋을텐데 그게뭘까. 브랜드에 속한 제품라인이나 쉐이드 몇개라던가
~~~

### 과외냥이

~~~text
그 어이없음은 자연스러워. 어드민의 정식 기능을 눌렀는데, 기능이 실패하면 사용자가 별도 도구인 SQL 편집기로 들어가 데이터가 어디까지 바뀌었는지 찾아서 직접 수리하라는 뜻이거든. 안전장치가 해야 할 책임을 사용자에게 넘긴 셈이야. 실제 확인창도 남길 행과 제거할 행의 `id`만 보여주고 수동 정리를 경고해. [AdminMergeRows.tsx](/home/mogi/code/swatch/swatch-v2/app/src/features/admin/routes/AdminMergeRows.tsx:158)

그리고 모기가 말한 핵심은 **두 행이 같은 현실의 브랜드를 가리키는지 확인할 근거**야. 지금 화면은 한글명·영문명·내부 식별자 정도와, 옮겨질 제품라인의 개수만 보여줘. 제품라인 이름이나 쉐이드 예시는 안 보여줘. [AdminMergeRows.tsx](/home/mogi/code/swatch/swatch-v2/app/src/features/admin/routes/AdminMergeRows.tsx:43) [AdminMergeRows.tsx](/home/mogi/code/swatch/swatch-v2/app/src/features/admin/routes/AdminMergeRows.tsx:302)

합치기 전에는 두 브랜드를 나란히 놓고 이런 정보가 있으면 판단하기 좋겠어.

- 한글명·영문명
- 공식 사이트나 국가처럼 브랜드 자체를 식별할 정보
- 각 브랜드에 속한 제품라인 개수와 대표 이름 몇 개
- 필요하면 쉐이드 개수와 대표 이름 몇 개
- 어느 쪽이 남고 어느 쪽이 사라지는지
- 합친 뒤 몇 개의 제품라인과 쉐이드가 어디로 옮겨지는지

전부 펼칠 필요는 없고, **제품라인 몇 개와 쉐이드 몇 개를 예시로 보여주는 것**이 특히 좋아 보여. 이름만 비슷하지만 전혀 다른 브랜드를 잘못 합치는 사고도 잡을 수 있으니까.

모기가 합치기 직전에 꼭 보고 싶은 최소 정보는 `두 브랜드 이름 + 제품라인 개수와 예시 + 쉐이드 개수와 예시` 정도일까? 공식 사이트 같은 정보도 필요할까?
~~~

## Turn 08

Codex turn key: `4b460adcd98830ad`

### 모기

~~~text
오우 너무좋아. 완전 짱인데? 헐. 맞다옹!! 저정도면 충분하다옹
~~~

### 과외냥이

~~~text
좋아. 그러면 합치기 전 확인 정보는 이걸로 확정이네.

**두 브랜드의 이름과 식별 정보, 각 브랜드에 속한 제품라인·쉐이드의 개수와 예시를 나란히 보고, 정말 같은 브랜드인지 판단할 수 있어야 함.**

이제 정상 완료 장면만 모기 말로 잡아보자. `맥 코스메틱`을 `맥`으로 합친 뒤, 목록과 그 아래 제품라인들은 정확히 어떻게 보여야 해?
~~~

## Turn 09

Codex turn key: `22dbfeb1b1ef0ae6`

### 모기

~~~text
맥 코스메틱인 제품라인들이 맥으로 옮겨져야됨 fk가.. 아니 근데생각해봤는데 흠. 맥 코스메틱에도 같은 제품라인, 맥에도 같은 제품라인 있는걸 생각못했어 이런 자식들(?)까지 머지작업에 포함하면 너무 범위가 커지겠지..? 내가 말하는거는 맥코스메틱에 맥시멀 실키 매트 립스틱, 맥에 맥시멀 실키 매트 립스틱(물론 pk는 다르겠지)이 있다면..? 그럼 얘네도 합쳐지면 좋을텐데 제품라인에 발색샷이 뭐가 달렸는지까지 추적하는거는 약간 코드구현관점에서는 안티패턴같기도..
~~~

### 과외냥이

~~~text
결론부터 말하면, **범위는 커지지만 발색샷까지 일일이 추적해야 하는 안티패턴은 아니야.** 오히려 모기가 실제로 빠진 사용자 장면을 발견했어.

현재 DB는 같은 브랜드 아래에서 `카테고리 + 제품라인 이름`이 같은 활성 제품라인 두 개를 허용하지 않아. 그래서:

1. `맥 코스메틱` 아래에 `맥시멀 실키 매트 립스틱 A`
2. `맥` 아래에 같은 이름·카테고리의 `맥시멀 실키 매트 립스틱 B`
3. 브랜드만 `맥 코스메틱 → 맥`으로 옮기려 함
4. 제품라인 A와 B가 충돌해서 브랜드 머지 전체가 실패하고 원래 상태로 돌아감

이렇게 돼. 원자성 덕분에 반쯤 망가지지는 않지만, 브랜드를 합치는 데 성공하지도 못해. [제품라인 고유성 마이그레이션](/home/mogi/code/swatch/swatch-v2/supabase/migrations/20260620020000_product_line_identity_unique.sql:28)

제품라인 A와 B도 합친다면, A 아래의 쉐이드들이 B를 가리키도록 옮기고 A를 숨김 처리하면 돼. 발색샷 같은 더 아래 데이터는 쉐이드의 고유번호를 계속 가리키므로, 쉐이드 자체의 고유번호를 보존하면 사진들을 하나씩 옮길 필요는 없어. [제품라인 합치기 조사](/home/mogi/code/swatch/swatch-v2/docs/wiki/postmortem/temp/20260711-samkimpepper-product-line-split-merge-assumption-correction.md:147)

위험한 건 **이름이 같다는 이유만으로 제품라인을 몰래 자동 합치기**야. 안전한 사용자 경험은 둘 중 하나야.

- 작은 범위: 충돌하는 제품라인 쌍을 보여주고 브랜드 머지를 막은 뒤, 제품라인을 먼저 따로 합치게 함.
- 큰 범위: 충돌하는 제품라인 쌍과 그 아래 쉐이드 예시를 보여주고, 모기가 같은 제품라인이라고 확인한 쌍만 브랜드와 함께 한 번에 합침.

기존 `sw-1yt` 계약에는 “브랜드 아래 동일 제품라인이 양쪽에 있을 때”가 명시돼 있지 않아. 이건 결과 메모에 새로 발견한 계약 공백으로 같이 남길게.

모기 입장에서는 어느 쪽이 좋아? **충돌하면 먼저 제품라인을 따로 합치게 하기**, 아니면 **확인한 제품라인 쌍까지 브랜드와 한 번에 합치기**?
~~~

## Turn 10

Codex turn key: `5cb2b7fa63a0b375`

### 모기

~~~text
후자는 좀 아닌것같아 그게 빈대잡으려다가 초가삼간 다태우는짓처럼 느껴진다옹
~~~

### 과외냥이

~~~text
나도 그 판단이 맞다고 봐. 브랜드 하나를 합친다는 동작이 이름이 비슷한 제품라인과 그 아래 데이터까지 연쇄적으로 판단·통합하기 시작하면, 사용자가 승인하지 않은 합치기가 너무 많이 숨어들어가.

경계를 이렇게 두면 깔끔해.

- 브랜드 아래 충돌하는 제품라인이 없으면 브랜드만 합침.
- 양쪽에 같은 제품라인 후보가 있으면 브랜드 머지를 시작하지 않음.
- 화면에서 충돌한 두 제품라인과 각각의 쉐이드 개수·예시를 보여줌.
- 모기가 제품라인 머지 화면에서 둘을 별도로 확인해 합침.
- 그다음 브랜드 머지를 다시 시도함.
- 차단된 동안에는 브랜드·제품라인·쉐이드가 하나도 바뀌지 않음.

즉, **자동 연쇄 합치기는 하지 않되, 왜 브랜드를 지금 합칠 수 없는지는 사람이 판단할 수 있게 보여주는 방식**이야.

이 동작이면 모기가 원하는 것과 맞아? 충돌 화면에는 “이 제품라인들을 먼저 합친 뒤 다시 시도해줘”라는 다음 행동까지 안내하면 될까?
~~~

## Turn 11

Codex turn key: `dd45fcce68556130`

### 모기

~~~text
ㅇㅇ 제품라인 합치는 것에 대한 정책은 이 pr에선 포함안하고싶다옹
~~~

### 과외냥이

~~~text
좋아. 그러면 이번 PR에서는 **브랜드 머지 중 제품라인 충돌을 발견하면 안전하게 중단하고, 어떤 제품라인이 충돌했는지만 보여주는 데서 끝**내자. 어떤 제품라인끼리 합칠지, 합친다면 쉐이드를 어떻게 다룰지는 별도 정책으로 넘기고.

다만 하나만 구분해야 해. 기존 `sw-1yt` 계약은 브랜드·카테고리뿐 아니라 **현재 존재하는 제품라인 머지 기능도 한 번에 성공하거나 전부 취소되도록 바꾸는 작업**까지 포함해. 이건 새 제품라인 합치기 정책을 만드는 것과는 달라.

모기가 빼고 싶은 건 **브랜드 머지에서 충돌한 제품라인을 어떻게 합칠지 정하는 새 정책**이고, 기존 제품라인 머지 기능의 안전성 개선은 이번 PR에 그대로 두는 게 맞아?
~~~

## Turn 12

Codex turn key: `8c22046f44d0a9cc`

### 모기

~~~text
아 그래???! 그것까지 포함인줄 몰랐다옹 ㅜㅜ 그러면 흠. 흠... 브랜드 머지에서 충돌한 제품라인말인데 이거 브랜드 머지를 할때, 제품라인이 충돌한다는것기준도 정해야하지않냐옹?
~~~

### 과외냥이

~~~text
맞아. **충돌 기준은 반드시 정해야 해.** 다만 이번 PR에서 “같은 실제 제품라인인가?”까지 추론하려 하면 다시 범위가 커지니, **현재 DB가 함께 존재하지 못하게 막는 객관적인 조건**만 쓰는 게 좋아 보여.

현재 활성 제품라인은 같은 브랜드 아래에서 다음 중 하나면 기술적으로 충돌해.

- 카테고리가 같고 제품라인 이름도 같음  
  이름은 앞뒤 공백과 대소문자를 무시해. 예를 들어 `맥시멀 립스틱`과 `  맥시멀 립스틱 `은 같게 봐. [제품라인 identity 인덱스](/home/mogi/code/swatch/swatch-v2/supabase/migrations/20260620020000_product_line_identity_unique.sql:28)
- URL에 쓰는 제품라인 식별 문자열인 `slug`가 같음. [초기 스키마](/home/mogi/code/swatch/swatch-v2/supabase/migrations/20260416063901_init_core.sql:93)

따라서 브랜드 머지 화면은 `맥 코스메틱`의 제품라인을 `맥`으로 옮겼을 때 위 조건에 걸리는 쌍이 있는지 먼저 보고:

- 충돌 쌍이 없으면 브랜드 머지를 진행
- 충돌 쌍이 있으면 시작 전에 중단
- 양쪽 제품라인의 이름·카테고리·쉐이드 예시를 보여줌
- 어떤 제품라인끼리 실제로 합칠지는 이번 PR에서 결정하지 않음
- 차단 중에는 아무 데이터도 바꾸지 않음

으로 경계를 잡을 수 있어.

이름이 조금 다르지만 사람이 보면 같은 제품인 경우까지 찾아내는 건 별도 제품라인 중복 정책이야. 이번 PR의 “충돌”은 **같은 브랜드가 된 뒤 DB가 동시에 둘 수 없는 제품라인 쌍**으로만 정의하면 될까?
~~~

## Turn 13

Codex turn key: `9cf1b4cd5bbc76ca`

### 모기

~~~text
ㅇㅇ 그렇게정의하자옹. 이름이나 slug가 좀 다른데, 현실?관점?에서 같은것을 찾아내느건 시스템역할이 아니라 유저개입이 필요하다고 본다옹
~~~

### 과외냥이

~~~text
좋아. 경계가 정확히 잡혔어.

- 시스템 역할: 같은 브랜드 아래에서 DB 규칙상 함께 존재할 수 없는 제품라인 쌍을 객관적으로 찾아 브랜드 머지를 차단하고, 판단에 필요한 정보를 보여줌.
- 사용자 역할: 이름이나 `slug`가 달라도 현실에서 같은 제품라인인지 판단하고, 필요하면 별도의 제품라인 머지를 직접 선택함.
- 하지 않을 일: 시스템이 이름 유사도만 보고 실제로 같은 제품이라고 추측해서 자동으로 합침.

이렇게 하면 “데이터 충돌 탐지”와 “현실의 같은 제품인지 판단”이 섞이지 않아.

이제 필기노트 마지막의 **안 쓰는 것**만 정하면 돼. 권한 없는 사용자, 두 관리자의 동시 실행, 이미 삭제된 행 같은 기술적 예외는 모기가 시나리오를 쓰지 않고 워커가 추가 테스트하는 영역으로 넘기면 될까?
~~~

## Turn 14

Codex turn key: `4394dcea450b8ddd`

### 모기

~~~text
이거 애초에 어드민기능이니까 권한없는사용자는 당연 어드민 제외한 계정이고. 두관리자 동시실행... 근데 기니피그(나 말고 관리자 다른한분)는 QA는 참여 아예? 안하신다고 보면되어서.. 동시성테스트 빡세게하는것에 비용많이 들일필요는 없을것같기는해... 이미삭제된행? 이건 뭐말하는거냐옹
~~~

### 과외냥이

~~~text
`이미 삭제된 행`은 이런 상황이야.

1. 모기가 머지 화면을 열어서 `맥 코스메틱`과 `맥`을 선택함.
2. 그 화면을 오래 열어둔 사이, 다른 작업이나 예전 탭에서 `맥 코스메틱`이 삭제됨.
3. 지금 탭에는 옛 목록이 남아 있어서 머지 버튼을 누름.
4. 서버에는 이제 존재하지 않거나 이미 삭제 표시된 행의 번호가 전달됨.

브랜드와 제품라인은 실제 행을 지우기보다 `is_deleted = true`로 숨기고, 카테고리는 행 자체를 지우는 현재 방식이라 둘 다 생길 수 있어. [AdminMergeRows.tsx](/home/mogi/code/swatch/swatch-v2/app/src/features/admin/routes/AdminMergeRows.tsx:43)

이때 서버는 “이미 삭제됐거나 찾을 수 없다”고 거부하고 아무것도 바꾸지 않으면 돼. 기존 계약에도 이 방어는 들어 있어. [sw-1yt 계약](/home/mogi/code/swatch/swatch-ops/contracts/2026-08-26-sw-1yt-admin-merge-rpc.md:14)

동시 실행도 꼭 두 관리자가 동시에 누르는 경우만 뜻하지는 않아. 같은 관리자가 버튼을 두 번 누르거나, 느린 네트워크 때문에 요청이 재전송되는 경우도 있어. 다만 모기가 말한 운영 규모라면 이걸 필기노트의 핵심 장면으로 쓰거나 과도한 동시성 시험에 비용을 들일 필요는 없어 보여.

그러면 `안 쓰는 것`은 이렇게 구분하면 될까?

- 비관리자 요청 차단
- 중복 요청이나 드문 동시 실행
- 이미 삭제됐거나 오래된 화면의 행
- 그 밖의 DB 예외

이건 워커가 기본 안전 테스트로 맡되, 무거운 동시성 시험까지 요구하지 않는 거야. 맞아?
~~~

## Turn 15

Codex turn key: `f09ffb3f529b6a96`

### 모기

~~~text
고민을 해봤다옹. 좀 의식의 흐름이라옹.. 일단 중간에 맥 코스메틱이 삭제되어서 머지를 할수없는것이, 관리자의 의도에 어긋난흐름으로 느껴진다옹... 그럼 관리자는 맥 코스메틱을 다시 살리고 나서 머지를 시도하는것이 뭔가 약간 있을법하고 자연스럽달까? (아닐수도. 다른예외케이스 있을수도.) 그치만 인간관점에서 자연스럽게한다치고 is_deleted=true인걸 억지로 머지해버리는건 약간. 규칙?위반?같이느껴짐 최종적으로 결론.. 삭제된행 머지 시에는 거부가 맞는것같다옹
~~~

### 과외냥이

~~~text
결론 좋아. **삭제된 행의 머지는 거부하는 게 맞아.**

삭제는 관리자가 이미 내린 별도의 상태 변경이야. 머지가 `is_deleted = true`인 행을 억지로 사용하면, 삭제를 몰래 취소하거나 삭제된 대상을 새 관계에 끌어들이는 셈이 돼. 특히 남길 브랜드가 삭제된 상태라면 제품라인들을 보이지 않는 브랜드 아래로 옮길 수도 있어.

자연스러운 흐름은 이거야.

- 선택한 두 브랜드 중 하나가 삭제됐으면 머지를 시작하지 않음.
- 어느 브랜드가 삭제돼 사용할 수 없는지 알려줌.
- 목록을 새로고침해 현재 상태를 보여줌.
- 관리자가 정말 그 브랜드를 사용하려면 먼저 명시적으로 복구함.
- 복구한 뒤 다시 머지를 시도함.
- 거부된 동안에는 어떤 데이터도 바뀌지 않음.

이제 필기노트 재료가 충분히 모였어. 내가 문장을 대신 만들면 안 되니, 모기가 자기 말로 5~10줄을 한 번에 써보자. 다음 키워드만 참고하면 돼:

`정상 브랜드 머지 / 합치기 전 판단 정보 / 중간 실패 원복 / 제품라인 충돌 차단 / 현실에서 같은 제품 판단은 사용자 몫 / 카테고리 자식 선택 차단 / 삭제된 행 거부 / SQL 수동 정리의 부자연스러움 / 워커에게 맡길 예외`
~~~

## Turn 16

Codex turn key: `5a034c9cf9de750e`

### 모기

~~~text
아 네가 문장대신만들면안되는거냐옹..? 아... 아.. 근데 예쁜노트필기TDD 첫번째시간이라서 약간 힘들다옹. 냐옹이한테 허락받고올까?ㅠㅠ
~~~

### 과외냥이

~~~text
아니야, 모기 혼자 처음부터 예쁜 문서 형태로 완성하라는 뜻은 아니야. **내용과 판단을 내가 대신 만들어주면 안 된다는 뜻**이고, 나는 한 줄씩 질문하고 모기가 말한 문장을 그대로 받아 적을 수 있어.

지금까지 모기가 이미 핵심 문장들을 거의 다 말했어. 내가 새 문장을 보태지 않고:

1. 모기가 실제로 말한 문장만 고름
2. 원래 말투 그대로 5~10줄로 놓음
3. 빠진 부분만 한 줄씩 질문함
4. 모기가 최종 원문을 보고 `ㅇㅇ`으로 확정함

이렇게 하면 규칙 안에서 할 수 있어. 냐옹이에게 다시 허락받으러 갈 필요 없어. 내가 문장을 매끈하게 고쳐 쓰거나 내용을 대신 결정해주길 원할 때만 별도 허락이 필요해.

지금까지 모기가 말한 문장들만 그대로 모아서 먼저 보여줄까?
~~~

## Turn 17

Codex turn key: `154f95b91f0915b5`

### 모기

~~~text
아니 매끈하게 고쳐쓰지말고 걍 내가 채팅으로 말해준거를 네가 모아가지고 마스터한테 전달할 테스트시나리오를 전달하면 안되냐옹?
~~~

### 과외냥이

~~~text
응, 그렇게 전달하면 돼. 이미 완료했어.

- 모기가 채팅에서 말한 문장 10줄을 다듬지 않고 [마스터 메모](/home/mogi/code/swatch/mogi-cards/master-delivery-memos.md:46)에 수합
- 제품라인 충돌의 기술 판독과 `sw-1yt` 계약 공백은 원문과 분리
- 기존 제품라인 직접 머지의 원자성 개선까지 제외할지는 `미확인 경계`로 표시
- 커밋: `3d79448`
- 중복 확인 파일럿도 예정된 3회를 채워 기존 종료 규칙대로 종료: `601af30`
- push는 하지 않았고, 대화 원문 훅 파일들은 커밋에 섞지 않았어.

이제 마스터가 메모를 회수하면 돼. 기존 제품라인 직접 머지의 안전성 개선을 이번 PR에 남길지는 지금 더 정해볼까, 마스터에게 확인 경계로 넘길까?
~~~

## Turn 18

Codex turn key: `71f9fbc879619530`

### 모기

~~~text
아 그 안전성 개선도 이번에 같이해도 무방하다고생각한다옹!
~~~

### 과외냥이

~~~text
확정해서 마스터 메모에 반영했어.

- 포함: 기존 제품라인 직접 머지의 원자성·안전성 개선
- 제외: 브랜드 머지 중 충돌한 제품라인을 자동으로 연쇄 병합하는 새 정책
- 커밋: `513830f`
- push는 하지 않았어.

이제 마스터가 오해 없이 계약을 보강할 수 있어.
~~~

## Turn 19

Codex turn key: `2729af1dd3bbf816`

### 모기

~~~text
지금 세션종료일지 남기지말고 워커가 우리 계약대로 구현하길 기다렸다가, pr 올라오면 그때 다시 검토하는게 낫겠지?
~~~

### 과외냥이

~~~text
응, 그게 나아. 지금은 **세션 종료가 아니라 구현 대기**로 두자.

- 종료 관찰일지 작성 안 함
- `SCRATCHPAD.md` 정리 안 함
- 상태값 변경 안 함
- 이미 커밋한 마스터 메모를 인수인계 지점으로 사용

PR이 올라오면 그때 예쁜필기노트 원문과 테스트 대응, 제품라인 충돌 차단, 직접 제품라인 머지의 원자성까지 실제 diff로 다시 확인하자. 지금은 여기서 멈춰둘게.
~~~

## Turn 20

Codex turn key: `b15ce8a9f6ef1119`

### 모기

~~~text
아 내가 컨텍스트 압축을 너무 걱정하나? 그래도 jsonl도 있고 원문도 따로 저장되고있으니까 괜찮겠지..?!
~~~

### 과외냥이

~~~text
응, 걱정할 만한 부분이지만 **지금 상태라면 압축 때문에 흐름을 잃을 가능성은 낮아.**

방금 실물로 확인했어.

- 현재 세션 원문이 [raw 파일](/home/mogi/code/swatch/mogi-cards/raw/2026-08-27-01-session.md)에 Turn 20까지 이어지고 있음
- 모기의 시나리오와 범위 결정은 git 커밋 `3d79448`, `513830f`에 보존됨
- 마스터도 이미 `e92785c`에서 “예쁜필기노트 1호 계약 반영·delta 흡수”로 회수했음
- JSONL도 별도 복구 경로로 남아 있음

native compaction은 현재 대화 문맥을 요약할 수 있지만 파일과 git 이력은 건드리지 않아. 정확한 말투가 필요하면 raw 원문을, 확정된 계약이 필요하면 마스터 메모와 git 이력을 다시 읽으면 돼.

유일한 작은 주의점은 현재 raw 파일이 아직 미커밋이라는 거야. 그래도 디스크에는 있고, 중요한 결정은 이미 커밋·마스터 회수까지 끝났으니 **PR을 기다리는 동안 세션 종료일지를 억지로 만들 필요는 없어.** 압축이 와도 PR이 올라오면 원문과 계약부터 다시 읽고 이어가면 된다옹.
~~~

## Turn 21

Codex turn key: `fbdf233c1feb6911`

### 모기

~~~text
좋다옹 이제 539 pr 올라왔다옹 이게 우리의 예쁜필기노트TDD를 통한 구현이라옹 마스터 냐옹이가 이렇게 당부했다옹 읽을 때 지표 두 개만 챙겨달라옹:
  1. 첫 절 읽고 "뭐가 바뀌었는지 안다"까지 체감 시간
  2. "내가 쓴 줄과 다르게 됐다" 싶은 줄 몇 개(있으면 어느 N번)

암튼 나는 스크래치패드부터 쓰겠다옹
~~~

## Turn 22

Codex turn key: `950687b6c26c82bd`

### 모기

~~~text
왜이러냐옹 ㅠㅠ
~~~

### 과외냥이

~~~text
아이고, 방금 응답 턴이 중간에 강제 중단됐어. 정확한 원인은 나도 확인할 수 없지만, **PR 판독이나 파일 변경은 시작하기 전이어서 망가진 건 없어.**

모기는 예정대로 PR #539 첫 절부터 읽고 `SCRATCHPAD.md`에 적어줘. 나는 아직 PR 내용이나 스크래치패드를 선행 판독하지 않고 기다릴게.

이번에 챙길 건 두 개만 기억해둘게.

1. 첫 절을 읽고 “뭐가 바뀌었는지 안다”까지의 체감 시간
2. 모기가 쓴 줄과 다르게 구현됐다고 느낀 번호와 개수

다 읽고 판독해달라고 부르면 그때 전체 메모를 묶어서 막힘 지도부터 보여줄게.
~~~

## Turn 23

Codex turn key: `fed1c528fa406155`

### 모기

~~~text
스크래치패드 다 썼다옹!
~~~

### 과외냥이

~~~text
메모는 세 묶음으로 보여.

1. 카테고리 트리와 사이클

- 부모를 자식으로 합치는 것뿐 아니라 반대 방향까지 다룬 점이 좋았음
- 부모 `립`은 직접 데이터가 달리기보다 분류 역할에 가까운 것 같다는 연결
- 여기서 말하는 `사이클`이 정확히 무엇인지는 막힘

2. 머지 규칙의 주인

- `화이트리스트 밖 표 이름 거부`에서 화이트리스트가 무엇인지
- 클라이언트의 `fkRefs`, `softDelete` 목록을 삭제했다는 뜻
- 삭제했다면 규칙이 어디로 갔는지, 단순 하드코딩은 아닌지

3. 삭제 방식과 머지 뒤 상태

- 카테고리는 hard delete, 브랜드·제품라인은 soft delete인 이유
- 이 차이가 코드상 안티패턴인지
- 브랜드를 합친 뒤 사라지는 브랜드 행은 실제 삭제되는지, 삭제 표시만 남는지
- 제품라인의 `brand_id`가 옮겨졌다면 옛 브랜드 행이 남아도 괜찮은지

`↔` 충돌 표시는 없고, “내가 쓴 줄과 다르게 됐다”는 명시적 표식도 현재 메모에는 없어. 첫 항목은 오히려 기대보다 좋았다는 연결로 보여.

추천 순서는 **1번 사이클 → 2번 규칙의 주인 → 3번 삭제 방식**이야. 이 묶음과 순서대로 볼까?
~~~

## Turn 24

Codex turn key: `537f04fef0d1b036`

### 모기

~~~text
좋다옹! 그리고 2번 규칙의 주인 설명할때는 너무 깊게보진 말아달라옹..
~~~

### 과외냥이

~~~text
좋아. 2번은 얕게 보기로 고정했어. 먼저 1번의 실제 변경만 보자.

PR #539 — 실제 변경 접촉  
판독 범위: `6e0da7a`(dev base) → `5603d3a`(fix1 포함 HEAD)

전체 변경 파일:

- `app/src/data/repos/adminMergeRepo.ts` +105/-0, 신규
- `app/src/features/admin/routes/AdminMergeRows.test.tsx` +200/-0, 신규
- `app/src/features/admin/routes/AdminMergeRows.tsx` +209/-129
- `app/src/features/admin/routes/adminMergeUtils.test.ts` +36/-0, 신규
- `app/src/features/admin/routes/adminMergeUtils.ts` +52/-0, 신규
- `supabase/POLICY-SNAPSHOT.md` +3/-1
- `supabase/migrations/20260826170000_rpc_admin_merge_rows.sql` +393/-0, 신규
- `supabase/tests/admin_merge_rows_verify.sql` +500/-0, 신규

카테고리 후보를 거르는 핵심 raw diff야.

```diff
diff --git a/app/src/features/admin/routes/AdminMergeRows.tsx b/app/src/features/admin/routes/AdminMergeRows.tsx
--- a/app/src/features/admin/routes/AdminMergeRows.tsx
+++ b/app/src/features/admin/routes/AdminMergeRows.tsx
@@ -115,52 +149,79 @@ export function AdminMergeRows() {
     fetchRows()
     setCanonicalId(null)
     setDuplicateId(null)
-    setImpact(null)
+    setPreview(null)
     setSuccess(null)
   }, [fetchRows, entity])
 
-  // duplicate 선택 시 영향 자동 점검
+  // 부모를 자식에 머지하는 경로는 화면에서부터 없앤다 (N3).
+  // DB 도 RPC 검사 + categories_no_cycle 트리거 2겹으로 막는다.
+  const parentRows = useMemo<ParentRow[]>(
+    () =>
+      config.selfParent
+        ? rows.map((r) => ({ id: Number(r.id), parent_id: r.parent_id == null ? null : Number(r.parent_id) }))
+        : [],
+    [rows, config.selfParent],
+  )
+  const blockedAsCanonical = useMemo(
+    () => (config.selfParent && duplicateId != null ? descendantIds(parentRows, duplicateId) : new Set<number>()),
+    [parentRows, duplicateId, config.selfParent],
+  )
+  const blockedAsDuplicate = useMemo(
+    () => (config.selfParent && canonicalId != null ? ancestorIds(parentRows, canonicalId) : new Set<number>()),
+    [parentRows, canonicalId, config.selfParent],
+  )
+
+  const canonicalOptions = rows.filter(
+    (r) => Number(r.id) !== duplicateId && !blockedAsCanonical.has(Number(r.id)),
+  )
+  const duplicateOptions = rows.filter(
+    (r) => Number(r.id) !== canonicalId && !blockedAsDuplicate.has(Number(r.id)),
+  )
+
+  // 양쪽이 정해지면 dry-run 으로 미리보기와 충돌을 먼저 받는다.
   useEffect(() => {
-    if (duplicateId == null) {
-      setImpact(null)
+    if (canonicalId == null || duplicateId == null || canonicalId === duplicateId) {
+      setPreview(null)
       return
     }
     let cancelled = false
-    setImpactLoading(true)
+    setPreviewLoading(true)
+    setError(null)
     ;(async () => {
-      const counts: ImpactCounts = {}
-      for (const ref of config.fkRefs) {
-        const { count, error: e } = await supabase
-          .from(ref.table)
-          .select('*', { count: 'exact', head: true })
-          .eq(ref.column, duplicateId)
-        if (cancelled) return
-        if (e) {
-          counts[`${ref.table}.${ref.column}`] = -1
-          continue
+      try {
+        const result = await adminMergeRows({
+          table: config.table,
+          duplicateId,
+          canonicalId,
+          dryRun: true,
+        })
+        if (!cancelled) setPreview(result)
+      } catch (err) {
+        if (!cancelled) {
+          setPreview(null)
+          setError(adminErrorMessage(err))
         }
-        counts[`${ref.table}.${ref.column}`] = count ?? 0
-      }
-      if (!cancelled) {
-        setImpact(counts)
-        setImpactLoading(false)
+      } finally {
+        if (!cancelled) setPreviewLoading(false)
       }
     })()
     return () => { cancelled = true }
-  }, [duplicateId, config.fkRefs])
+  }, [canonicalId, duplicateId, config.table])
+
+  const conflicts = preview?.conflicts ?? []
+  const blocked = conflicts.length > 0
 
   async function handleMerge() {
-    if (canonicalId == null || duplicateId == null) return
-    if (canonicalId === duplicateId) {
-      setError('canonical 과 duplicate 가 같은 row 입니다')
-      return
-    }
+    if (canonicalId == null || duplicateId == null || blocked) return
+
+    const dup = preview?.preview?.duplicate
+    const canon = preview?.preview?.canonical
     if (!confirm(
       `정말 머지하시겠습니까?\n\n` +
-      `canonical (남김): id=${canonicalId}\n` +
-      `duplicate (제거): id=${duplicateId}\n\n` +
-      `Supabase JS 는 트랜잭션 보장 X — 부분 실패 가능. ` +
-      `실패 시 SQL editor 에서 수동 정리 필요.`,
+      `canonical (남김): ${canon?.name ?? ''} (id=${canonicalId})\n` +
+      `duplicate (제거): ${dup?.name ?? ''} (id=${duplicateId})\n\n` +
+      `${dup?.productLines ?? 0}개 제품 라인 · ${dup?.shades ?? 0}개 쉐이드가 canonical 로 옮겨집니다.\n` +
+      `한 트랜잭션으로 실행되며, 도중에 실패하면 아무것도 바뀌지 않습니다.`,
     )) return
```

연결된 순수 함수 테스트 hunk야.

```diff
diff --git a/app/src/features/admin/routes/adminMergeUtils.test.ts b/app/src/features/admin/routes/adminMergeUtils.test.ts
new file mode 100644
index 00000000..f0bc1432
--- /dev/null
+++ b/app/src/features/admin/routes/adminMergeUtils.test.ts
@@ -0,0 +1,36 @@
+import { describe, expect, it } from 'vitest'
+import { descendantIds } from './adminMergeUtils'
+
+/**
+ * sw-1yt / 워커 추가 케이스 W8 — 노트 줄이 아니라 N3 화면 요건을 떠받치는 순수 함수.
+ * 계약 §2-3(c): categories 대상 후보에서 duplicate 의 자손을 제외한다.
+ */
+const rows = [
+  { id: 1, parent_id: null },
+  { id: 2, parent_id: 1 },
+  { id: 3, parent_id: 2 },
+  { id: 4, parent_id: null },
+  { id: 5, parent_id: 4 },
+]
+
+describe('descendantIds (W8)', () => {
+  it('자손을 깊이 제한 없이 전부 모은다', () => {
+    expect([...descendantIds(rows, 1)].sort()).toEqual([2, 3])
+  })
+
+  it('자기 자신은 포함하지 않는다', () => {
+    expect(descendantIds(rows, 1).has(1)).toBe(false)
+  })
+
+  it('잎 노드는 빈 집합', () => {
+    expect(descendantIds(rows, 3).size).toBe(0)
+  })
+
+  it('기존 사이클이 있어도 무한 루프에 빠지지 않는다', () => {
+    const cyclic = [
+      { id: 10, parent_id: 11 },
+      { id: 11, parent_id: 10 },
+    ]
+    expect([...descendantIds(cyclic, 10)]).toEqual([11])
+  })
+})
```

이 줄들만 봤을 때, 화면에서 어떤 카테고리 조합이 선택지에서 사라진 것 같아?
~~~

## Turn 25

Codex turn key: `ccc48e70cca97dad`

### 모기

~~~text
코드를 보고 이해한거라고는 말 못하겠다옹 암튼 부모<->자식간의 조합은 안되는거긴한데 그건 그냥 원래 알고있었다옹 아 내가 타입스크립트를 안써봐서.. 코드가 잘 안들어오는구먼
~~~

### 과외냥이

~~~text
응, TypeScript 코드가 안 들어온 건 실패가 아니야. 이 경우 raw diff보다 **행의 변경 전후 표**가 훨씬 낫다옹.

그리고 중요한 교정이 있어. 실제 구현은 `부모 ↔ 자식` 양방향을 모두 막는 게 아니야. **부모를 자기 자손 쪽으로 합치는 방향만 막고, 자식을 부모 쪽으로 합치는 방향은 허용해.**

예를 들어 현재 상태가 이렇다고 하자.

| 카테고리 행 | `parent_id` |
|---|---|
| 립 | 없음 |
| 틴트 | 립 |
| 워터 틴트 | 틴트 |

`립`을 없애고 `워터 틴트`에 합치면:

- `틴트.parent_id`가 `워터 틴트`로 바뀜
- 그런데 `워터 틴트.parent_id`는 여전히 `틴트`
- `틴트 → 워터 틴트 → 틴트`로 빙글빙글 돌게 됨

이게 여기서 말하는 **사이클**이야.

반대로 `틴트`를 없애고 부모 `립`에 합치면:

- `워터 틴트.parent_id`가 `립`으로 바뀜
- `틴트`는 사라짐
- `립 → 워터 틴트`라는 정상 트리로 남음

코드에 `descendantIds`와 `ancestorIds`가 둘 다 나온 이유는 두 방향을 모두 금지해서가 아니야. 사용자가 두 선택칸 중 어느 쪽을 먼저 골라도, 똑같은 **부모를 자손에 합치는 선택**을 후보에서 제거하려는 거야. [AdminMergeRows.tsx](https://github.com/baksohyeon/swatch-v2/blob/5603d3ab7cd356bccbd491a6e2a0c261eb3c3c83/app/src/features/admin/routes/AdminMergeRows.tsx#L149-L178)

그래서 PR 첫 절의 “자손뿐 아니라 조상도 뺐다”는 문구가 양방향 금지처럼 읽히기 쉬웠어. 실제 코드는 바로 뒤 설명처럼 `자식 → 부모`를 정상 경로로 남겨둬.

여기서 모기의 진짜 의도를 다시 확인해야 해. **자식 카테고리를 부모에 합치는 것도 막고 싶은 거야, 아니면 현재 구현처럼 부모를 자손에 합치는 방향만 막으면 돼?**
~~~

## Turn 26

Codex turn key: `f6b96430b1ab54ac`

### 모기

~~~text
일단 기계적으로 말하면 양방향 다 막아야된다옹. 근데 그건 양방향을 다 막는다!라기보단.. 그냥 부모(립이라는 추상적카테고리.. 그저 립이라고 묶기위한..)가 추상적카테고리라서.. 라고생각함.. 그래서 그 추상적카테고리 대상의 머지는 막아야한다는..
~~~

### 과외냥이

~~~text
그렇다면 핵심은 **부모·자식 관계라서 양방향을 막는 것**이 아니고, **분류용 상위 카테고리 자체를 일반 중복 행처럼 머지하면 안 된다**는 정책이네.

현재 구현은 그 정책과 달라.

- 허용됨: 자식 `틴트`를 부모 `립`에 합치기
- 허용됨: 부모 `립`을 관계없는 `아이` 카테고리에 합치기
- 차단됨: 부모 `립`을 자기 자손 `틴트`에 합쳐 사이클 만들기

즉 현재 구현은 **트리가 빙글 도는 경우만 차단**해. 모기가 말한 **추상적인 분류 카테고리를 머지 대상에서 제외**하는 규칙은 없어.

현재 `categories` 행에는 “추상 카테고리”라는 별도 표시가 없어서, 가장 가까운 기계적 기준은 **나를 부모로 가리키는 자식 행이 하나라도 있는가**야. [database.types.ts](/home/mogi/code/swatch/swatch-v2/app/src/data/database.types.ts:284)

예쁜필기노트 판정으로는:

- N3의 표면 문장인 “부모를 자식에 합치지 않기”는 구현됨.
- 하지만 그 문장의 실제 배경인 “분류용 부모 카테고리는 머지 대상이 아님”은 구현되지 않음.
- 따라서 잠정적으로 **의도와 다르게 된 줄 1개: N3**이야.

정확한 계약을 확인할게. **자식 행이 하나라도 달린 카테고리는 남길 쪽과 없앨 쪽 어느 선택칸에도 나오지 않게 하고, 자식이 없는 카테고리끼리만 머지를 허용하는 게 모기의 뜻이야?**
~~~

## Turn 27

Codex turn key: `4a2317bab9334ada`

### 모기

~~~text
아 약간! 약간 불안하긴하지만 그게 내뜻 맞는것같아... (내가 추후에 카테고리를 세분화하지 않는한.. 그치만 그럴일없겠지?! 없겠지?!!?!)
~~~

### 과외냥이

~~~text
그 불안 때문에 오히려 **영구 불변식이 아니라 현재 어드민 머지 도구의 안전한 경계**로 두는 게 맞아.

미래에 카테고리를 더 세분화해도 문제없어. 그때 상위 분류를 합치거나 재구성할 필요가 생기면, 일반 중복 행 머지가 아니라 자식 이동 결과까지 확인하는 별도 흐름으로 정책을 다시 정하면 돼. 지금 당장 미래의 모든 카테고리 구조를 약속할 필요는 없어.

N3 의도 차이는 마스터 메모에 전달했어.

- 다르게 된 줄: `N3` 1개
- 판독 SHA: `5603d3a`
- 커밋: `3ed37be`
- 제품 저장소는 건드리지 않았어.

이제 첫 번째 지표도 기록하자. PR 첫 절을 읽기 시작해서 “뭐가 바뀌었는지 알겠다”까지 체감상 얼마나 걸렸어? 정확한 초가 아니어도 `바로 / 1분쯤 / 3분 이상 / 아직 아니었음` 정도면 돼.
~~~

## Turn 28

Codex turn key: `9fc9069cc0dad493`

### 모기

~~~text
음.... 2~3분정도? 아니 2분정도.. 아 이거 시간을 잴걸 ㅋ
~~~

### 과외냥이

~~~text
2번은 이 지도만 알면 충분해.

### 화이트리스트

서버가 머지를 허용하는 표 이름 목록이야.

```text
brands
categories
product_lines
```

화면이 실수하거나 누군가 API를 직접 호출해서 `users` 같은 다른 표 이름을 보내면 서버가 거부해. [마이그레이션](https://github.com/baksohyeon/swatch-v2/blob/5603d3ab7cd356bccbd491a6e2a0c261eb3c3c83/supabase/migrations/20260826170000_rpc_admin_merge_rows.sql#L112-L116)

### `fkRefs`

없앨 행을 가리키던 자식 데이터가 어느 자리로 옮겨져야 하는지를 적은 지도였어.

- 브랜드 머지: 제품라인의 `brand_id` 이동
- 카테고리 머지: 제품라인의 `category_id`와 자식 카테고리의 `parent_id` 이동
- 제품라인 머지: 쉐이드의 `product_line_id` 이동

### `softDelete`

없앨 행을 실제로 지울지, 삭제 표시만 남길지를 정한 값이었어.

- 브랜드·제품라인: 삭제 표시만 남김
- 카테고리: 실제 행 삭제

예전에는 브라우저 코드가 `fkRefs`와 `softDelete`를 들고 여러 요청을 보냈어. 지금은 브라우저가 표 이름과 두 행 번호만 서버에 보내고, 서버가 이동 대상과 삭제 방식을 정해서 한 번에 실행해. [서버 실행 분기](https://github.com/baksohyeon/swatch-v2/blob/5603d3ab7cd356bccbd491a6e2a0c261eb3c3c83/supabase/migrations/20260826170000_rpc_admin_merge_rows.sql#L253-L284)

“하드코딩 아니야?”에 대한 답은 **서버에 명시적으로 고정한 것은 맞아. 하지만 여기서는 의도적인 안전장치야.** 아무 표나 자동으로 추측해서 합치는 범용 함수보다, 허용된 세 표의 이동·삭제 규칙을 서버 계약에 박는 편이 안전해.

다만 PR의 “다음 표를 추가하면 마이그 한 곳만 고친다”는 표현은 문자 그대로는 조금 과해. 서버의 머지 동작은 한곳만 고치지만, 새 표를 화면에 노출하려면 클라이언트의 표 이름 타입과 표시 설정도 추가해야 해. [클라이언트 설정](https://github.com/baksohyeon/swatch-v2/blob/5603d3ab7cd356bccbd491a6e2a0c261eb3c3c83/app/src/features/admin/routes/AdminMergeRows.tsx#L26-L64)

이 정도면 `화이트리스트`, `fkRefs`, `softDelete`가 어디로 갔는지 잡혔어?
~~~

## Turn 29

Codex turn key: `87bdedf62560e435`

### 모기

~~~text
아 근데 표가 그 DB의 테이블을 말하는거지?ㅋ 헷갈려서 ㅋ 화이트리스트랑 나머지도 다 이해했다옹!
~~~

### 과외냥이

~~~text
응. `표`는 전부 **DB 테이블**을 뜻해. 그 표현 때문에 한 번 더 해석해야 했다는 것도 기록 포인트로 남겨둘게.

3번 삭제 방식의 결론은 이거야.

| 머지 대상 | 없앨 행의 최종 상태 |
|---|---|
| 브랜드 | 행은 남고 `is_deleted=true` |
| 제품라인 | 행은 남고 `is_deleted=true` |
| 카테고리 | 행 자체가 실제로 삭제됨 |

브랜드를 `맥 코스메틱 → 맥`으로 합치면:

1. 제품라인들의 `brand_id`가 `맥`으로 바뀜
2. `맥 코스메틱` 브랜드 행은 DB에 남음
3. 그 행에 `is_deleted=true`, `deleted_at=현재 시각`이 기록됨
4. 일반 조회는 삭제 표시된 브랜드를 제외하므로 화면에서는 사라짐

[PR #539 머지 분기](https://github.com/baksohyeon/swatch-v2/blob/5603d3ab7cd356bccbd491a6e2a0c261eb3c3c83/supabase/migrations/20260826170000_rpc_admin_merge_rows.sql#L253-L284)

왜 카테고리만 실제 삭제하도록 처음 설계했는지는 **확인된 근거가 없어**. 현재 확인되는 사실은:

- 브랜드·제품라인 테이블에는 처음부터 삭제 표시 컬럼이 있음.
- 카테고리 테이블에는 그 컬럼이 없음.
- PR #539는 새 정책을 정하지 않고 기존 차이를 그대로 서버로 옮김.
- 워커 보고도 “카테고리에는 soft-delete 컬럼이 없어서 현행 hard delete를 유지했다”고만 적음. [워커 보고](/home/mogi/code/swatch/swatch-ops/reports/2026-08-27-sw-1yt-control-report.md:30)

섞어 쓰는 것 자체가 무조건 안티패턴은 아니야. 데이터 종류별 수명이 다르면 그럴 수 있어. 하지만 **왜 수명이 다른지 정책이 없는데 구조만 다르고, 실제 삭제가 다른 데이터까지 조용히 지운다면 부채**야.

실제로 카테고리를 삭제하면 연결된 `category_trait_rules`도 함께 삭제되는 경로가 발견됐고, 이미 열린 이슈 `sw-cep`로 등록돼 있어. 새 메모를 중복해서 만들 필요는 없어. [운영 이슈 원문](/home/mogi/code/swatch/swatch-ops/.beads/issues.jsonl:16)

그러니까 모기의 질문에 정확히 답하면:

- 합쳐진 옛 브랜드 행은 DB에 남지만 화면에서는 사라짐.
- 제품라인을 전부 옮겼으므로 활성 제품 관계의 주인 역할은 끝남.
- 카테고리 hard delete의 정당한 제품 이유는 아직 확인되지 않음.
- 현재 카테고리 실제 삭제에는 별도 데이터 손실 문제가 있어서 후속 판단이 필요함.

이제 세 묶음 중 아직 남은 막힘이 있어, 아니면 여기까지는 연결됐어?
~~~

## Turn 30

Codex turn key: `16a9608debabad61`

### 모기

~~~text
ㅇㅇ 이해했어 설명 이번에도 맘에든다옹 그리고 네말에 동의한다옹 왜 수명이 다른지 정책이 없는데 구조만 다르다니.. 이건 그냥 처음에 대충 ai 짬때려서 테이블 막 만들고(공통컬럼 뭐추가할지 안정해두고;;) 지금에서야 걍 이유가있을거라고 추측한것이군..
~~~

### 과외냥이

~~~text
맞아. 다만 **“처음 AI가 대충 만들었다”는 원인은 실측되지 않은 추측**으로 남겨야 해. 지금 근거로 확정할 수 있는 건 여기까지야.

- 초기 스키마에서 브랜드·제품라인에는 삭제·수정 이력 컬럼을 넣었음.
- 카테고리에는 같은 수명주기 컬럼을 넣지 않았음.
- 왜 다르게 설계했는지 설명하는 제품 정책은 찾지 못함.
- PR #539도 이유를 새로 확정하지 않고 기존 구조를 그대로 옮김.
- 그 차이 때문에 카테고리 규칙이 같이 사라지는 실제 부작용까지 생김.

그러니까 **“정책에 따른 의도적 차이라기보다, 근거가 사라진 초기 스키마 차이를 지금까지 승계했다”** 정도가 현재 증거에 맞는 결론이야.

이번 설명 방식이 잘 맞았다는 것도 기억해둘게. 세 막힘은 모두 연결됐고, 지표는 체감 약 2분·의도 차이 N3 한 줄로 남겼어.

핵심길 뒤 아직 raw diff를 직접 안 본 파일은 아래야.

- `adminMergeRepo.ts`
- `AdminMergeRows.test.tsx`
- `adminMergeUtils.ts`
- `POLICY-SNAPSHOT.md`
- RPC 마이그레이션
- SQL 검증 파일

끌리는 샛길 파일 하나를 골라도 되고, 피곤하면 이번에는 샛길을 생략해도 돼.
~~~

## Turn 31

Codex turn key: `06cfee89dce84e55`

### 모기

~~~text
생략하자옹. 그리고 마지막으로.. category_trait_rules 이거 삭제되는거 뭔가. 문제없지않나?싶기도.. 카테고리가 필요없는데 카테고리추천규칙이 남아있으면 그게더문제아님? 뭔가 연계된 화면에서 오류날것같은데
~~~

### 과외냥이

~~~text
맞다옹. **진짜 카테고리 삭제라면 `category_trait_rules`도 같이 삭제되는 게 맞다옹.** 규칙만 남으면 존재하지 않는 카테고리를 가리키니까 더 이상한 상태고, 현재 DB도 `ON DELETE CASCADE`로 그걸 막는다옹.

다만 지금 사건은 단순 삭제가 아니라 **중복 카테고리 머지**라서 조금 다르다옹.

예를 들어:

- 중복 카테고리 `립스틱(오타)`에는 `스틱 형태를 기본 선택`하는 규칙이 있음
- canonical `립스틱`에는 그 규칙이 없음
- 머지하면 제품라인은 canonical `립스틱`으로 옮겨짐
- 그런데 중복 카테고리의 추천 규칙은 옮겨지지 않고 같이 삭제됨

그러면 화면이 오류를 내지는 않는다옹. 새 제품 등록 화면은 그냥 canonical 쪽 규칙만 읽으므로, **추천이나 기본 선택이 조용히 사라지는 쪽**에 가깝다옹.

그래서 정확한 결론은 이거다옹.

- 일반 카테고리 삭제: 규칙까지 cascade 삭제하는 것이 자연스러움
- 중복 카테고리 머지: 삭제 전에 두 카테고리의 규칙을 어떻게 합칠지 정책이 필요함
- 규칙을 무조건 옮기는 것도 같은 trait 규칙의 설정값이 충돌할 수 있어서 위험함

즉 기존 이슈의 “cascade 삭제 자체가 버그”라는 표현은 너무 강하고, **“머지 경로에 추천 규칙 비교·충돌 정책이 없다”**가 정확하다옹.

이번 PR 범위에서는 중복 카테고리에 규칙이 존재하면 자동으로 날리지 말고, 머지를 막아서 canonical 규칙과 먼저 비교하게 하는 쪽이 네 의도와 가까울까옹?
~~~

## Turn 32

Codex turn key: `82d7869e450a612d`

### 모기

~~~text
아아... 아니. 너무 복잡해 그건.. 아닌가? 약간.. 테이블과 테이블사이 매핑이니까 복잡하지않나? 아닌가?!?! 그래서 난 걍 둘 중 택1하라고 할것같아 왜냐하면. 둘중 하나는 늦게 만든걸테고, 늦게 만든거는 카테고리규칙도 대충만들었거나 안만들었겠지.. (확률적이긴해) 그럼 둘중 하나가 정본일 가능성이 높다옹! 그것도그렇고 머지가 너무 복잡해서 이게나을듯
~~~

### 과외냥이

~~~text
맞다옹. **관리자가 둘 중 하나를 정본으로 고르면 그 카테고리의 규칙 묶음만 남기고, 없애는 카테고리의 규칙은 함께 삭제**하면 된다옹.

행별로 합치기 시작하면 같은 `trait` 규칙의 기본값·추천 여부 충돌까지 판단해야 해서 머지가 급격히 복잡해진다옹. 반면 네 정책은 “규칙까지 포함해서 카테고리 하나를 택1”이라 명확하다옹. 현재 cascade 동작도 이 정책과 일치하고, 연계 화면에서 삭제된 카테고리 규칙을 참조하는 오류도 생기지 않는다옹.

기존 `sw-cep`의 “규칙 소실 버그” 판단은 과도했다고 정정해서 마스터 메모에 전달했고, 커밋은 `b286497`이다옹. 굳이 보완한다면 머지 전에 양쪽 규칙 개수 정도만 보여줘서 관리자가 정본을 고르게 하는 정도면 충분하겠지옹?
~~~

## Turn 33

Codex turn key: `36a1e5a9380c1bcd`

### 모기

~~~text
ㅇㅇ 충분하다옹! 그리고 카테고리 건드릴일은 별로 없으니까
~~~

### 과외냥이

~~~text
ㅇㅇ, 카테고리 작업 빈도까지 낮다면 복잡한 규칙 병합기를 만들 이유가 더 적다옹.

정책은 이렇게 확정하면 된다옹:

- 관리자가 canonical 카테고리를 택함
- canonical의 규칙 묶음만 보존
- duplicate의 규칙은 cascade 삭제
- 선택 전에 양쪽 규칙 개수 정도만 표시

마스터 메모에도 운영 빈도와 보완 범위를 추가했다옹. 커밋은 `f92d587`이다옹. 이 건은 여기까지면 충분하다옹.
~~~

## Turn 34

Codex turn key: `f03e9b1348534391`

### 모기

~~~text
ㅇㅇ 그럼 여기서 세션종료하자옹ㅋ
~~~

### 과외냥이

~~~text
세션 종료 정리 완료했다옹ㅋ

- [종료 관찰일지](/home/mogi/code/swatch/mogi-cards/head-test/2026-08-27-pr539-pretty-notebook-tdd-first-review-session-end-analysis.md) 작성
- `SCRATCHPAD.md` 세션 메모 정리
- [대화 원문](/home/mogi/code/swatch/mogi-cards/raw/2026-08-27-01-pr539-pretty-notebook-tdd-first-review.md) 별도 보존
- 종료 기록 커밋: `35f5943`
- 원문 커밋: `6a13298`

오늘 예쁜필기노트TDD 1호 과외 끝이라옹. 푹 쉬자옹ㅋ
~~~

## Turn 35

Codex turn key: `566dcbb3187e7653`

### 모기

~~~text
아맞다. 근데 세션 극초반이었던것같은데.. 뭐냐 네가 harness 디렉토리에 그 임시 파일럿.. 중복 어쩌구, 그거 다 됐다고 했었던가? 흠 뭐였더라 ㅋ
~~~

### 과외냥이

~~~text
맞다옹ㅋ 그건 **`발견물 중복 확인 관문 파일럿`**이었다옹.

목적은 과외 중 발견한 문제를 마스터 메모에 쓰기 전에:

- 이미 열린 이슈와 같은 문제인지
- 같은 원인이지만 새로운 영향인지
- 완전히 새로운 문제인지

확인해서 중복 이슈를 만들지 않는 것이었어.

예정된 3회를 모두 채웠다옹:

1. PR #524 — 기존 `sw-ber`와 같은 영향
2. PR #535 — `sw-ber`의 새로운 영향
3. 이번 `sw-1yt` — 브랜드 머지의 제품라인 충돌이라는 새로운 영향

그래서 **자동 적용은 종료했고, 시작 가이드에서도 활성 파일럿 절을 삭제**했어. 영구 규칙으로 승격한 것은 아니고 실험 기록만 [파일](/home/mogi/code/swatch/mogi-cards/harness/pilots/2026-08-20-discovery-dedupe-gate.md)에 남겨뒀다옹. 종료 커밋이 `601af30`이고, 내가 초반에 “예정된 3회를 채워 종료했다”고 말한 게 맞다옹ㅋ
~~~

## Turn 36

Codex turn key: `a2ec33c83870d723`

### 모기

~~~text
그래? 근데 흠. 영구 승격해야되나 말아야되나 모르겠네 까먹고있었어 ㅋ
~~~

### 과외냥이

~~~text
내 추천은 **결과만 가볍게 영구 승격하고, 파일럿 절차 전체는 승격하지 않는 것**이다옹.

3회 결과가 꽤 일관됐어.

- 1회: 기존 이슈와 같은 내용을 새 이슈로 복제하지 않음
- 2회: 기존 이슈의 새로운 영향은 눌러버리지 않고 delta로 전달
- 3회: 이번 브랜드 머지 충돌도 `sw-1yt`의 delta로 정확히 연결

즉 핵심 구분은 실제로 유용했어.

> 같은 원인 + 같은 영향 → 중복 전달하지 않음  
> 같은 원인 + 새로운 영향 → 기존 이슈를 밝힌 delta로 전달

다만 매번 git 이력까지 기계적으로 전수 검색하는 비용이 얼마나 거슬리는지는 3회 모두 `미관찰`이었어. 그래서 그 무거운 절차까지 영구화할 근거는 부족하다옹.

영구 규칙은 이 정도면 충분해 보여:

> 새 발견물을 마스터 메모에 쓰기 전에 현재 메모와 열린 운영 이슈를 짧게 확인한다. 같은 원인·같은 영향이면 중복 작성하지 않고, 새로운 영향이면 기존 이슈의 delta로 작성한다. 과거 git 이력은 동일성을 가릴 수 없을 때만 확인한다.

현재는 파일럿 절을 가이드에서 제거했기 때문에 자동 적용이 끝났고, 절대 규칙 2에 “파일럿이 활성인 동안”이라는 죽은 조건만 남아 있어. 이 경량판으로 영구 승격하면 그 문장도 깔끔하게 정리할 수 있다옹. 나는 **경량 영구 승격 찬성**이다옹.
~~~

## Turn 37

Codex turn key: `3f9eef10249d507e`

### 모기

~~~text
경량 영구승격 좋고. 흠... 무거운 절차라.. 그럼 매번 git이력까지 전수검색하고온거야? 3번다?
~~~

### 과외냥이

~~~text
응, **3번 다 git 이력을 확인한 건 맞다옹.** 다만 내가 “무거운 절차”라고 조금 크게 표현했어. 정확히는 매번 아래 세 곳만 확인했어.

1. 현재 `master-delivery-memos.md`
2. 그 파일 하나의 git 이력
3. 열린 운영 이슈 목록

저장소 전체 커밋이나 과거 세션 JSONL을 전수검색한 건 아니다옹.

경량 영구 승격도 완료했어.

- 평소에는 현재 마스터 메모와 열린 이슈만 확인
- 같은 원인·같은 영향이면 중복 생략
- 같은 원인·새 영향이면 기존 이슈 delta
- 둘만으로 판단이 안 되거나 모기가 과거 전달을 기억할 때만 git 이력·원문 추가 확인

파일럿 상태도 `completed`, 결과는 `lightweight-rule-promoted`로 바꿨고 커밋은 `2b1b9d0`이다옹.
~~~

## Turn 38

Codex turn key: `a5f742c1337aaa4d`

### 모기

~~~text
ㅇㅇ 좋다옹. 그리고 지금 마스터 냐옹이랑 그.. 두개를 비교하고있어 예쁜필기TDD적용한 계약대로 구현한거랑(A), 그냥 평소처럼구현한거(B) 이게 그 비교표인데 아 모르겠어.. 지금 편향을 제거할수가없다옹;;  그대로 있고 끝 절("결정 후보")까지 온전하다옹. 보던 뷰어가 긴 표 줄을 접었을
  가능성이 크다옹(GitHub/에디터 폭). 핵심 표만 여기 다시 붙인다옹:

  ┌──────────────────┬──────────────────────────────┬────────────────────────────┐
  │                  │        A (노트 있음)         │       B (노트 없음)        │
  ├──────────────────┼──────────────────────────────┼────────────────────────────┤
  │ 합치기 전        │ 카드 2장:                    │                            │
  │ 미리보기(N5)     │ 이름·slug·라인/쉐이드        │ 없음                       │
  │                  │ 수·예시 3                    │                            │
  ├──────────────────┼──────────────────────────────┼────────────────────────────┤
  │ 같은 라인        │ 변경 전 차단 + 충돌 쌍 목록  │ 없음 — UNIQUE 롤백 후 원문 │
  │ 충돌(N6·N7)      │                              │  에러(B는 "후속"으로 미룸) │
  ├──────────────────┼──────────────────────────────┼────────────────────────────┤
  │ 부모→자식        │ RPC+트리거+화면 후보 제외    │ RPC+트리거                 │
  │ 머지(N3)         │                              │                            │
  ├──────────────────┼──────────────────────────────┼────────────────────────────┤
  │ 삭제된 행(N8)    │ 거부                         │ 거부(관례로 도달)          │
  ├──────────────────┼──────────────────────────────┼────────────────────────────┤
  │ 실패 시(N2·N4)   │ 전량 롤백·문구 삭제          │ 동일                       │
  ├──────────────────┼──────────────────────────────┼────────────────────────────┤
  │ 사이클 코드      │ P0410 전용                   │ P0001 범용                 │
  ├──────────────────┼──────────────────────────────┼────────────────────────────┤
  │ 벽시계 / 토큰 /  │ 실질 ~55분 / 184k / 393      │ 19분 / 157k / 268          │
  │ 마이그 줄        │                              │                            │
  └──────────────────┴──────────────────────────────┴────────────────────────────┘

  모기 판정 칸: 첫 절 이해 약 2분 · 다른 줄 1개(N3) → fix2로 정정 중. B가 더 본 것:
  CASCADE(→ sw-cep, 정책 확정으로 종결).

  "어 다"에서 끊긴 말이 있으면 이어서 말해달라옹 🐾

✻ Worked for 26s · done 4:41 PM · 1 shell still running

❯ 흠. 근데 나는 A가 더 나아보이는데..... 흠..... 근데 내가 기니피그를 좋아해서
  객관적판단이 안된다옹!

● 좋은 자기 의심이다옹. 그럼 감정 빼고 표만 다시 보자옹 — A가 좋아 보이는 건 감정
  때문만이 아니다, 근데 모든 칸이 A 편은 아니다.

  감정과 무관하게 A 편인 것(실측)
  - N5·N6·N3-화면: A에만 있고, B는 N6를 보고도 "후속"으로 미뤘다옹. 이건 "노트가
    범위를 확정한다"는 실측이지 취향이 아니다옹.
  - 모기가 "다른 줄"을 1개로 특정할 수 있었다옹. B였으면 "미리보기 없음·충돌 안내
    없음·후보 제외 없음"이 리뷰 코멘트 3개로 왔을 것.

  B 편인 것(이것도 실측)
  - 19분 vs 55분, 157k vs 184k, 마이그 268줄 vs 393줄 — B가 싸고 작다옹.
  - CASCADE는 B만 봤다옹. 노트가 시선을 좁힌 부작용.
  - A가 하는 일이 더 많으니 더 오래·더 비싼 게 당연 — "노트 때문에 느려졌다"가 아니라
    "노트가 요구를 늘렸다".
  - B는 A 잔재를 봤다(구조 힌트). B에 유리한 오염.
  - 나도 편향이다옹 — A 계약을 내가 썼고, 제도를 내가 등재했다옹. 이 비교표 작성자가
    나라는 것 자체가 A 쪽 바람.
~~~

### 과외냥이

~~~text
편향을 완전히 제거할 필요는 없고, **판정 범위를 좁히면 된다옹.** 이 한 번으로 “예쁜필기TDD가 항상 우월하다”는 결론은 못 내리지만, **이번 어드민 머지에서는 A가 모기의 실제 요구를 더 많이 구현했다**는 판정은 가능해.

표를 공정하게 다시 읽으면:

- 완성도: A 우세  
  N5 미리보기와 N6 충돌 선검사·표시가 실제로 필요하다고 모기가 확정했고, B에는 없음.

- 비용·크기: B 우세  
  B가 훨씬 빠르고 작음. 다만 구현한 요구 자체가 적어서 순수한 생산성 비교는 아님.

- 의도 추적성: A 우세  
  N3 한 줄이 다르다는 걸 정확히 집어 fix2로 돌릴 수 있었음. “처음부터 완벽했다”가 아니라, 어긋남의 위치가 드러났다는 장점이야.

- 독립적인 탐색 폭: B가 하나 더 봄  
  다만 CASCADE는 최종적으로 버그가 아니라 “canonical 규칙만 남긴다”는 정책으로 확정됐어. 따라서 `버그 1개 발견`보다는 `정책 질문 1개 제기`로 세는 게 정확하다옹.

- 인과관계: 판정 불가  
  B가 A의 잔재를 봤고, 구현 범위도 달랐으므로 시간·토큰 차이를 전부 노트 효과로 돌릴 수 없음.

그러니까 현재 가장 정직한 결론은 이거야.

> A는 이번 파괴적 어드민 작업에서 더 비싸지만, 모기가 중요하게 여긴 동작을 더 빠짐없이 구현 범위에 넣었다. 다만 한 쌍의 오염된 비교만으로 모든 작업에 적용할 제도라고 확정할 수는 없다.

그리고 모기가 A의 결과를 실제로 더 가치 있다고 느끼는 건 제거해야 할 편향만은 아니야. 제품 소유자가 미리보기·충돌 차단을 중요하게 평가한다는 것 자체가 **효용 기준**이니까. 다만 “기분이 좋다”와 “구현 결과가 낫다”를 분리해 적으면 충분하다옹.

내 추천 결정 후보는 **A 우승 선언이 아니라, 파괴적이거나 정책 선택이 많은 작업에만 예쁜필기TDD를 2건 정도 더 적용해본다**야. 이 정도 결론이면 네 마음에도 과장처럼 느껴지지 않나옹?
~~~
