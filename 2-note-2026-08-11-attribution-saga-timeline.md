# 귀속(owner_uid) 사가 — 포스트모템 원재료 타임라인

> 포스트모템 작성용 사실 모음 (마스터 냐옹이 Gen 4, 2026-08-11). 해석·교훈은 일부러 최소화 — 그건 네 포스트모템 몫.
> 원문: swatch-ops contracts/ 문서들 + sw 트래커 + PR #495·#496·#498. **완결됨 (2026-08-11 UAT 4/4).**

## head 체인

**#496**: `461f630`(1차 구현) → `be17d63`(fix 8건) → `6985ac3`(의미론 docs) → `f22d5c9`(2차: 인덱스+표기) → `8583ff1`(3차: 소유자 결정 2건) → 머지 `cac1dc9`
**#498**: `871bbf3`(게이트 신설) → `597e4b1`(2차: P2 2건 + P0 회귀 수리) → 머지 `07ba976`

## 타임라인

**08-10 (설계·구현일)**
- 발단: PR #493 과외 세션 발견 — 핸들 변경 시 귀속 풀림, 원격 실측 15/20건. SEED-020 승격.
- 모기 브레인스토밍 → spec 승인(PR #495): 귀속 진실 = owner_uid, 결정 3건(핸들 변경 무영향 / un-verify만 원복 / 직접권 이전 안 함).
- 구현 워커 → PR #496 (5커밋, `461f630`).
- codex 교차 리뷰 1차: **P1 3 · P2 3 · P3 2, 머지 보류.** (삭제 요청 잔존 / 원복 불완전 / B′ 자격 박탈 = 미승인 편차 / 공개 프로필 미반영 / lower 충돌 / anon 노출)
- 마스터 세대 교체(Gen 2 → Gen 4). fix 패스 = 인계 1순위.

**08-10 저녁 (fix 패스)**
- fix 워커: 8건 전부 수리(`be17d63`), 검증 25 assertions + 음성 대조. **READY 1차.**
- 과외 발견 ①: 원작자 직접권 재검토 요구 → 모기, spec 결정 3 재확인(재설계는 sw-c2p로).

**08-11 오전 (의미론과 모델)**
- 과외 발견 ②: **owner_bound_handle 값이 원복 판정에 미사용** + **특정 핸들 취소를 지정할 입력 자체가 없음** — 모기의 "옛 핸들을 어디서 입력받는데?" 반복 질문이 직접 원인 (과외냥이가 관찰 메모로 명시).
- 모기 확정: **un-verify = 사용자 단위 전량 원복** (검증은 사람의 속성). docs 정합 워커(`6985ac3`) — 워커가 계약 밖 3번째 옛 문구까지 발견해 ask. **READY 2차.**
- 과외 발견 ③ (모델 층): bound_handle 제거 검토 → 자기 정정(author_handle은 불변 아님) → 상태 모델 재점검 요구. 마스터 전이표 전수 추적 → **모델 닫힘 판정.** 모기 결정 2건: 정체성은 사용자당 하나(이벤트 단위 취소·계정 이전 미지원) / bound_handle 유지 = 불변 감사 기록.
- 모기 시나리오 점검: 트위터 해킹·계정 이주 = 적대적 핸들 재활용 특수 케이스 → 커버 확인(고정 불가침 + 전량 원복 복구). 스키마 변경 0.

**08-11 낮 (보안과 2·3차)**
- 과외 발견 ④ (보안, P1급): **create_swatch가 클라이언트 author_handle 무검증 신뢰 → 귀속 위조 가능** (마스터 실측 확정). 모기 제품 결정 3건: 자기 트윗만 / 대리등록은 어드민 전용 / 출시판 미검증 유저 등록 서버 차단 → 별도 레인 sw-85a(launch gate 필수).
- codex 교차 리뷰 2차(fix 델타): **P1 0 · P2 1(lower 충돌 비원자 — 동시 승인 레이스) · P3 1(표기 카운트), 승인 보류.** 7/8 닫힘 확인.
- 2차 수리 워커: 부분 UNIQUE 인덱스 + 기존 데이터 정리 구문 + 표기 정합(`f22d5c9`). **READY 3차.**
- 모기 판단 지점 확정 2건: 공개 기여 = owner_uid 단일(대리등록은 운영 이력) / removal_requested_at 읽기 = 소유자·어드민만 → 3차 수리 ⏳.
- 모기 퀴즈 게이트 통과(정정 기록 = 답안, A1 초기 혼동 세션 내 정정).

## 발견 주체 분포 (누가 뭘 잡았나)

- **교차 리뷰(codex) 3회**: 구현 결함 8건 + 2건 + 2건 — 코드 레벨 결함과 경계 케이스에 강함. 한계도 드러남: 형제 미머지 PR을 못 봐서 이미 닫힌 건을 P2로 올렸다.
- **과외 게이트(모기+과외냥이) 4회**: 의미론 갭·모델 공백·보안 신뢰 경계 — **리뷰와 READY가 놓친 것들.** 공통점: "사용자 관점의 단순한 질문"에서 출발.
- **워커 자가 보고 5건**: 옛 문구 ask, 선재 실패(sw-rp3), SQL 주석 낡음, **RPC 가드 우회로**, **dev P0 회귀** — 뒤 둘은 계약 자체의 결함이었고 워커가 잡았다. 계약의 "범위 밖은 메모로 / 막히면 ask" 조항이 작동한 결과.

**08-11 오후 (게이트 레인과 두 번째 반전)**
- 등록 게이트 워커가 **RPC 가드만으론 무력함을 스스로 발견** — `insert_owner` 정책이 `created_by`만 보고 `author_handle`엔 제약이 없어, PostgREST 직접 테이블 POST로 가드를 통째로 우회. 트리거(최후 방어) 승인. 워커는 그 밖에도 계약이 지정한 42501이 레포 #277 결정(전용 SQLSTATE·메시지 파싱 금지)과 충돌함을 발견해 ask — 마스터 계약의 결함 2건이 워커에게 잡힘.
- PR #498 READY → 교차 리뷰 3차: **P1 0 · P2 3**(UPDATE 재활용 / service_role 비대칭 / 대소문자 identity). 마스터 재판정 유효 2건 — 대소문자 건은 #496의 부분 유니크 인덱스가 이미 닫음(리뷰는 `origin/dev` 기준이라 형제 미머지 PR을 구조적으로 못 봄).
- #496 머지(`cac1dc9`, 12:06). 2차 수리 착수 중 **P0 발견**: #496의 컬럼 GRANT 축소 + `create_swatch`의 `to_jsonb(swatches.*)` 조합으로 **dev에서 모든 로그인 유저의 등록이 42501로 죽어 있었다.** 워커가 자기 변경을 전부 걷어낸 dev 원본 상태로도 재현해 격리 증명. 원격 미적용 상태 — 소유자가 db push를 미뤄둔 덕에 프로덕션 무피해.
- 원격 데이터 감사: 불일치 76건 중 검증 유저 관련 15건은 전부 어드민 대리등록(모기 14 + 기니피그 1). 노출 0. 부수로 모기의 계정 2개 분리 구조가 드러남(어드민=메인, 검증 핸들=세컨).
- PR #498 머지(`07ba976`, 15:33) → db push → 원격 구조 쿼리 5개 통과 → **앱 UAT 4/4 통과.** 두 레인 종료.

## 결말

- 프로덕션 실작동 확인: 핸들 변경에도 귀속 유지 / 남의 핸들 등록 차단 / 어드민 대리등록 허용 / 미검증 등록 차단 / backfill 15건 복원.
- 총 4개 PR 라운드(#496 4라운드 + #498 2라운드), 교차 리뷰 3회, 워커 6명, P0 1건·P1 3건·P2 6건 처리.
- **READY 판정이 뒤집힌 횟수: 4회** (의미론 갭 / codex 2차 P2 / codex 3차 P2 / P0 회귀). 매번 뒤집은 주체가 달랐다 — 과외 게이트, 교차 리뷰, 교차 리뷰, 워커 자신.
- 부산물: 모델 전환 판정 규약 신설, 부팅 컨텍스트 최소화 원칙 명문화, 과외 메모 단일 수신함 배관, 절감 트랙 수단 2의 범위 구체화.
